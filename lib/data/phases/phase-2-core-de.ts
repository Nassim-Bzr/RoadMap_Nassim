import type { Week } from "../types";

export const PHASE2_CORE_DE: Week[] = [
  {
    id: "w11",
    title: "Docker",
    tasks: [
      {
        id: "d61",
        label: "Dockerfile + images",
        day: "Lun",
        description: "Docker = packager une app + ses deps dans une image isolée. 'It works on my machine' → DEAD. Indispensable pour déployer pipelines, partager environnements, scaler. Tout DE doit savoir écrire un Dockerfile.",
        url: "https://docs.docker.com/get-started/",
        resource: "Docker - Get Started",
        resources: [
          { type: "doc", title: "Docker Get Started", url: "https://docs.docker.com/get-started/" },
          { type: "doc", title: "Dockerfile Reference", url: "https://docs.docker.com/engine/reference/builder/" },
          { type: "article", title: "Best Practices Dockerfile", url: "https://docs.docker.com/develop/develop-images/dockerfile_best-practices/" },
          { type: "tool", title: "Play with Docker (sandbox)", url: "https://labs.play-with-docker.com/" },
        ],
        exercise: "# Objectif: containeriser un pipeline Python\n\n# 1. Dockerfile basique\n# FROM python:3.11-slim\n# WORKDIR /app\n# COPY requirements.txt .\n# RUN pip install --no-cache-dir -r requirements.txt\n# COPY . .\n# CMD [\"python\", \"-m\", \"my_pipeline\"]\n\n# 2. Build et run\n# docker build -t my-pipeline:0.1 .\n# docker run --rm my-pipeline:0.1\n# docker run -v $(pwd)/data:/app/data my-pipeline:0.1\n\n# 3. Dockerfile multi-stage (image plus petite)\n# # Stage 1: build\n# FROM python:3.11 AS builder\n# WORKDIR /build\n# COPY requirements.txt .\n# RUN pip install --user --no-cache-dir -r requirements.txt\n# \n# # Stage 2: runtime (slim)\n# FROM python:3.11-slim\n# WORKDIR /app\n# COPY --from=builder /root/.local /root/.local\n# ENV PATH=/root/.local/bin:$PATH\n# COPY . .\n# CMD [\"python\", \"-m\", \"my_pipeline\"]\n\n# Résultat: image de 300MB → 100MB\n\n# 4. Optimiser cache (ordre des COPY)\n# # MAUVAIS (rebuild deps si code change):\n# COPY . .\n# RUN pip install -r requirements.txt\n# \n# # BON (deps cachées tant que requirements.txt change pas):\n# COPY requirements.txt .\n# RUN pip install -r requirements.txt\n# COPY . .\n\n# 5. .dockerignore (réduire contexte build)\n# .git\n# .venv\n# __pycache__\n# *.pyc\n# tests/\n# notebooks/\n# data/raw/\n# .env\n\n# 6. Tags et registres\n# docker tag my-pipeline:0.1 myuser/my-pipeline:0.1\n# docker push myuser/my-pipeline:0.1\n# \n# # GitHub Container Registry\n# docker tag my-pipeline:0.1 ghcr.io/nassim/pipeline:0.1\n# docker push ghcr.io/nassim/pipeline:0.1\n\n# 7. Run avec env vars + volumes\n# docker run \\\n#   -e DATABASE_URL=postgres://... \\\n#   -e API_KEY=$API_KEY \\\n#   -v $(pwd)/data:/app/data \\\n#   -v $(pwd)/output:/app/output \\\n#   --name my-pipeline \\\n#   --rm \\\n#   my-pipeline:0.1\n\n# 8. Image size optimization\n# docker images   # voir tailles\n# Tips:\n# - Utiliser 'slim' ou 'alpine' base image\n# - Multi-stage build\n# - Combiner RUN (1 layer = 1 instruction)\n# - --no-cache-dir avec pip\n# - Supprimer apt cache: rm -rf /var/lib/apt/lists/*\n\n# 9. Health check\n# HEALTHCHECK --interval=30s --timeout=3s \\\n#   CMD curl -f http://localhost:8000/health || exit 1\n\n# 10. Non-root user (sécurité)\n# RUN useradd --create-home appuser\n# USER appuser",
      },
      {
        id: "d62",
        label: "Docker Compose multi-services",
        day: "Mar",
        description: "Docker Compose = orchestrer plusieurs containers (app + DB + cache + etc.) avec 1 fichier YAML. Setup dev local en 1 commande. Le format de référence pour pipelines locaux et prototypage.",
        url: "https://docs.docker.com/compose/",
        resource: "Docker Compose Docs",
        resources: [
          { type: "doc", title: "Docker Compose Docs", url: "https://docs.docker.com/compose/" },
          { type: "doc", title: "Compose File Spec", url: "https://docs.docker.com/compose/compose-file/" },
          { type: "github", title: "Awesome Compose (templates)", url: "https://github.com/docker/awesome-compose" },
        ],
        exercise: "# Objectif: stack data complète locale\n\n# docker-compose.yml\nversion: '3.9'\n\nservices:\n  postgres:\n    image: postgis/postgis:16-3.4\n    environment:\n      POSTGRES_USER: dev\n      POSTGRES_PASSWORD: dev\n      POSTGRES_DB: gpsdb\n    ports:\n      - '5432:5432'\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n      - ./init.sql:/docker-entrypoint-initdb.d/init.sql\n    healthcheck:\n      test: [CMD, pg_isready, -U, dev]\n      interval: 5s\n      retries: 5\n\n  redis:\n    image: redis:7-alpine\n    ports:\n      - '6379:6379'\n    healthcheck:\n      test: [CMD, redis-cli, ping]\n\n  pipeline:\n    build: .\n    depends_on:\n      postgres:\n        condition: service_healthy\n    environment:\n      DATABASE_URL: postgresql://dev:dev@postgres:5432/gpsdb\n      REDIS_URL: redis://redis:6379\n    volumes:\n      - ./src:/app/src\n      - ./data:/app/data\n    command: python -m my_pipeline\n\n  jupyter:\n    image: jupyter/scipy-notebook\n    ports:\n      - '8888:8888'\n    volumes:\n      - ./notebooks:/home/jovyan/work\n    environment:\n      JUPYTER_ENABLE_LAB: 'yes'\n\n  metabase:\n    image: metabase/metabase\n    ports:\n      - '3000:3000'\n    depends_on:\n      - postgres\n\nvolumes:\n  pgdata:\n\n# Commandes:\n# docker compose up -d              # start tout\n# docker compose logs -f pipeline   # logs d'un service\n# docker compose ps                 # status\n# docker compose exec postgres psql -U dev gpsdb\n# docker compose down -v            # stop + clean volumes\n\n# Networking auto:\n# Les services se voient par leur nom (postgres, redis)\n# DATABASE_URL=postgresql://dev:dev@postgres:5432/...\n#                                    ^^ nom du service\n\n# Profiles (services optionnels)\n# services:\n#   metabase:\n#     profiles: [analytics]\n#\n# docker compose --profile analytics up\n\n# Environment files\n# services:\n#   pipeline:\n#     env_file: .env\n\n# Override pour dev (compose.override.yml chargé auto)\n# services:\n#   pipeline:\n#     build: .\n#     volumes:\n#       - ./src:/app/src   # hot reload\n\n# Production: utilise un compose.prod.yml différent\n# docker compose -f docker-compose.yml -f compose.prod.yml up",
      },
      {
        id: "d63",
        label: "Containeriser pipeline production",
        day: "Mer",
        description: "Préparer ton image pour la prod : multi-stage build, user non-root, health checks, secrets via env vars, logs vers stdout. Différence dev/prod = essentielle.",
        url: "https://testdriven.io/blog/docker-best-practices/",
        resource: "Docker Best Practices (testdriven)",
        resources: [
          { type: "article", title: "Docker Production Best Practices", url: "https://testdriven.io/blog/docker-best-practices/" },
          { type: "doc", title: "Docker Security Best Practices", url: "https://docs.docker.com/develop/security-best-practices/" },
          { type: "doc", title: "12 Factor App", url: "https://12factor.net/" },
        ],
        exercise: "# Objectif: image production-ready\n\n# 1. Dockerfile pro complet\n# # syntax=docker/dockerfile:1.6\n# \n# # Stage 1: dependencies\n# FROM python:3.11-slim AS deps\n# WORKDIR /app\n# COPY requirements.txt .\n# RUN --mount=type=cache,target=/root/.cache/pip \\\n#     pip install --user --no-cache-dir -r requirements.txt\n# \n# # Stage 2: runtime\n# FROM python:3.11-slim\n# \n# # Sécurité: user non-root\n# RUN groupadd -r app && useradd -r -g app -d /app -s /sbin/nologin app\n# \n# # System deps minimaux\n# RUN apt-get update && apt-get install -y --no-install-recommends \\\n#     curl \\\n#     && rm -rf /var/lib/apt/lists/*\n# \n# WORKDIR /app\n# \n# # Copy deps depuis builder\n# COPY --from=deps /root/.local /home/app/.local\n# ENV PATH=/home/app/.local/bin:$PATH\n# \n# # Copy code\n# COPY --chown=app:app . .\n# \n# USER app\n# \n# # Health check\n# HEALTHCHECK --interval=30s --timeout=3s --retries=3 \\\n#     CMD curl -f http://localhost:8000/health || exit 1\n# \n# # Logs vers stdout (12-factor)\n# ENV PYTHONUNBUFFERED=1 \\\n#     PYTHONDONTWRITEBYTECODE=1\n# \n# EXPOSE 8000\n# CMD [\"python\", \"-m\", \"my_app\"]\n\n# 2. Multi-arch build (Mac M1 + Linux)\n# docker buildx create --use\n# docker buildx build --platform linux/amd64,linux/arm64 \\\n#     -t myuser/app:latest --push .\n\n# 3. Scan vulnerabilités\n# docker scout cves my-pipeline:0.1\n# Ou:\n# trivy image my-pipeline:0.1\n\n# 4. Signing (Cosign)\n# cosign sign --yes ghcr.io/nassim/pipeline:0.1\n\n# 5. .dockerignore optimal\n# .git\n# .gitignore\n# .github\n# .venv\n# venv\n# __pycache__\n# *.pyc\n# .pytest_cache\n# .mypy_cache\n# .coverage\n# .env\n# .env.*\n# tests/\n# notebooks/\n# *.md\n# docs/\n# data/\n\n# 6. Variables (12-factor compliant)\n# Toujours via ENV, jamais hardcodé\n# - DATABASE_URL\n# - LOG_LEVEL\n# - WORKERS\n# - CACHE_TTL\n\n# 7. Logs structurés JSON (production)\n# import structlog\n# logger = structlog.get_logger()\n# logger.info('processed', records=1000, duration_ms=234)\n# Output: {\"event\":\"processed\",\"records\":1000,...}\n# = parsable par Datadog, ELK, etc.\n\n# 8. Test image en local (mode prod)\n# docker run --rm \\\n#     -e DATABASE_URL=postgres://... \\\n#     -p 8000:8000 \\\n#     --memory=512m \\\n#     --cpus=0.5 \\\n#     my-pipeline:0.1\n\n# 9. Image size benchmark\n# - python:3.11        ~ 1GB\n# - python:3.11-slim   ~ 130MB  ✓ recommended\n# - python:3.11-alpine ~ 50MB   (mais glibc issues)\n\n# 10. Distroless (ultra-secure, micro)\n# FROM gcr.io/distroless/python3-debian12\n# COPY --from=builder /app /app\n# WORKDIR /app\n# CMD [\"my_app.py\"]\n# Image ~ 50MB sans shell (sécurité++)",
      },
      {
        id: "d64",
        label: "Docker networking & volumes",
        day: "Jeu",
        description: "Bridge network par défaut, custom networks, volumes pour persistance. Comprendre comment containers communiquent et stockent données. Indispensable pour debug en prod.",
        url: "https://docs.docker.com/network/",
        resource: "Docker - Networking",
        resources: [
          { type: "doc", title: "Docker Networking", url: "https://docs.docker.com/network/" },
          { type: "doc", title: "Docker Volumes", url: "https://docs.docker.com/storage/volumes/" },
          { type: "article", title: "Docker Networking Explained", url: "https://www.docker.com/blog/understanding-docker-networking/" },
        ],
        exercise: "# Objectif: networking et persistance\n\n# 1. Networks\n# docker network ls\n# Default: bridge, host, none\n\n# Créer network custom (services privés)\n# docker network create app-net\n# docker run --network app-net --name db postgres\n# docker run --network app-net app   # peut accéder à 'db'\n\n# 2. Compose networks\n# services:\n#   pipeline:\n#     networks: [internal]\n#   postgres:\n#     networks: [internal]\n#   nginx:\n#     networks: [internal, public]\n# networks:\n#   internal:\n#     internal: true   # pas d'accès Internet\n#   public:\n\n# 3. Ports\n# -p 8000:8000   # host:container\n# -p 127.0.0.1:8000:8000   # localhost only\n# -p 8000-8005:8000-8005   # range\n\n# 4. Volumes (persistance)\n# Type 1: Named volume (gérée par Docker)\n# docker volume create pgdata\n# docker run -v pgdata:/var/lib/postgresql/data postgres\n# Avantages: portable, optimisé OS, backup facile\n\n# Type 2: Bind mount (chemin host)\n# docker run -v /home/me/code:/app python\n# Avantages: dev (hot reload), accès direct\n# Inconvénients: dépend du host, perm issues\n\n# Type 3: tmpfs (RAM, éphémère)\n# docker run --tmpfs /tmp python\n# Pour secrets temporaires, données sensibles\n\n# 5. Volume inspection\n# docker volume ls\n# docker volume inspect pgdata\n# docker volume prune   # cleanup non utilisés\n\n# 6. Backup volume\n# docker run --rm \\\n#     -v pgdata:/source \\\n#     -v $(pwd):/backup \\\n#     alpine tar czf /backup/pgdata.tar.gz -C /source .\n\n# 7. Health checks (orchestration)\n# services:\n#   db:\n#     healthcheck:\n#       test: [CMD, pg_isready]\n#       interval: 10s\n#       timeout: 5s\n#       retries: 5\n#       start_period: 30s\n#   app:\n#     depends_on:\n#       db:\n#         condition: service_healthy   # attend health = OK\n\n# 8. DNS interne\n# Dans network custom, services accessibles par nom:\n# postgresql://user:pass@postgres:5432/db\n#                          ^^ nom du service\n\n# 9. Debug network\n# # Test connectivité depuis container\n# docker exec -it pipeline ping postgres\n# docker exec -it pipeline curl http://postgres:5432\n# docker exec -it pipeline nslookup postgres\n\n# # Voir IPs/ports d'un network\n# docker network inspect app-net\n\n# 10. Limites resources (production)\n# docker run \\\n#     --memory=512m \\\n#     --memory-swap=512m \\\n#     --cpus=0.5 \\\n#     --pids-limit=100 \\\n#     my-app",
      },
      {
        id: "d65",
        label: "Pipeline Docker complet",
        day: "Sam",
        description: "Projet portfolio : pipeline ETL conteneurisé avec Compose (Python + DB + viz). Démontre maîtrise complète Docker en contexte réel data engineering. À mettre en haut du CV.",
        url: "https://github.com/docker/awesome-compose",
        resource: "Awesome Compose Examples",
        resources: [
          { type: "github", title: "Awesome Compose Examples", url: "https://github.com/docker/awesome-compose" },
          { type: "doc", title: "Docker Production Checklist", url: "https://docs.docker.com/config/containers/start-containers-automatically/" },
          { type: "tool", title: "Dockerized data stack examples", url: "https://github.com/marketplace?category=&query=data+engineering&type=actions" },
        ],
        exercise: "# Projet: pipeline data complet dockerisé\n\n# Repo: github.com/nassim/dockerized-pipeline\n\n# Stack:\n# - Python pipeline (Polars + SQLAlchemy)\n# - PostgreSQL + PostGIS\n# - Streamlit dashboard\n# - Metabase analytics\n# - Adminer (DB UI)\n\n# Structure:\n# pipeline/\n# ├── docker-compose.yml\n# ├── docker-compose.prod.yml\n# ├── Dockerfile\n# ├── Makefile\n# ├── requirements.txt\n# ├── .env.example\n# ├── src/\n# │   └── pipeline/\n# │       ├── extract.py\n# │       ├── transform.py\n# │       └── load.py\n# ├── dashboard/\n# │   ├── Dockerfile\n# │   └── app.py\n# ├── sql/\n# │   └── init.sql\n# └── README.md\n\n# Makefile (UX++)\n# .PHONY: build up down logs clean\n# build:\n# \tdocker compose build\n# up:\n# \tdocker compose up -d\n# down:\n# \tdocker compose down\n# logs:\n# \tdocker compose logs -f pipeline\n# psql:\n# \tdocker compose exec postgres psql -U dev -d gpsdb\n# clean:\n# \tdocker compose down -v\n# test:\n# \tdocker compose run --rm pipeline pytest\n\n# README démarrage en 30s:\n# ## Quick Start\n# ```bash\n# git clone ...\n# cd dockerized-pipeline\n# cp .env.example .env\n# make up\n# # Pipeline: http://localhost:8501\n# # Metabase: http://localhost:3000\n# ```\n\n# Critères qualité:\n# ✓ make up démarre tout en 30s\n# ✓ Dashboard Streamlit fonctionnel\n# ✓ Données chargées auto à l'init\n# ✓ Tests passent dans container\n# ✓ Compose prod différent (no dev mounts, restart policies)\n# ✓ README avec architecture diagram\n# ✓ GIF de démo dans README\n\n# Patterns avancés:\n\n# 1. Init container (job qui run 1x au start)\n# services:\n#   db-init:\n#     build: .\n#     command: python -m pipeline.init_db\n#     depends_on:\n#       postgres: {condition: service_healthy}\n#     restart: 'no'\n\n# 2. Cron via Compose\n# services:\n#   cron:\n#     build: .\n#     command: ['crond', '-f']\n#     volumes:\n#       - ./crontab:/etc/crontabs/root\n\n# 3. Profiles (dev vs prod)\n# services:\n#   dev-tools:\n#     profiles: [dev]\n#   monitoring:\n#     profiles: [prod]\n# # docker compose --profile dev up\n\n# 4. Logging driver\n# services:\n#   pipeline:\n#     logging:\n#       driver: json-file\n#       options:\n#         max-size: '10m'\n#         max-file: '3'\n\n# 5. Restart policies\n# restart: unless-stopped   # auto-restart sauf stop manuel\n# restart: on-failure:5      # retry max 5x\n\n# 6. Ressources\n# deploy:\n#   resources:\n#     limits:\n#       cpus: '0.5'\n#       memory: 512M\n#     reservations:\n#       memory: 256M\n\n# Bonus pro: déployer ce stack sur serveur\n# - Hetzner Cloud (5€/mois)\n# - SSH + git pull + make up\n# - Reverse proxy: Caddy ou Traefik\n# - Backup DB: cron + restic + S3",
      },
    ],
  },
  {
    id: "w12",
    title: "Architecture Pipelines",
    tasks: [
      {
        id: "d66",
        label: "ETL vs ELT",
        day: "Lun",
        description: "ETL (transformation avant load) = approche classique, contrôle qualité. ELT (load brut puis transform en SQL) = approche moderne avec data warehouses (BigQuery, Snowflake). Compréhension critique pour designer pipelines.",
        url: "https://www.fivetran.com/blog/what-is-elt",
        resource: "Fivetran - ETL vs ELT",
        resources: [
          { type: "article", title: "ETL vs ELT (Fivetran)", url: "https://www.fivetran.com/blog/what-is-elt" },
          { type: "article", title: "Modern Data Stack (a16z)", url: "https://a16z.com/emerging-architectures-for-modern-data-infrastructure/" },
          { type: "video", title: "Seattle Data Guy - ETL vs ELT", url: "https://youtu.be/voC0ewDeltA" },
        ],
        exercise: "# Objectif: comprendre les paradigmes\n\n# ETL (classique)\n# Source → [transform sur ton infra] → Data Warehouse\n# \n# Avantages:\n# ✓ Schema enforced en amont\n# ✓ Garantit qualité data en DW\n# ✓ Réduit coût stockage DW\n# \n# Inconvénients:\n# ✗ Pas de re-traitement facile\n# ✗ Logique de transform en Python (vs SQL natif)\n# ✗ Casse si schema source change\n\n# ELT (moderne)\n# Source → Raw → [transform en SQL dans DW]\n# \n# Avantages:\n# ✓ Schema-on-read (flexible)\n# ✓ Re-process possible (raw conservé)\n# ✓ SQL = lingua franca (analystes peuvent contribuer)\n# ✓ Compute scalable du DW (BigQuery, Snowflake)\n# \n# Inconvénients:\n# ✗ Coût stockage data brute\n# ✗ Coût compute par requête\n\n# Stack moderne ELT:\n# Source → Fivetran/Airbyte → Snowflake/BigQuery → dbt → Looker/Metabase\n#          (extract+load)      (storage+compute)     (transform)  (BI)\n\n# Pattern hybride:\n# - Streaming: ETL (transform on-the-fly Kafka)\n# - Batch analytics: ELT\n# - ML features: ETL vers feature store\n\n# Décision matrix:\n# Volume < 100GB         → ETL ou ELT, peu importe\n# Volume > 1TB           → ELT (compute distribué)\n# Quasi-temps réel        → Streaming ETL\n# Multiple data sources   → ELT (load brut, transform commun)\n# Compliance stricte      → ETL (filtrer PII avant load)\n\n# Cas concret: pipeline GPS Renault\n# Source: 100GB/jour fichiers Parquet\n# \n# ETL approach:\n# 1. Read Parquet (Polars)\n# 2. Filter zones, calc speeds, anonymize\n# 3. Write aggregates to Postgres\n# Volumes ↓, mais re-process ↑\n# \n# ELT approach:\n# 1. Load all Parquet to S3 (raw)\n# 2. dbt models in Snowflake/Athena: filter, aggregate, dim\n# 3. Marts pour BI\n# Volumes ↑, mais flexibilité ↑\n\n# Outils:\n# Extract+Load (E+L):\n# - Airbyte (open source, 350+ connectors)\n# - Fivetran (managed, expensive)\n# - Singer (taps + targets)\n# - Custom Python (cas spécifiques)\n# \n# Transform (T):\n# - dbt (le standard)\n# - Coalesce\n# - Custom SQL\n\n# 12 questions pour choisir:\n# 1. Volume données ? 2. Vélocité (temps réel ?) 3. Variété (sources?)\n# 4. Coût stockage acceptable ? 5. Compétences team (SQL vs Python) ?\n# 6. Compliance/PII ? 7. Re-process probable ?\n# 8. Quel destinataire (BI, ML, app) ?\n# 9. Fréquence ? 10. Reliability needed ?\n# 11. Budget infra ? 12. Existing tooling ?",
      },
      {
        id: "d67",
        label: "Batch vs Streaming",
        day: "Mar",
        description: "Batch = traiter données par lots (toutes les heures, jours). Streaming = traiter event par event en temps réel. Choix dépend de la latence acceptable. Streaming = complexité++, Batch = simplicité++.",
        url: "https://www.confluent.io/learn/batch-vs-real-time-data-processing/",
        resource: "Confluent - Batch vs Streaming",
        resources: [
          { type: "article", title: "Batch vs Streaming (Confluent)", url: "https://www.confluent.io/learn/batch-vs-real-time-data-processing/" },
          { type: "article", title: "Lambda vs Kappa Architecture", url: "https://www.databricks.com/glossary/lambda-architecture" },
          { type: "video", title: "ByteByteGo - Streaming Explained", url: "https://youtu.be/4Spo2QRTz1k" },
          { type: "book", title: "Designing Data-Intensive Applications", url: "https://dataintensive.net/" },
        ],
        exercise: "# Objectif: choisir le bon paradigme\n\n# BATCH\n# Pattern: collecter pendant N heures, traiter d'un coup\n# Latence: minutes → jours\n# Outils: Airflow, Spark, dbt\n\n# Avantages:\n# ✓ Simple à raisonner\n# ✓ Re-traitement facile\n# ✓ Optimisé débit (throughput)\n# ✓ Coût/cas plus bas\n\n# Cas d'usage:\n# - Reports quotidiens\n# - ML training\n# - Aggregations historiques\n# - Backup, archivage\n\n# STREAMING\n# Pattern: chaque event traité < seconde\n# Latence: ms → secondes\n# Outils: Kafka, Flink, Spark Streaming, Materialize\n\n# Avantages:\n# ✓ Latence ultra basse\n# ✓ Réactivité (alertes temps réel)\n# ✓ Économies (pas batch overhead)\n\n# Inconvénients:\n# ✗ Complexité (state, exactly-once, watermarks)\n# ✗ Debug plus dur\n# ✗ Operations 24/7\n\n# Cas d'usage:\n# - Fraud detection\n# - Trading algorithmique\n# - Live dashboards (KPIs)\n# - Personnalisation (recos temps réel)\n# - IoT (alertes capteurs)\n\n# COMBINAISONS:\n\n# 1. Lambda Architecture\n# Source ──┬── Batch layer (historique) ──┐\n#          └── Speed layer (temps réel) ──┴── Serving layer\n# \n# Critique: 2 codebases à maintenir\n\n# 2. Kappa Architecture (Jay Kreps, Confluent)\n# Source → Streaming → Streaming → Serving\n# Tout en streaming, re-traitement = re-stream\n# Plus simple, mais plus contraint\n\n# 3. Hybrid (réaliste)\n# - Streaming: événements critiques temps réel\n# - Batch: aggregations + analytics jour\n# - Streaming → Batch storage à fin de période\n\n# Décision:\n\n# Q: Quelle latence acceptable ?\n# A: \n# - 24h: Batch nightly\n# - 1h: Batch hourly (Airflow)\n# - 5min: Micro-batch (Spark Streaming, dbt + freq)\n# - 1s: Streaming (Kafka + Flink)\n# - <100ms: Edge / in-memory (Redis, Materialize)\n\n# Q: Quel volume ?\n# A:\n# - 1M events/jour: batch suffit\n# - 100M events/jour: hybride\n# - 1B+: streaming + tiered storage\n\n# Q: Coût ?\n# A:\n# - Batch: optimal (utilise compute en off-peak)\n# - Streaming: 2-5x plus cher (serveurs 24/7)\n\n# Cas concret laverie GPS:\n# - Reports mensuels: Batch (Airflow + Polars)\n# - Dashboard temps réel positions: Streaming léger (Kafka → Redis → API)\n# - ML training: Batch (offline)\n# - Détection anomalie: Streaming (Flink CEP)\n\n# Snippet streaming simple (Python + Redis Streams)\nimport redis\nimport json\n\nr = redis.Redis()\n\n# Producer\nr.xadd('gps_stream', {\n    'lat': 48.85, 'lon': 2.35,\n    'vehicle': 'truck-123', 'speed': 50\n})\n\n# Consumer\nfor stream, msgs in r.xread({'gps_stream': '$'}, block=0):\n    for msg_id, data in msgs:\n        process_gps(data)",
      },
      {
        id: "d68",
        label: "Lake vs Warehouse vs Lakehouse",
        day: "Mer",
        description: "Data Lake = stockage brut bon marché (S3 + Parquet). Data Warehouse = structuré, optimisé queries (Snowflake, BigQuery). Lakehouse = combinaison (Delta Lake, Iceberg). Choix conditionne toute l'architecture.",
        url: "https://www.databricks.com/glossary/data-lakehouse",
        resource: "Databricks - Lakehouse",
        resources: [
          { type: "article", title: "What is Lakehouse (Databricks)", url: "https://www.databricks.com/glossary/data-lakehouse" },
          { type: "article", title: "Modern Data Stack guide", url: "https://www.moderndatastack.xyz/" },
          { type: "video", title: "Seattle DG - Lake/Warehouse/Lakehouse", url: "https://youtu.be/lzUpWjOhGKo" },
        ],
        exercise: "# Objectif: choisir l'architecture de stockage\n\n# DATA LAKE\n# = Storage object (S3, GCS, Azure Blob) + fichiers Parquet/JSON\n# \n# Avantages:\n# ✓ Très bon marché (~0.02$/GB/mois S3)\n# ✓ Schema-on-read (flexible)\n# ✓ Multi-format (CSV, JSON, Parquet, images, ML models)\n# ✓ Scale infini\n# \n# Inconvénients:\n# ✗ Pas de transactions ACID\n# ✗ Pas de schema enforcement\n# ✗ Pas d'index → queries lentes\n# ✗ Risque 'data swamp' (chaos)\n# \n# Outils: Athena, Trino/Presto, Spark, Polars\n\n# DATA WAREHOUSE\n# = Base de données spécialisée analytics\n# \n# Snowflake, BigQuery, Redshift, Synapse\n# \n# Avantages:\n# ✓ Performance queries (columnar, MPP)\n# ✓ Schema strict (qualité)\n# ✓ Transactions ACID\n# ✓ SQL standard\n# ✓ BI tools intégrés\n# \n# Inconvénients:\n# ✗ Coûteux ($1-10/TB scanned)\n# ✗ Format propriétaire (vendor lock-in)\n# ✗ Pas optimal pour ML/data science\n\n# DATA LAKEHOUSE (le futur)\n# = Lake (S3) + format ACID (Delta, Iceberg, Hudi)\n# \n# Promesse: combine forces des 2\n# - Stockage bas coût (S3)\n# - Transactions ACID (comme DW)\n# - Schema evolution\n# - Time travel (queries point-in-time)\n# - Multi-engine (Spark, Trino, Polars all readables)\n# \n# Outils:\n# - Delta Lake (Databricks, MIT)\n# - Apache Iceberg (Netflix, AWS, Apple)\n# - Apache Hudi (Uber)\n\n# DÉCISION:\n\n# Quand Data Lake suffit:\n# - Données semi-structurées massives (logs, IoT)\n# - Coût > performance\n# - ML training (besoin de raw)\n# - Archivage long terme\n\n# Quand Warehouse:\n# - BI / dashboards critiques\n# - Données structurées\n# - Compliance stricte\n# - Team SQL\n\n# Quand Lakehouse:\n# - Mix BI + ML\n# - Volumes croissants\n# - Évite vendor lock\n# - Multi-engine\n\n# Architecture moderne typique (Medallion):\n# \n# Bronze (raw, S3)\n#  └── Silver (cleaned, Delta)\n#       └── Gold (business marts, Delta + Snowflake mirror)\n\n# Stack open source complète:\n# Sources → Airbyte → S3 (bronze) → Spark/Polars → Delta (silver+gold)\n#                                                  ↓\n#                                              Trino + dbt → BI tools\n\n# Stack managed (cher mais simple):\n# Sources → Fivetran → Snowflake (everything) → Looker / Metabase\n\n# Cas Renault GPS:\n# Bronze: S3 raw Parquet (toutes positions)\n# Silver: Delta cleaned (filtré, deduplicated)\n# Gold: Marts par cas d'usage (mobilité, maintenance, sales)\n# Query: Trino sur Delta + dbt + Metabase\n\n# Coût estimation:\n# 1TB/jour de GPS\n# Lake (S3 standard): 23$/jour (storage)\n# Warehouse (Snowflake): 100-1000$/jour (storage + compute)\n# Lakehouse (S3 + Delta + Trino EC2): 30$/jour\n\n# 90% des startups: Lakehouse > Warehouse pur",
      },
      {
        id: "d69",
        label: "Star Schema modeling",
        day: "Jeu",
        description: "Star schema = modèle dimensionnel pour analytics (Kimball). 1 fact table centrale + N dimension tables autour. Standard incontournable du DW depuis 30 ans. Comprendre = pouvoir designer warehouses.",
        url: "https://www.kimballgroup.com/data-warehouse-business-intelligence-resources/kimball-techniques/dimensional-modeling-techniques/",
        resource: "Kimball Group - Dimensional Modeling",
        resources: [
          { type: "doc", title: "Kimball Group Techniques", url: "https://www.kimballgroup.com/data-warehouse-business-intelligence-resources/kimball-techniques/dimensional-modeling-techniques/" },
          { type: "book", title: "The Data Warehouse Toolkit (Kimball)", url: "https://www.kimballgroup.com/data-warehouse-business-intelligence-resources/books/data-warehouse-dw-toolkit/" },
          { type: "article", title: "Star vs Snowflake Schema", url: "https://www.holistics.io/blog/star-vs-snowflake-schema/" },
        ],
        exercise: "-- Objectif: modèle dimensionnel pour GPS\n\n-- 1. STAR SCHEMA\n--                  ┌─────────────┐\n--                  │  dim_date   │\n--                  └──────┬──────┘\n--                         │\n--   ┌──────────────┐  ┌───┴────────┐  ┌───────────────┐\n--   │ dim_vehicle  │──┤ fact_trips ├──┤ dim_location  │\n--   └──────────────┘  └───┬────────┘  └───────────────┘\n--                         │\n--                  ┌──────┴──────┐\n--                  │ dim_driver  │\n--                  └─────────────┘\n\n-- 2. Fact table (mesures + foreign keys)\nCREATE TABLE fact_trips (\n  trip_key BIGSERIAL PRIMARY KEY,\n  -- Foreign keys vers dimensions\n  date_key INT REFERENCES dim_date(date_key),\n  vehicle_key INT REFERENCES dim_vehicle(vehicle_key),\n  driver_key INT REFERENCES dim_driver(driver_key),\n  origin_location_key INT REFERENCES dim_location(location_key),\n  dest_location_key INT REFERENCES dim_location(location_key),\n  -- Métriques (additives)\n  distance_km NUMERIC(10,2),\n  duration_seconds INT,\n  fuel_liters NUMERIC(8,2),\n  revenue_eur NUMERIC(10,2),\n  -- Métriques semi-additives\n  start_odometer INT,\n  end_odometer INT,\n  -- Degenerate dimensions (IDs sources)\n  trip_id_source VARCHAR(50)\n);\n\n-- 3. Dimensions\nCREATE TABLE dim_date (\n  date_key INT PRIMARY KEY,   -- ex: 20260507\n  date_actual DATE,\n  year INT,\n  quarter INT,\n  month INT,\n  month_name VARCHAR(20),\n  day_of_month INT,\n  day_of_week INT,\n  day_name VARCHAR(20),\n  is_weekend BOOLEAN,\n  is_holiday BOOLEAN,\n  fiscal_year INT,\n  iso_week INT\n);\n-- Pré-population de 50 ans:\n-- INSERT INTO dim_date SELECT generate_series(...) ...\n\nCREATE TABLE dim_vehicle (\n  vehicle_key SERIAL PRIMARY KEY,\n  vehicle_id VARCHAR(50),\n  brand VARCHAR(50),\n  model VARCHAR(50),\n  year INT,\n  fuel_type VARCHAR(20),\n  fleet VARCHAR(50),\n  -- SCD Type 2 (slowly changing)\n  valid_from DATE,\n  valid_to DATE,\n  is_current BOOLEAN\n);\n\nCREATE TABLE dim_location (\n  location_key SERIAL PRIMARY KEY,\n  city VARCHAR(100),\n  region VARCHAR(50),\n  country VARCHAR(50),\n  population INT,\n  geo POINT\n);\n\n-- 4. Slowly Changing Dimensions (SCD)\n-- Type 1: overwrite (perdre historique)\n-- Type 2: nouveau record + dates valid_from/to (garde historique)\n-- Type 3: ajouter colonne 'previous_X' (garde 1 changement)\n\n-- Exemple SCD Type 2: véhicule change de fleet\n-- AVANT:\n-- vehicle_key=1, vehicle_id='V-100', fleet='taxi-blue', valid_from='2020-01-01', is_current=true\n-- APRÈS changement le 2026-05-07:\n-- vehicle_key=1, fleet='taxi-blue', valid_to='2026-05-07', is_current=false  -- ferme l'ancien\n-- vehicle_key=2, vehicle_id='V-100', fleet='taxi-red', valid_from='2026-05-07', is_current=true  -- nouveau\n\n-- 5. Queries analytics typiques\n-- Total km par mois et par fleet\nSELECT\n  d.year, d.month_name, v.fleet,\n  SUM(f.distance_km) as total_km,\n  COUNT(*) as n_trips\nFROM fact_trips f\nJOIN dim_date d ON f.date_key = d.date_key\nJOIN dim_vehicle v ON f.vehicle_key = v.vehicle_key\nWHERE d.year = 2026\nGROUP BY d.year, d.month, d.month_name, v.fleet\nORDER BY d.month, v.fleet;\n\n-- 6. Snowflake schema (variante)\n-- Dimension normalisée en sous-dimensions\n-- dim_location → dim_city → dim_region → dim_country\n-- Avantages: moins redondance\n-- Inconvénients: plus de joins (perf ↓)\n-- Star > Snowflake en analytics\n\n-- 7. Pattern: ETL pour fact table\n-- 1. Lookup foreign keys (souvent en cache)\n-- 2. Insert into fact (append-only typiquement)\n-- 3. Vérifier cohérence (no orphans)\n\n-- 8. Materialized views (perf)\nCREATE MATERIALIZED VIEW monthly_kpis AS\nSELECT d.year, d.month, COUNT(*) as n, SUM(f.distance_km) as km\nFROM fact_trips f JOIN dim_date d USING (date_key)\nGROUP BY d.year, d.month;\n\nREFRESH MATERIALIZED VIEW monthly_kpis;",
      },
      {
        id: "d70",
        label: "Idempotence & data contracts",
        day: "Ven",
        description: "Idempotence = relancer le pipeline 100x donne le même résultat. Critique car les pipelines plantent et doivent être rejouables. UPSERT vs INSERT, déduplication par hash. Concept clé pour fiabilité.",
        url: "https://maximebeauchemin.medium.com/functional-data-engineering-a-modern-paradigm-for-batch-data-processing-2327ec32c42a",
        resource: "Functional Data Engineering (Maxime Beauchemin)",
        resources: [
          { type: "article", title: "Functional Data Engineering", url: "https://maximebeauchemin.medium.com/functional-data-engineering-a-modern-paradigm-for-batch-data-processing-2327ec32c42a" },
          { type: "doc", title: "dbt - Idempotency", url: "https://docs.getdbt.com/blog/idempotent-data-transformations" },
          { type: "article", title: "Data Contracts Explained", url: "https://www.gable.ai/blog/data-contracts" },
        ],
        exercise: "# Objectif: pipelines rejouables sans pollution\n\n# 1. Idempotence: pourquoi ?\n# Cas réel:\n# - Pipeline plante à 80%\n# - Tu relances\n# - SANS idempotence: doublons en DB, bad data\n# - AVEC idempotence: idem que si avait fini du 1er coup\n\n# 2. Patterns Idempotence\n\n# A. DELETE + INSERT (simple, correct)\n# DELETE FROM trips_2026_05_07 WHERE date = '2026-05-07';\n# INSERT INTO trips SELECT ... WHERE date = '2026-05-07';\n\n# B. UPSERT (MERGE en SQL standard)\n# MERGE INTO trips t\n# USING staging s ON t.id = s.id\n# WHEN MATCHED THEN UPDATE SET ...\n# WHEN NOT MATCHED THEN INSERT ...;\n\n# C. INSERT IGNORE (deduplication par PK)\n# INSERT INTO trips ... ON CONFLICT (id) DO NOTHING;\n\n# D. Truncate partitions et re-load\n# Souvent utilisé en data warehouses\n# Très efficace + idempotent par construction\n\n# 3. Pattern Polars\nimport polars as pl\n\ndef process_day(date: str):\n    df = (\n        pl.scan_parquet(f's3://raw/{date}/*.parquet')\n        .filter(pl.col('valid'))\n        .collect()\n    )\n    \n    # Idempotent write: overwrite partition\n    df.write_parquet(\n        f's3://processed/year={date[:4]}/month={date[5:7]}/day={date[8:10]}/data.parquet'\n    )\n    # Re-run = écrase mêmes fichiers (S3 versioning si besoin de rollback)\n\n# 4. Watermarks (event time vs processing time)\n# Late arriving data: events qui arrivent en retard\n# Solution: re-process des fenêtres récentes\n\n# def reprocess_last_n_days(n: int = 7):\n#     for day in last_n_days(n):\n#         process_day(day)   # idempotent\n\n# 5. Data Contracts (Pydantic)\nfrom pydantic import BaseModel, Field, field_validator\nfrom datetime import datetime\n\nclass GPSEvent(BaseModel):\n    \"\"\"Contract pour GPS events. Toute violation = rejection.\"\"\"\n    vehicle_id: str = Field(min_length=1, max_length=50)\n    timestamp: datetime\n    latitude: float = Field(ge=-90, le=90)\n    longitude: float = Field(ge=-180, le=180)\n    speed_kmh: float = Field(ge=0, le=300)\n    \n    @field_validator('timestamp')\n    @classmethod\n    def not_future(cls, v):\n        if v > datetime.now():\n            raise ValueError('Timestamp in future')\n        return v\n\n# Validation au flow ingest\ndef ingest_event(raw: dict):\n    try:\n        event = GPSEvent(**raw)\n        store(event)\n    except ValidationError as e:\n        log.error('Bad event', extra={'error': str(e), 'data': raw})\n        send_to_dead_letter(raw)\n\n# 6. Data Contracts en YAML (pour dbt-like tools)\n# models/staging/gps_events.yml\n# version: 2\n# models:\n#   - name: stg_gps_events\n#     description: GPS events validated\n#     columns:\n#       - name: vehicle_id\n#         description: Vehicle unique ID\n#         tests:\n#           - not_null\n#           - relationships: { to: ref('dim_vehicles'), field: id }\n#       - name: timestamp\n#         tests:\n#           - not_null\n#       - name: speed_kmh\n#         tests:\n#           - not_null\n#           - dbt_utils.accepted_range:\n#               min_value: 0\n#               max_value: 300\n\n# 7. Schema evolution backward compatible\n# Règles:\n# ✓ Ajouter colonne = OK\n# ✓ Renommer colonne = casse (use alias)\n# ✓ Supprimer colonne = casse (deprecate d'abord)\n# ✓ Type change = casse (cast soft d'abord)\n\n# Outils:\n# - Schema Registry (Confluent, Avro)\n# - Apache Iceberg (schema evolution natif)\n# - dbt (warning si change)\n\n# 8. Audit trail\n# Chaque write = log:\n# - Source\n# - Timestamp\n# - Volume (records)\n# - Hash (data integrity)\n# - Version pipeline\n\n# Permet: 'Que s'est-il passé le 7 mai à 4h ?'",
      },
    ],
  },
];
