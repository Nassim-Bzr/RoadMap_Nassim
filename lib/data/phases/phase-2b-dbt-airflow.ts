import type { Week } from "../types";

export const PHASE2_DBT_AIRFLOW: Week[] = [
  {
    id: "w13",
    title: "dbt (Data Build Tool)",
    tasks: [
      {
        id: "d71",
        label: "dbt install + configuration",
        day: "Lun",
        description: "dbt = transformer données dans le warehouse avec SQL + Jinja. Standard de fait du modern data stack. Permet aux analystes d'écrire des transforms versionnées, testées, documentées. Utilisé par 9000+ entreprises (Snowflake, JPMorgan, Spotify).",
        url: "https://docs.getdbt.com/docs/introduction",
        resource: "dbt - Documentation",
        resources: [
          { type: "doc", title: "dbt Documentation", url: "https://docs.getdbt.com/docs/introduction" },
          { type: "course", title: "dbt Fundamentals (gratuit)", url: "https://courses.getdbt.com/courses/fundamentals" },
          { type: "github", title: "dbt-utils (utility macros)", url: "https://github.com/dbt-labs/dbt-utils" },
          { type: "tool", title: "dbt Cloud (free dev tier)", url: "https://www.getdbt.com/signup" },
        ],
        exercise: "# Objectif: setup dbt + 1er modèle\n\n# 1. Installation\nuv pip install dbt-postgres   # ou dbt-snowflake, dbt-bigquery, dbt-duckdb\n# Adapter par DB cible\n\n# 2. Init project\ndbt init my_project\ncd my_project\n\n# Structure créée:\n# my_project/\n# ├── dbt_project.yml         # config projet\n# ├── models/\n# │   ├── staging/\n# │   ├── intermediate/\n# │   └── marts/\n# ├── tests/\n# ├── macros/\n# ├── seeds/                   # CSV files\n# ├── snapshots/                # SCD Type 2\n# └── analyses/\n\n# 3. profiles.yml (~/.dbt/profiles.yml)\n# my_project:\n#   target: dev\n#   outputs:\n#     dev:\n#       type: postgres\n#       host: localhost\n#       user: dev\n#       password: \"{{ env_var('DB_PASSWORD') }}\"\n#       port: 5432\n#       dbname: gpsdb\n#       schema: dev_nassim\n#       threads: 4\n#     prod:\n#       type: postgres\n#       host: prod.example.com\n#       schema: analytics\n\n# 4. Test connection\ndbt debug\n# Doit afficher 'Connection test: OK!'\n\n# 5. dbt_project.yml (config)\n# name: my_project\n# version: '1.0.0'\n# profile: my_project\n# \n# model-paths: [models]\n# test-paths: [tests]\n# \n# models:\n#   my_project:\n#     staging:\n#       +materialized: view       # vues légères\n#     marts:\n#       +materialized: table       # tables matérialisées\n\n# 6. Premier modèle (models/staging/stg_trips.sql)\n# {{ config(materialized='view') }}\n# \n# WITH source AS (\n#     SELECT * FROM {{ source('raw','trips') }}\n# )\n# SELECT\n#     id AS trip_id,\n#     vehicle_id,\n#     city,\n#     ts AT TIME ZONE 'UTC' AS started_at_utc,\n#     duration_sec / 60.0 AS duration_min,\n#     distance_km,\n#     CASE\n#         WHEN distance_km / NULLIF(duration_sec, 0) * 3600 > 200 THEN TRUE\n#         ELSE FALSE\n#     END AS is_anomaly\n# FROM source\n# WHERE distance_km IS NOT NULL\n\n# 7. Sources (models/staging/_sources.yml)\n# version: 2\n# sources:\n#   - name: raw\n#     description: Raw GPS data from S3\n#     schema: raw\n#     tables:\n#       - name: trips\n#         description: Trip events from vehicles\n#         columns:\n#           - name: id\n#             tests: [unique, not_null]\n\n# 8. Run !\ndbt run                              # tous models\ndbt run --select stg_trips           # 1 model\ndbt run --select staging             # tout un dossier\ndbt run --select +marts              # avec deps amont\ndbt run --select state:modified      # seulement changés\n\n# 9. Test\ndbt test\ndbt test --select stg_trips\n\n# 10. Doc\ndbt docs generate\ndbt docs serve   # ouvre site sur :8080 avec lineage graph !",
      },
      {
        id: "d72",
        label: "Models & materializations",
        day: "Mar",
        description: "Materializations = comment dbt stocke ton modèle. View (vue, gratuite, lente), Table (matérialisée, rapide), Incremental (append-only, perf++). Le bon choix dépend de la taille et fréquence d'usage.",
        url: "https://docs.getdbt.com/docs/build/materializations",
        resource: "dbt - Materializations",
        resources: [
          { type: "doc", title: "dbt Materializations", url: "https://docs.getdbt.com/docs/build/materializations" },
          { type: "doc", title: "Incremental Models", url: "https://docs.getdbt.com/docs/build/incremental-models" },
          { type: "article", title: "When to use which materialization", url: "https://www.getdbt.com/blog/the-most-popular-dbt-materializations" },
        ],
        exercise: "-- Objectif: choisir bonne materialization\n\n-- 1. VIEW (default pour staging)\n-- Légère, query exécutée à chaque SELECT\n-- {{ config(materialized='view') }}\n-- \n-- Utilise quand:\n-- ✓ Données petites/moyennes\n-- ✓ Source change souvent\n-- ✓ Logique simple\n\n-- 2. TABLE (default pour marts)\n-- Recréée à chaque dbt run\n-- {{ config(materialized='table') }}\n-- \n-- Utilise quand:\n-- ✓ Datasets moyens/gros (cache résultat)\n-- ✓ Lecture fréquente\n-- ✓ Calcul cher (joins, aggregates)\n\n-- 3. INCREMENTAL (gros volumes, append)\n-- Insère/update seulement les nouveaux records\n-- {{ config(\n--     materialized='incremental',\n--     unique_key='trip_id',\n--     on_schema_change='append_new_columns'\n-- ) }}\n-- \n-- SELECT *\n-- FROM {{ ref('stg_trips') }}\n-- {% if is_incremental() %}\n--   WHERE updated_at > (SELECT MAX(updated_at) FROM {{ this }})\n-- {% endif %}\n-- \n-- Utilise quand:\n-- ✓ Gros volumes (>10M rows)\n-- ✓ Append-mostly (logs, events)\n-- ✓ Batch quotidien/horaire\n\n-- 4. EPHEMERAL (CTE inlined)\n-- Pas matérialisé, intégré au SQL des descendants\n-- {{ config(materialized='ephemeral') }}\n-- \n-- Utilise quand:\n-- ✓ Logique réutilisable, mais pas worth de matérialiser\n-- ✓ Réduit le nombre de tables\n\n-- 5. Custom: Snapshot (SCD Type 2)\n-- snapshots/vehicles_snapshot.sql\n-- {% snapshot vehicles_snapshot %}\n--   {{ config(\n--       target_schema='snapshots',\n--       unique_key='id',\n--       strategy='check',\n--       check_cols=['fleet','brand','model'],\n--   ) }}\n--   SELECT * FROM {{ source('raw','vehicles') }}\n-- {% endsnapshot %}\n\n-- dbt snapshot   ← exécute snapshots\n\n-- 6. Decision matrix:\n-- Volume    Logique    → Choix\n-- < 1M      Simple     → view\n-- 1-100M    Cher       → table\n-- > 100M    Append     → incremental\n-- < 1M      Réutilisé  → ephemeral\n-- Historique requis     → snapshot\n\n-- 7. Patterns avancés\n\n-- Incremental avec UPSERT (merge)\n-- {{ config(\n--     materialized='incremental',\n--     unique_key='id',\n--     incremental_strategy='merge'\n-- ) }}\n\n-- Insert overwrite (BigQuery / Spark)\n-- {{ config(\n--     materialized='incremental',\n--     incremental_strategy='insert_overwrite',\n--     partition_by={'field':'date','data_type':'date'}\n-- ) }}\n\n-- 8. Indexes (Postgres)\n-- {{ config(\n--     materialized='table',\n--     indexes=[\n--         {'columns':['vehicle_id'], 'type':'btree'},\n--         {'columns':['city','date'], 'type':'btree'},\n--     ]\n-- ) }}\n\n-- 9. Pre/post hooks\n-- {{ config(\n--     post_hook=\"VACUUM ANALYZE {{ this }}\",\n--     pre_hook=\"SET search_path TO analytics\"\n-- ) }}\n\n-- 10. Refresh strategy\n-- - dbt run                       quotidien\n-- - dbt run --select +my_mart    sur changement\n-- - dbt run --full-refresh        rebuild incrémental\n-- - dbt build                     run + test combinés",
      },
      {
        id: "d73",
        label: "Sources, refs, lineage",
        day: "Mer",
        description: "{{ ref('model') }} = référence à un autre modèle dbt (génère lineage). {{ source('schema','table') }} = référence à donnée externe. dbt construit auto le DAG de dépendances + génère docs visuelles. Lineage = traçabilité.",
        url: "https://docs.getdbt.com/docs/build/sources",
        resource: "dbt - Sources & Refs",
        resources: [
          { type: "doc", title: "dbt Sources", url: "https://docs.getdbt.com/docs/build/sources" },
          { type: "doc", title: "dbt ref()", url: "https://docs.getdbt.com/reference/dbt-jinja-functions/ref" },
          { type: "doc", title: "dbt Documentation", url: "https://docs.getdbt.com/docs/collaborate/documentation" },
        ],
        exercise: "-- Objectif: lineage propre, doc auto-générée\n\n-- 1. Sources (entrées du DAG)\n-- models/sources.yml\n-- version: 2\n-- sources:\n--   - name: raw\n--     database: production_db\n--     schema: raw_data\n--     loaded_at_field: ingested_at   # freshness check\n--     freshness:\n--       warn_after: {count: 12, period: hour}\n--       error_after: {count: 24, period: hour}\n--     tables:\n--       - name: trips\n--         description: Raw GPS trip events\n--         loaded_at_field: ingested_at\n--         columns:\n--           - name: id\n--             description: Trip unique ID\n--             tests:\n--               - unique\n--               - not_null\n--           - name: vehicle_id\n--             tests:\n--               - relationships:\n--                   to: source('raw','vehicles')\n--                   field: id\n--       - name: vehicles\n--         description: Fleet metadata\n\n-- Test source freshness\n-- dbt source freshness\n\n-- 2. Référencer source\n-- models/staging/stg_trips.sql\n-- WITH source AS (\n--     SELECT * FROM {{ source('raw','trips') }}\n-- )\n-- SELECT id AS trip_id, ... FROM source\n\n-- 3. Référencer autre modèle (ref)\n-- models/marts/fct_daily_trips.sql\n-- WITH trips AS (\n--     SELECT * FROM {{ ref('stg_trips') }}\n-- ),\n-- vehicles AS (\n--     SELECT * FROM {{ ref('stg_vehicles') }}\n-- )\n-- SELECT\n--     DATE_TRUNC('day', t.started_at) AS day,\n--     v.fleet,\n--     COUNT(*) AS n_trips,\n--     SUM(t.distance_km) AS total_km\n-- FROM trips t\n-- JOIN vehicles v ON t.vehicle_id = v.id\n-- GROUP BY 1, 2\n\n-- 4. Documentation (descriptions partout)\n-- models/marts/_marts.yml\n-- version: 2\n-- models:\n--   - name: fct_daily_trips\n--     description: |\n--       Daily aggregated trips per vehicle fleet.\n--       Updated every day at 4 AM UTC.\n--     columns:\n--       - name: day\n--         description: '{{ doc(\"day_dimension\") }}'\n--         tests: [not_null]\n--       - name: fleet\n--         description: Vehicle fleet name\n--       - name: n_trips\n--         description: Number of trips that day\n--         tests: [not_null]\n\n-- 5. Doc blocks (réutilisables)\n-- models/docs.md\n-- {% docs day_dimension %}\n-- The day on which trips occurred. Truncated from started_at.\n-- {% enddocs %}\n\n-- 6. Génère et serve\n-- dbt docs generate     # build site dans target/\n-- dbt docs serve         # ouvre :8080\n-- → lineage graph interactif (clique modèles)\n-- → search modèles, columns, descriptions\n\n-- 7. Naming conventions\n-- staging/    → stg_<source>__<table>      ex: stg_raw__trips\n-- intermediate/ → int_<entity>_<verb>       ex: int_trips_pivoted\n-- marts/      → fct_<entity>, dim_<entity>  ex: fct_trips, dim_vehicles\n\n-- 8. dbt-utils (macros utilitaires)\n-- packages.yml\n-- packages:\n--   - package: dbt-labs/dbt_utils\n--     version: 1.1.1\n-- \n-- dbt deps   ← installe\n\n-- Utilisations:\n-- {{ dbt_utils.surrogate_key(['col_a','col_b']) }}   -- hash key\n-- {{ dbt_utils.pivot('column', dbt_utils.get_column_values(table=ref('x'), column='y')) }}\n-- {{ dbt_utils.date_spine(start_date='2020-01-01', datepart='day') }}\n\n-- 9. Lineage utility\n-- dbt list --select +my_model+    -- amont ET aval\n-- dbt list --select +my_model     -- amont (deps)\n-- dbt list --select my_model+     -- aval (descendants)\n-- dbt run --select state:modified+ -- modifs + descendants\n\n-- 10. Best practices structure\n-- models/\n-- ├── staging/                ← 1 modèle = 1 source table\n-- │   ├── _stg_models.yml\n-- │   ├── stg_raw__trips.sql\n-- │   └── stg_raw__vehicles.sql\n-- ├── intermediate/            ← logique réutilisable\n-- │   └── int_trips_enriched.sql\n-- └── marts/                   ← business-ready tables\n--     ├── core/\n--     │   ├── fct_trips.sql\n--     │   └── dim_vehicles.sql\n--     └── analytics/\n--         └── kpi_monthly.sql",
      },
      {
        id: "d74",
        label: "Tests & macros",
        day: "Jeu",
        description: "Tests dbt = SQL queries qui doivent renvoyer 0 lignes (sinon fail). 4 tests built-in (not_null, unique, accepted_values, relationships). Custom tests = SQL libre. Macros = Jinja réutilisable. Tests = qualité data garantie.",
        url: "https://docs.getdbt.com/docs/build/data-tests",
        resource: "dbt - Tests",
        resources: [
          { type: "doc", title: "dbt Tests Docs", url: "https://docs.getdbt.com/docs/build/data-tests" },
          { type: "doc", title: "dbt Macros (Jinja)", url: "https://docs.getdbt.com/docs/build/jinja-macros" },
          { type: "github", title: "dbt-expectations (Great Expectations port)", url: "https://github.com/calogica/dbt-expectations" },
          { type: "github", title: "dbt-utils tests", url: "https://github.com/dbt-labs/dbt-utils#generic-tests" },
        ],
        exercise: "-- Objectif: garantie qualité avec tests\n\n-- 1. Tests built-in (4 disponibles)\n-- models/_models.yml\n-- version: 2\n-- models:\n--   - name: stg_trips\n--     columns:\n--       - name: id\n--         tests:\n--           - not_null\n--           - unique\n--       - name: status\n--         tests:\n--           - accepted_values:\n--               values: ['completed','cancelled','ongoing']\n--       - name: vehicle_id\n--         tests:\n--           - relationships:\n--               to: ref('stg_vehicles')\n--               field: id\n\n-- 2. Tests dbt-utils (très utiles)\n-- - dbt_utils.expression_is_true:\n--     expression: 'distance_km > 0 AND distance_km < 10000'\n-- - dbt_utils.unique_combination_of_columns:\n--     combination_of_columns: [vehicle_id, started_at]\n-- - dbt_utils.recency:\n--     datepart: day\n--     field: created_at\n--     interval: 7\n-- - dbt_utils.accepted_range:\n--     min_value: 0\n--     max_value: 200\n\n-- 3. Tests dbt-expectations (Great Expectations style)\n-- - dbt_expectations.expect_column_values_to_be_between:\n--     min_value: 0\n--     max_value: 1000\n-- - dbt_expectations.expect_table_row_count_to_be_between:\n--     min_value: 1000\n-- - dbt_expectations.expect_column_distinct_count_to_be_greater_than:\n--     value: 10\n\n-- 4. Tests SQL custom (singular)\n-- tests/no_negative_distance.sql\n-- SELECT * FROM {{ ref('stg_trips') }}\n-- WHERE distance_km < 0\n-- → fail si > 0 lignes retournées\n\n-- 5. Tests génériques (réutilisables)\n-- macros/test_no_nulls_in_columns.sql\n-- {% test no_nulls_in_columns(model, columns) %}\n--   SELECT *\n--   FROM {{ model }}\n--   WHERE 1=2\n--   {% for col in columns %}\n--     OR {{ col }} IS NULL\n--   {% endfor %}\n-- {% endtest %}\n\n-- Usage:\n-- - name: stg_trips\n--   tests:\n--     - no_nulls_in_columns:\n--         columns: [id, vehicle_id, started_at]\n\n-- 6. Run tests\n-- dbt test\n-- dbt test --select stg_trips        -- 1 model\n-- dbt test --select tag:critical     -- par tag\n-- dbt build                          -- run + test ensemble\n\n-- 7. Macros (Jinja réutilisable)\n-- macros/get_status_label.sql\n-- {% macro get_status_label(status_col) %}\n--   CASE {{ status_col }}\n--     WHEN 'C' THEN 'Completed'\n--     WHEN 'X' THEN 'Cancelled'\n--     ELSE 'Other'\n--   END\n-- {% endmacro %}\n\n-- Usage dans modèle:\n-- SELECT id, {{ get_status_label('status') }} AS status_label\n-- FROM {{ source('raw','trips') }}\n\n-- 8. Macro avec pre-hook (auto-run)\n-- macros/grant_select.sql\n-- {% macro grant_select(schema_name, role) %}\n--   GRANT USAGE ON SCHEMA {{ schema_name }} TO {{ role }};\n--   GRANT SELECT ON ALL TABLES IN SCHEMA {{ schema_name }} TO {{ role }};\n-- {% endmacro %}\n\n-- dbt_project.yml:\n-- on-run-end:\n--   - \"{{ grant_select('analytics', 'looker') }}\"\n\n-- 9. Severity\n-- - name: id\n--   tests:\n--     - not_null:\n--         severity: error    -- défaut, fail le run\n--     - unique:\n--         severity: warn      -- log warning seulement\n\n-- 10. Tags & meta\n-- - name: fct_trips\n--   tags: ['critical', 'daily']\n--   meta:\n--     owner: 'data-team'\n--     pii: false\n\n-- dbt run --select tag:critical\n-- dbt test --select tag:daily\n\n-- 11. Pattern: contracts (dbt 1.5+)\n-- - name: fct_trips\n--   config:\n--     contract:\n--       enforced: true        -- valide schema strict au build\n--   columns:\n--     - name: id\n--       data_type: bigint\n--       constraints: [{type: not_null}, {type: unique}]\n\n-- 12. Documentation alternative (vue Tableau, GraphQL)\n-- dbt docs generate\n-- → JSON dans target/manifest.json + catalog.json\n-- Réutilisable par outils tiers (Hightouch, Castor, Atlan)",
      },
      {
        id: "d75",
        label: "Pipeline dbt complet",
        day: "Sam",
        description: "Projet portfolio: ELT complet avec dbt sur dataset open data. Démontre maîtrise du modern data stack. Très demandé en entretien (dbt = compétence #1 demandée Data Engineer 2025).",
        url: "https://github.com/dbt-labs/jaffle_shop",
        resource: "dbt Labs - Jaffle Shop (référence)",
        resources: [
          { type: "github", title: "Jaffle Shop (référence)", url: "https://github.com/dbt-labs/jaffle_shop" },
          { type: "tool", title: "dbt-duckdb (local sans cloud)", url: "https://github.com/duckdb/dbt-duckdb" },
          { type: "course", title: "dbt Learn (gratuit)", url: "https://courses.getdbt.com/" },
        ],
        exercise: "-- Projet: pipeline ELT mobilité avec dbt\n\n-- Repo: github.com/nassim/dbt-mobility-pipeline\n\n-- Stack:\n-- - DuckDB (local, gratuit, rapide) ou PostGIS\n-- - dbt-duckdb (ou dbt-postgres)\n-- - Données: Vélib Paris (data.gouv) ou Bike-sharing dataset Kaggle\n\n-- Architecture:\n-- raw_velib_trips ──┐\n-- raw_velib_stations ─┼─→ staging ──→ intermediate ──→ marts ──→ exposures\n-- raw_weather ──────┘\n\n-- 1. Sources\n-- models/staging/_sources.yml\n-- sources:\n--   - name: raw\n--     tables:\n--       - name: velib_trips\n--       - name: velib_stations\n--       - name: weather_paris\n\n-- 2. Staging (1 model par source table)\n-- models/staging/stg_velib__trips.sql\n-- WITH source AS (SELECT * FROM {{ source('raw','velib_trips') }}),\n-- renamed AS (\n--     SELECT\n--         trip_id,\n--         CAST(started_at AS TIMESTAMP) AS started_at,\n--         CAST(ended_at AS TIMESTAMP) AS ended_at,\n--         start_station_id,\n--         end_station_id,\n--         DATEDIFF('second', started_at, ended_at) AS duration_seconds,\n--         user_type\n--     FROM source\n--     WHERE started_at IS NOT NULL\n-- )\n-- SELECT * FROM renamed\n\n-- 3. Intermediate (logique réutilisable)\n-- models/intermediate/int_trips_enriched.sql\n-- WITH trips AS (SELECT * FROM {{ ref('stg_velib__trips') }}),\n-- stations AS (SELECT * FROM {{ ref('stg_velib__stations') }}),\n-- weather AS (SELECT * FROM {{ ref('stg_velib__weather') }})\n-- SELECT\n--     t.*,\n--     s_start.name AS start_station_name,\n--     s_start.lat AS start_lat, s_start.lon AS start_lon,\n--     s_end.name AS end_station_name,\n--     ST_Distance(\n--         ST_Point(s_start.lon, s_start.lat),\n--         ST_Point(s_end.lon, s_end.lat)\n--     ) * 111000 AS distance_m,\n--     w.temp_c, w.precip_mm\n-- FROM trips t\n-- LEFT JOIN stations s_start ON t.start_station_id = s_start.id\n-- LEFT JOIN stations s_end ON t.end_station_id = s_end.id\n-- LEFT JOIN weather w ON DATE_TRUNC('hour', t.started_at) = w.hour\n\n-- 4. Marts (business tables)\n-- models/marts/core/fct_trips.sql (incremental)\n-- {{ config(materialized='incremental', unique_key='trip_id') }}\n-- SELECT * FROM {{ ref('int_trips_enriched') }}\n-- {% if is_incremental() %}\n-- WHERE started_at > (SELECT MAX(started_at) FROM {{ this }})\n-- {% endif %}\n\n-- models/marts/analytics/kpi_daily.sql\n-- SELECT\n--     DATE_TRUNC('day', started_at) AS day,\n--     user_type,\n--     COUNT(*) AS n_trips,\n--     AVG(duration_seconds / 60.0) AS avg_duration_min,\n--     SUM(distance_m / 1000.0) AS total_km,\n--     AVG(temp_c) AS avg_temp\n-- FROM {{ ref('fct_trips') }}\n-- GROUP BY 1, 2\n\n-- 5. Tests\n-- _models.yml:\n-- - name: fct_trips\n--   tests:\n--     - dbt_utils.unique_combination_of_columns:\n--         combination_of_columns: [trip_id]\n--     - dbt_expectations.expect_table_row_count_to_be_between:\n--         min_value: 1000\n--   columns:\n--     - name: duration_seconds\n--       tests:\n--         - dbt_utils.accepted_range:\n--             min_value: 60         -- min 1 min\n--             max_value: 86400      -- max 24h\n\n-- 6. Documentation riche\n-- - Description par modèle/colonne\n-- - Doc blocks pour metrics complexes\n-- - dbt docs serve → site shareable\n\n-- 7. CI GitHub Actions\n-- .github/workflows/dbt.yml:\n-- - name: dbt run + test\n--   run: |\n--     dbt deps\n--     dbt build\n\n-- 8. Exposures (downstream tools)\n-- exposures.yml:\n-- exposures:\n--   - name: dashboard_velib\n--     type: dashboard\n--     url: https://metabase.example.com/dashboard/123\n--     depends_on:\n--       - ref('kpi_daily')\n--     owner: {name: Nassim, email: ...}\n\n-- 9. Snapshots (changements stations)\n-- snapshots/stations_snapshot.sql:\n-- {% snapshot stations %}\n--   {{ config(strategy='check', check_cols=['name','status']) }}\n--   SELECT * FROM {{ source('raw','velib_stations') }}\n-- {% endsnapshot %}\n\n-- 10. README pro\n-- ## Pipeline ELT Vélib avec dbt\n-- - Stack: dbt + DuckDB + Python\n-- - 16 modèles SQL\n-- - 47 tests\n-- - Lineage: 3 niveaux (staging → int → marts)\n-- - Run: `make dbt-build`\n-- - Documentation: link vers GitHub Pages docs\n-- \n-- ![Lineage screenshot](docs/lineage.png)\n-- \n-- ## Insights\n-- - Top 10 stations\n-- - Pattern temporel weekend\n-- - Impact météo\n\n-- Bonus pro:\n-- - dbt-osmosis (auto-doc colonnes)\n-- - elementary (data quality monitoring)\n-- - Steampipe (query GitHub via SQL)",
      },
    ],
  },
  {
    id: "w14",
    title: "Airflow",
    tasks: [
      {
        id: "d76",
        label: "DAGs, Tasks, Operators",
        day: "Lun",
        description: "Airflow = orchestrateur de pipelines. DAG = workflow de tasks dépendantes. Standard de fait pour pipelines batch (Airbnb, Lyft, Stripe). Compétence indispensable pour Data Engineer.",
        url: "https://airflow.apache.org/docs/apache-airflow/stable/tutorial.html",
        resource: "Airflow - Tutorial",
        resources: [
          { type: "doc", title: "Airflow Tutorial", url: "https://airflow.apache.org/docs/apache-airflow/stable/tutorial.html" },
          { type: "course", title: "Astronomer Academy (gratuit)", url: "https://academy.astronomer.io/" },
          { type: "tool", title: "Astro CLI (Airflow local)", url: "https://docs.astronomer.io/astro/cli/install-cli" },
          { type: "github", title: "Airflow Examples", url: "https://github.com/apache/airflow/tree/main/airflow/example_dags" },
        ],
        exercise: "# Objectif: 1er DAG fonctionnel\n\n# Setup avec Astro CLI (le plus simple)\n# brew install astro\n# astro dev init my-airflow\n# cd my-airflow\n# astro dev start    # Airflow UI sur :8080\n\n# 1. DAG basique\n# dags/etl_gps.py\nfrom airflow.decorators import dag, task\nfrom datetime import datetime, timedelta\nimport pendulum\n\n@dag(\n    dag_id='etl_gps_pipeline',\n    start_date=pendulum.datetime(2026, 1, 1, tz='Europe/Paris'),\n    schedule='@daily',           # cron: '0 4 * * *'\n    catchup=False,                # ne pas re-run historique\n    max_active_runs=1,            # 1 run à la fois\n    tags=['gps','daily'],\n    default_args={\n        'owner': 'nassim',\n        'retries': 2,\n        'retry_delay': timedelta(minutes=5),\n    }\n)\ndef etl_pipeline():\n    \n    @task\n    def extract():\n        import polars as pl\n        df = pl.read_csv('s3://bucket/raw/gps.csv')\n        return len(df)   # XCom: passe résultat tâche suivante\n\n    @task\n    def transform(n_records: int):\n        # ... transformations\n        return {'records': n_records, 'cleaned': n_records - 100}\n\n    @task\n    def load(stats: dict):\n        # ... write to DB\n        print(f'Loaded {stats[\"cleaned\"]}')\n\n    # Définit dépendances par appel de fonction\n    n = extract()\n    stats = transform(n)\n    load(stats)\n\netl_pipeline()\n\n# 2. Operators classiques (style legacy)\nfrom airflow import DAG\nfrom airflow.operators.bash import BashOperator\nfrom airflow.operators.python import PythonOperator\n\nwith DAG('legacy_style', ...) as dag:\n    extract = BashOperator(\n        task_id='extract',\n        bash_command='python -m my_pipeline.extract'\n    )\n    transform = PythonOperator(\n        task_id='transform',\n        python_callable=my_function\n    )\n    extract >> transform   # extract THEN transform\n\n# 3. Operators essentiels\n# - BashOperator: shell command\n# - PythonOperator: appelle fonction Python\n# - PostgresOperator: SQL on Postgres\n# - SnowflakeOperator: Snowflake\n# - S3CopyObjectOperator: copy S3\n# - HttpOperator: HTTP requests\n# - DockerOperator: run Docker container\n# - KubernetesPodOperator: K8s job\n# - DbtRunOperator: integration dbt\n# - SlackAPIOperator: notifications\n\n# 4. Scheduling (cron expressions)\n# schedule='@daily'           # 0 0 * * *\n# schedule='@hourly'\n# schedule='@weekly'\n# schedule='0 4 * * 1-5'      # 4 AM lun-ven\n# schedule='0 */6 * * *'      # toutes 6h\n# schedule=timedelta(minutes=15)\n# schedule=None                # manual only\n\n# 5. Templating (Jinja dans operators)\n# bash_command='gsutil cp s3://bucket/{{ ds }}/* /tmp/'\n# {{ ds }} = exec date YYYY-MM-DD\n# {{ ds_nodash }} = YYYYMMDD\n# {{ data_interval_start }} = datetime\n# {{ data_interval_end }}\n# {{ params.my_param }}\n\n# 6. XComs (passage de données)\n# Method 1: TaskFlow API (auto)\n# n = extract()  # return value devient XCom\n# transform(n)    # reçoit le XCom\n\n# Method 2: classic\n# def extract(**context):\n#     context['ti'].xcom_push(key='count', value=100)\n# def transform(**context):\n#     n = context['ti'].xcom_pull(task_ids='extract', key='count')\n\n# 7. Lancer manuellement\n# airflow dags trigger etl_gps_pipeline\n# airflow dags backfill etl_gps_pipeline -s 2026-01-01 -e 2026-01-31",
      },
      {
        id: "d77",
        label: "Setup Airflow + premier DAG",
        day: "Mar",
        description: "Mettre Airflow en place localement avec Docker Compose. UI Airflow = visualiser DAGs, runs, logs, retry manuel. Premier DAG end-to-end qui fait quelque chose d'utile.",
        url: "https://airflow.apache.org/docs/apache-airflow/stable/howto/docker-compose/index.html",
        resource: "Airflow - Docker Compose Setup",
        resources: [
          { type: "doc", title: "Airflow Docker Compose", url: "https://airflow.apache.org/docs/apache-airflow/stable/howto/docker-compose/index.html" },
          { type: "tool", title: "Astro CLI (alternative)", url: "https://docs.astronomer.io/astro/cli/overview" },
          { type: "tool", title: "Cosmos (dbt + Airflow)", url: "https://github.com/astronomer/astronomer-cosmos" },
        ],
        exercise: "# Objectif: Airflow opérationnel + DAG concret\n\n# Option A: Astro CLI (recommandé)\n# brew install astro   # ou apt/scoop\n# astro dev init my-airflow-project\n# cd my-airflow-project\n# astro dev start    # démarre tout\n# # UI: localhost:8080 (admin/admin)\n\n# Option B: Docker Compose officiel\n# curl -LfO 'https://airflow.apache.org/docs/apache-airflow/2.8.0/docker-compose.yaml'\n# mkdir -p ./dags ./logs ./plugins ./config\n# echo 'AIRFLOW_UID=50000' > .env\n# docker compose up airflow-init\n# docker compose up\n\n# Structure projet Astro:\n# my-airflow/\n# ├── dags/\n# │   ├── etl_gps.py\n# │   └── dbt_run.py\n# ├── tests/\n# ├── plugins/\n# ├── include/\n# ├── Dockerfile\n# ├── requirements.txt\n# └── airflow_settings.yaml\n\n# Premier DAG: ingest data.gouv quotidien\n# dags/ingest_dvf.py\nfrom airflow.decorators import dag, task\nfrom airflow.providers.postgres.operators.postgres import PostgresOperator\nimport pendulum\n\n@dag(\n    dag_id='ingest_dvf_daily',\n    start_date=pendulum.datetime(2026, 1, 1, tz='UTC'),\n    schedule='0 4 * * *',   # 4 AM tous les jours\n    catchup=False,\n    tags=['ingestion'],\n)\ndef ingest_dvf():\n    \n    @task\n    def download() -> str:\n        import requests\n        from pathlib import Path\n        url = 'https://files.data.gouv.fr/geo-dvf/latest/csv/2024/full.csv.gz'\n        path = '/tmp/dvf.csv.gz'\n        with requests.get(url, stream=True) as r:\n            with open(path, 'wb') as f:\n                for chunk in r.iter_content(8192):\n                    f.write(chunk)\n        return path\n\n    @task\n    def transform(input_path: str) -> str:\n        import polars as pl\n        df = (\n            pl.scan_csv(input_path, separator='|')\n            .filter(pl.col('valeur_fonciere').is_not_null())\n            .filter(pl.col('valeur_fonciere') > 1000)\n            .with_columns(\n                pl.col('date_mutation').str.to_date(),\n                pl.col('valeur_fonciere').cast(pl.Float64)\n            )\n            .collect(streaming=True)\n        )\n        out = '/tmp/dvf_clean.parquet'\n        df.write_parquet(out)\n        return out\n\n    @task\n    def load(parquet_path: str):\n        import polars as pl\n        from sqlalchemy import create_engine\n        df = pl.read_parquet(parquet_path)\n        engine = create_engine(os.environ['DATABASE_URL'])\n        df.write_database('dvf_transactions', engine, if_exists='append')\n\n    create_table = PostgresOperator(\n        task_id='create_table',\n        postgres_conn_id='postgres_default',\n        sql='''\n            CREATE TABLE IF NOT EXISTS dvf_transactions (\n                id BIGSERIAL PRIMARY KEY,\n                date_mutation DATE,\n                valeur_fonciere NUMERIC,\n                ...\n            )\n        ''',\n    )\n\n    raw = download()\n    parquet = transform(raw)\n    create_table >> load(parquet)\n\ningest_dvf()\n\n# Connections (UI ou Variables)\n# Admin → Connections:\n# postgres_default: postgresql://user:pass@host:5432/db\n# slack_default: webhook URL\n# aws_default: AWS keys\n\n# Variables (UI ou Variables → Add)\n# Admin → Variables:\n# api_key: xxx\n# bucket_name: my-bucket\n\n# Usage dans DAG:\nfrom airflow.models import Variable\napi_key = Variable.get('api_key')\n\n# Tests DAG localement\n# astro dev pytest         # tests structure DAGs\n# astro dev parse           # vérifie compile\n# astro dev run dags test ingest_dvf 2026-05-07   # teste 1 run",
      },
      {
        id: "d78",
        label: "XComs, TaskGroups, Sensors",
        day: "Mer",
        description: "XCom (cross-communication) = passer données entre tasks. TaskGroup = regrouper visuellement tasks. Sensor = attendre événement (fichier sur S3, table populée). Patterns essentiels pour pipelines réels.",
        url: "https://airflow.apache.org/docs/apache-airflow/stable/core-concepts/xcoms.html",
        resource: "Airflow - XComs",
        resources: [
          { type: "doc", title: "XComs Documentation", url: "https://airflow.apache.org/docs/apache-airflow/stable/core-concepts/xcoms.html" },
          { type: "doc", title: "TaskGroups", url: "https://airflow.apache.org/docs/apache-airflow/stable/core-concepts/dags.html#taskgroups" },
          { type: "doc", title: "Sensors Guide", url: "https://airflow.apache.org/docs/apache-airflow/stable/core-concepts/sensors.html" },
        ],
        exercise: "# Objectif: patterns DAG avancés\n\n# 1. XComs avec TaskFlow (le plus simple)\n@task\ndef extract() -> dict:\n    return {'count': 1000, 'path': '/tmp/data.csv'}\n\n@task  \ndef transform(input_data: dict):\n    print(f'Got {input_data[\"count\"]} records')\n\nresult = extract()\ntransform(result)   # auto XCom\n\n# 2. XComs custom backend (pour gros objets)\n# Par défaut XComs en DB Airflow (limite: ~1MB)\n# Pour DataFrames: utilise S3/GCS comme backend\n# airflow.cfg:\n# [core]\n# xcom_backend = airflow.providers.amazon.aws.xcom.S3XComBackend\n\n# 3. TaskGroups (organisation visuelle)\nfrom airflow.utils.task_group import TaskGroup\n\nwith TaskGroup('extract_sources') as extract_group:\n    @task\n    def extract_api(): ...\n    @task\n    def extract_db(): ...\n    @task\n    def extract_s3(): ...\n    [extract_api(), extract_db(), extract_s3()]\n\nwith TaskGroup('transform') as tf_group:\n    @task\n    def clean(): ...\n    @task\n    def enrich(): ...\n    clean() >> enrich()\n\nextract_group >> tf_group\n\n# 4. Sensors (attendre événement)\nfrom airflow.providers.amazon.aws.sensors.s3 import S3KeySensor\nfrom airflow.sensors.filesystem import FileSensor\nfrom airflow.providers.postgres.sensors.postgres import SqlSensor\n\nwait_for_file = S3KeySensor(\n    task_id='wait_for_data',\n    bucket_key='s3://bucket/raw/{{ ds }}/data.csv',\n    aws_conn_id='aws_default',\n    poke_interval=60,         # check chaque 60s\n    timeout=60*60*4,           # timeout 4h\n    mode='reschedule',         # libère slot pendant attente\n)\n\nwait_for_table = SqlSensor(\n    task_id='wait_for_data_loaded',\n    conn_id='postgres_default',\n    sql=\"SELECT COUNT(*) FROM stg_trips WHERE date = '{{ ds }}' HAVING COUNT(*) > 0\",\n    poke_interval=300,\n    timeout=3600,\n)\n\nwait_for_file >> process_task >> wait_for_table\n\n# 5. Branching (conditional flow)\nfrom airflow.operators.python import BranchPythonOperator\n\ndef choose_path(**context):\n    n = context['ti'].xcom_pull(task_ids='extract', key='count')\n    if n > 1_000_000:\n        return 'big_data_processing'\n    return 'small_data_processing'\n\nbranch = BranchPythonOperator(\n    task_id='choose',\n    python_callable=choose_path,\n)\n\nextract >> branch >> [big_data_processing, small_data_processing]\n\n# 6. Trigger Rules\n# Default: 'all_success' = parent succeed all\nfrom airflow.utils.trigger_rule import TriggerRule\n\ncleanup = BashOperator(\n    task_id='cleanup',\n    bash_command='rm -rf /tmp/*',\n    trigger_rule=TriggerRule.ALL_DONE,   # toujours run, succès ou fail\n)\n\nnotify_failure = SlackAPIOperator(\n    ...,\n    trigger_rule=TriggerRule.ONE_FAILED,\n)\n\n# 7. Dynamic Task Mapping (Airflow 2.3+)\n@task\ndef get_files() -> list[str]:\n    return ['file1.csv', 'file2.csv', 'file3.csv']\n\n@task\ndef process(file: str):\n    print(f'Processing {file}')\n\n# Process tous les fichiers en parallèle dynamiquement\nprocess.expand(file=get_files())\n\n# 8. SLAs (Service Level Agreements)\nfrom datetime import timedelta\n\n@task(\n    sla=timedelta(hours=2),    # alerte si > 2h\n)\ndef long_task():\n    pass\n\n# 9. Datasets (data-aware scheduling)\nfrom airflow.datasets import Dataset\n\ndataset_trips = Dataset('s3://bucket/trips/')\n\n@dag(\n    schedule=[dataset_trips],   # trigger quand dataset updated\n)\ndef downstream_dag():\n    pass\n\n@task(outlets=[dataset_trips])\ndef produce_trips():\n    # Updates dataset automatiquement\n    pass\n\n# 10. Pools (limit concurrence)\n# UI → Admin → Pools\n# Crée 'api_pool' size=5\n@task(pool='api_pool')\ndef api_call(): ...\n# Max 5 tasks API en parallèle\n# (évite rate limiting)",
      },
      {
        id: "d79",
        label: "Error handling & alerting",
        day: "Jeu",
        description: "Pipelines plantent. Logging, retries, alerts Slack/email = vital pour ne pas découvrir un problème 3 jours après. on_failure_callback + Sentry/Slack = stack monitoring pro.",
        url: "https://airflow.apache.org/docs/apache-airflow/stable/howto/email-config.html",
        resource: "Airflow - Email Configuration",
        resources: [
          { type: "doc", title: "Airflow Callbacks", url: "https://airflow.apache.org/docs/apache-airflow/stable/administration-and-deployment/logging-monitoring/callbacks.html" },
          { type: "tool", title: "Sentry pour Airflow", url: "https://docs.sentry.io/platforms/python/integrations/airflow/" },
          { type: "doc", title: "Slack Webhooks", url: "https://api.slack.com/messaging/webhooks" },
        ],
        exercise: "# Objectif: pipeline observable et resilient\n\n# 1. Retries (built-in)\n@task(\n    retries=3,\n    retry_delay=timedelta(minutes=5),\n    retry_exponential_backoff=True,   # 5min, 10min, 20min\n    max_retry_delay=timedelta(hours=1),\n)\ndef api_call():\n    response = requests.get('https://flaky-api.com')\n    response.raise_for_status()\n\n# 2. on_failure_callback (notification Slack)\nfrom airflow.providers.slack.notifications.slack_webhook import send_slack_webhook_notification\n\ndef on_failure(context):\n    task = context['task_instance']\n    dag_run = context['dag_run']\n    msg = f'''\n        :red_circle: Task FAILED\n        DAG: {task.dag_id}\n        Task: {task.task_id}\n        Run: {dag_run.run_id}\n        Logs: {task.log_url}\n    '''\n    send_slack_webhook_notification(\n        slack_webhook_conn_id='slack_alerts',\n        text=msg,\n    )\n\n# Apply au DAG entier\n@dag(\n    on_failure_callback=on_failure,\n    on_success_callback=on_success,   # optionnel\n    on_retry_callback=on_retry,\n)\ndef my_dag():\n    @task(\n        on_failure_callback=on_failure,   # ou par task\n    )\n    def my_task():\n        pass\n\n# 3. Email config (production)\n# airflow.cfg:\n# [smtp]\n# smtp_host = smtp.gmail.com\n# smtp_starttls = True\n# smtp_user = airflow@example.com\n# smtp_password = xxxx\n# smtp_port = 587\n# smtp_mail_from = airflow@example.com\n\n# Default args dans DAG:\n# default_args = {\n#     'email': ['nassim@example.com'],\n#     'email_on_failure': True,\n#     'email_on_retry': False,\n# }\n\n# 4. Sentry integration\n# pip install sentry-sdk\n# airflow.cfg:\n# [sentry]\n# sentry_dsn = https://xxx@sentry.io/yyy\n# sentry_on = True\n\n# Toute exception non catchée → Sentry\n\n# 5. Custom SLA missed callback\ndef sla_miss_callback(dag, task_list, blocking_task_list, slas, blocking_tis):\n    msg = f'SLA missed for: {[s.task_id for s in slas]}'\n    notify_slack(msg)\n\n@dag(sla_miss_callback=sla_miss_callback)\n\n# 6. Logging structuré\nimport logging\n\n@task\ndef my_task(**context):\n    log = logging.getLogger(__name__)\n    log.info('Starting task', extra={\n        'execution_date': context['ds'],\n        'task_id': context['task'].task_id,\n    })\n    try:\n        result = do_work()\n        log.info('Done', extra={'records': len(result)})\n    except Exception as e:\n        log.exception('Failed!')\n        raise\n\n# 7. Try/Except patterns\n@task\ndef robust_extract():\n    try:\n        data = fetch_api()\n    except RateLimitError:\n        log.warning('Rate limited, sleeping')\n        time.sleep(60)\n        data = fetch_api()\n    except DataNotReadyError:\n        log.info('Data not ready, soft skip')\n        return None\n    except Exception as e:\n        log.exception('Unexpected error')\n        raise   # ré-raise pour Airflow retry\n\n# 8. Dead Letter Queue pattern\n@task\ndef process_records(records: list):\n    failed = []\n    for r in records:\n        try:\n            process(r)\n        except Exception as e:\n            failed.append({'record': r, 'error': str(e)})\n    \n    if failed:\n        # Stocke dans une DLQ pour analyse\n        write_to_s3('dlq/{{ ts }}/failed.json', failed)\n        log.warning(f'{len(failed)} records failed')\n\n# 9. Health check DAG\n@dag(schedule='*/15 * * * *')   # toutes 15 min\ndef health_check():\n    @task\n    def check_db():\n        result = postgres.run_sql('SELECT 1')\n        if not result: raise Exception('DB down')\n    \n    @task\n    def check_freshness():\n        last_run = postgres.run_sql(\n            'SELECT MAX(created_at) FROM stg_trips'\n        )\n        if (datetime.now() - last_run).hours > 2:\n            raise Exception('Stale data')\n\n# 10. Monitoring metrics\n# Airflow expose métriques Prometheus\n# airflow.cfg:\n# [metrics]\n# statsd_on = True\n# statsd_host = localhost\n# statsd_port = 8125\n\n# Metrics importantes:\n# - dag_processing.last_duration\n# - operator_failures\n# - operator_successes\n# - executor.open_slots\n# Dashboards Grafana ready",
      },
      {
        id: "d80",
        label: "DAG complet pipeline GPS",
        day: "Sam",
        description: "Projet portfolio: DAG production-ready avec extract API, transform Polars, load DB, tests qualité, alertes Slack. Démontre maîtrise complète Airflow. Très demandé en alternance/CDI.",
        url: "https://registry.astronomer.io/",
        resource: "Astronomer Registry (templates)",
        resources: [
          { type: "tool", title: "Astronomer Registry", url: "https://registry.astronomer.io/" },
          { type: "github", title: "Awesome Apache Airflow", url: "https://github.com/jghoman/awesome-apache-airflow" },
          { type: "tool", title: "Cosmos (dbt + Airflow)", url: "https://github.com/astronomer/astronomer-cosmos" },
        ],
        exercise: "# Projet: Pipeline GPS production avec Airflow\n\n# Repo: github.com/nassim/airflow-gps-pipeline\n\n# Architecture:\n# 1. Sensor: attendre fichier sur S3\n# 2. Validate: schema + freshness\n# 3. Extract: download depuis S3\n# 4. Transform: Polars (clean + features)\n# 5. Load: PostGIS\n# 6. dbt run: aggregations\n# 7. dbt test: qualité\n# 8. Notify: Slack si OK, alert si fail\n\n# dags/gps_pipeline.py\nfrom airflow.decorators import dag, task, task_group\nfrom airflow.providers.amazon.aws.sensors.s3 import S3KeySensor\nfrom airflow.providers.amazon.aws.operators.s3 import S3CopyObjectOperator\nfrom airflow.providers.slack.notifications.slack import send_slack_notification\nfrom cosmos import DbtTaskGroup, ProjectConfig, ProfileConfig\nimport pendulum\nfrom datetime import timedelta\n\nDEFAULT_ARGS = {\n    'owner': 'data-team',\n    'retries': 3,\n    'retry_delay': timedelta(minutes=5),\n}\n\n@dag(\n    dag_id='gps_daily_pipeline',\n    start_date=pendulum.datetime(2026, 1, 1, tz='UTC'),\n    schedule='0 4 * * *',\n    catchup=False,\n    max_active_runs=1,\n    default_args=DEFAULT_ARGS,\n    tags=['gps','daily','core'],\n    on_failure_callback=send_slack_notification(\n        slack_conn_id='slack_alerts',\n        text=':red_circle: GPS Pipeline FAILED for {{ ds }}',\n    ),\n    on_success_callback=send_slack_notification(\n        slack_conn_id='slack_data',\n        text=':white_check_mark: GPS Pipeline OK for {{ ds }}',\n    ),\n)\ndef gps_pipeline():\n    \n    # 1. Sensor\n    wait_for_data = S3KeySensor(\n        task_id='wait_for_raw_data',\n        bucket_key='s3://renault-raw/year={{ ds[:4] }}/month={{ ds[5:7] }}/day={{ ds[8:10] }}/_SUCCESS',\n        poke_interval=300,\n        timeout=4 * 3600,\n        mode='reschedule',\n        soft_fail=False,\n    )\n    \n    # 2. Validate\n    @task\n    def validate_source(date: str = '{{ ds }}'):\n        from my_pipeline.validators import validate_s3_data\n        result = validate_s3_data(date)\n        if not result.valid:\n            raise ValueError(f'Validation failed: {result.errors}')\n        return result.row_count\n    \n    # 3. Extract + Transform (TaskGroup)\n    @task_group(group_id='extract_transform')\n    def extract_transform_group():\n        @task\n        def download(date: str = '{{ ds }}') -> str:\n            from my_pipeline.extract import download_from_s3\n            return download_from_s3(date)\n        \n        @task\n        def transform(input_path: str) -> str:\n            from my_pipeline.transform import process_gps\n            return process_gps(input_path)\n        \n        path = download()\n        return transform(path)\n    \n    # 4. Load PostGIS\n    @task\n    def load_to_postgis(parquet_path: str, date: str = '{{ ds }}'):\n        from my_pipeline.load import load_postgis\n        # Idempotent: DELETE + INSERT\n        load_postgis(parquet_path, date, mode='overwrite_partition')\n    \n    # 5. dbt (via Cosmos)\n    dbt_group = DbtTaskGroup(\n        group_id='dbt_transform',\n        project_config=ProjectConfig('/usr/local/airflow/dags/dbt/my_project'),\n        profile_config=ProfileConfig(\n            profile_name='my_project',\n            target_name='prod',\n            profiles_yml_filepath='/usr/local/airflow/dags/dbt/profiles.yml',\n        ),\n    )\n    \n    # 6. Quality checks\n    @task\n    def quality_checks(date: str = '{{ ds }}'):\n        from my_pipeline.qa import run_quality_checks\n        result = run_quality_checks(date)\n        if not result.passed:\n            raise AssertionError(f'QA failed: {result.failures}')\n    \n    # 7. Refresh dashboards\n    @task\n    def refresh_metabase():\n        # Trigger Metabase cache refresh\n        import requests\n        requests.post('https://metabase.example.com/api/dataset/cache-refresh',\n            headers={'X-API-Key': '{{ var.value.metabase_key }}'})\n    \n    # Orchestration\n    n_records = validate_source()\n    parquet = extract_transform_group()\n    loaded = load_to_postgis(parquet)\n    \n    wait_for_data >> n_records >> parquet >> loaded >> dbt_group >> quality_checks() >> refresh_metabase()\n\ngps_pipeline()\n\n# Tests DAG (tests/test_dags.py)\nimport pytest\nfrom airflow.models import DagBag\n\n@pytest.fixture\ndef dagbag():\n    return DagBag(dag_folder='dags/', include_examples=False)\n\ndef test_dag_loaded(dagbag):\n    assert 'gps_daily_pipeline' in dagbag.dag_ids\n    assert dagbag.import_errors == {}\n\ndef test_dag_structure(dagbag):\n    dag = dagbag.get_dag('gps_daily_pipeline')\n    assert len(dag.tasks) >= 5\n\n# Critères qualité repo:\n# ✓ Idempotent (re-run safe)\n# ✓ Tests sur tous les DAGs\n# ✓ CI: dbt parse + airflow tests\n# ✓ Docker Compose pour run local\n# ✓ README avec architecture diagram\n# ✓ Variables/Connections documentées\n# ✓ Monitoring: Sentry + Slack\n# ✓ SLAs définis sur tasks critiques",
      },
    ],
  },
];
