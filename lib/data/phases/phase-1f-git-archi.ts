import type { Week } from "../types";

export const PHASE1_GIT_ARCHI: Week[] = [
  {
    id: "w9",
    title: "Git + Testing",
    tasks: [
      {
        id: "d49",
        label: "Git branches & workflow",
        day: "Lun",
        description: "Git c'est OBLIGATOIRE pour tout dev/data. Maîtriser branches/merge/rebase = ne jamais perdre de code et collaborer correctement. GitHub Flow (1 branche main + features) ou GitFlow (avec develop) sont les standards.",
        url: "https://git-scm.com/book/en/v2",
        resource: "Pro Git Book (gratuit)",
        resources: [
          { type: "book", title: "Pro Git Book (free)", url: "https://git-scm.com/book/en/v2" },
          { type: "tool", title: "Learn Git Branching (interactif)", url: "https://learngitbranching.js.org/" },
          { type: "doc", title: "GitHub Flow", url: "https://docs.github.com/en/get-started/quickstart/github-flow" },
          { type: "tool", title: "Oh Shit, Git! (sortir des galères)", url: "https://ohshitgit.com/" },
        ],
        exercise: "# Objectif: workflow Git pro\n\n# 1. Setup initial\ngit config --global user.name 'Nassim'\ngit config --global user.email 'nassim@example.com'\ngit config --global core.editor 'code --wait'\ngit config --global pull.rebase true   # rebase by default\ngit config --global init.defaultBranch main\n\n# 2. Workflow feature branch\ngit checkout main\ngit pull\ngit checkout -b feat/add-gps-pipeline\n# ... edits ...\ngit add src/pipeline.py\ngit commit -m 'feat: add GPS pipeline with logging'\ngit push -u origin feat/add-gps-pipeline\n# Open PR sur GitHub\n\n# 3. Convention commits (Conventional Commits)\n# feat: nouvelle fonctionnalité\n# fix: correction bug\n# docs: documentation\n# refactor: refacto sans changement comportement\n# test: ajout tests\n# chore: maintenance (deps, configs)\n# perf: amélioration performance\n# style: formatting\n\ngit commit -m 'feat(pipeline): add Polars support'\ngit commit -m 'fix(api): handle 429 rate limit'\ngit commit -m 'docs: update README installation'\n\n# 4. Rebase vs Merge\n# Rebase: rejoue tes commits sur la pointe de main (historique linéaire)\ngit checkout feat/x\ngit fetch origin\ngit rebase origin/main\n# Si conflict: résous, puis git rebase --continue\n\n# Merge: crée un merge commit (préserve historique)\ngit merge origin/main\n\n# 5. Interactif rebase (clean tes commits avant PR)\ngit rebase -i HEAD~5\n# squash, reword, edit, drop des commits\n\n# 6. Stash (mettre de côté)\ngit stash push -m 'wip refacto'\ngit checkout other-branch   # libre\ngit checkout original-branch\ngit stash pop\n\n# 7. Logs utiles\ngit log --oneline --graph --all\ngit log --author='Nassim'\ngit log --since='1 week'\ngit blame src/pipeline.py   # qui a écrit chaque ligne\ngit reflog                   # historique de TES actions (sauve souvent)\n\n# 8. Annuler proprement\ngit reset HEAD~1                # défait dernier commit (garde changes)\ngit reset --hard HEAD~1         # défait + supprime changes (DANGER)\ngit revert <commit>             # crée nouveau commit qui annule\ngit checkout -- file.py         # défait modifications non-staged\n\n# 9. Hooks (lint avant commit)\n# .git/hooks/pre-commit:\n#!/bin/sh\nruff check . || exit 1\nmypy src/ || exit 1\n# ou utilise pre-commit framework (recommandé)",
      },
      {
        id: "d50",
        label: "Pull Requests & Code Review",
        day: "Mar",
        description: "PRs sont le coeur de la collaboration. Un bon PR = description claire + diff petit + tests + screenshots. Code review = donner ET recevoir feedback constructif. C'est 80% de la communication async en équipe tech.",
        url: "https://google.github.io/eng-practices/review/",
        resource: "Google - Code Review Guidelines",
        resources: [
          { type: "doc", title: "Google Code Review Guide", url: "https://google.github.io/eng-practices/review/" },
          { type: "article", title: "Conventional Comments", url: "https://conventionalcomments.org/" },
          { type: "doc", title: "GitHub PR Best Practices", url: "https://docs.github.com/en/pull-requests/collaborating-with-pull-requests" },
          { type: "github", title: "PR Template Examples", url: "https://github.com/stevemao/github-issue-templates" },
        ],
        exercise: "# Objectif: PRs propres et reviews efficaces\n\n# 1. PR Template (.github/pull_request_template.md)\n# ## Description\n# Ce PR ajoute [feature]. Solves #123.\n#\n# ## Changes\n# - Ajout module `pipeline/extract.py`\n# - Tests pour scénarios edge cases\n# - Update README\n#\n# ## How to test\n# ```bash\n# pytest tests/test_extract.py\n# python -m pipeline run --date 2026-05-07\n# ```\n#\n# ## Screenshots (si UI)\n# ![Before](url) ![After](url)\n#\n# ## Checklist\n# - [ ] Tests passent\n# - [ ] Doc à jour\n# - [ ] Pas de secrets commit\n\n# 2. PR petit = PR mergé vite\n# Idéal: < 400 lignes, 1 sujet\n# Si > 500 lignes: split en plusieurs PRs\n\n# 3. Donner feedback (Conventional Comments)\n# 'praise: clean implementation, love the lazy approach'\n# 'suggestion: consider adding retry on network errors'\n# 'question: why did you choose Polars over Pandas here?'\n# 'nitpick: typo line 42'\n# 'issue: this fails when input is empty (test it)'\n# 'thought: we might want to extract this into a util'\n\n# 4. Recevoir feedback\n# - Ne pas le prendre personnel\n# - 'Disagree but commit' si manager insiste\n# - Argumenter avec faits/code, pas opinions\n# - Remercier le reviewer\n\n# 5. Auto-review avant push\n# Re-lis ton diff:\ngit diff --staged\ngit diff main..feat/x\n\n# Quesitons à se poser:\n# - Y a-t-il des prints/logs de debug oubliés ?\n# - Variables hard-codées qui devraient être config ?\n# - Tests pour les nouveaux paths ?\n# - Documentation des fonctions publiques ?\n\n# 6. Draft PR (work in progress)\ngh pr create --draft --title 'feat: WIP pipeline'\n# Convertit en ready quand fini\ngh pr ready 123\n\n# 7. CLI GitHub (gh)\ngh pr create --title '...' --body '...' --base main\ngh pr list --author '@me'\ngh pr review 123 --approve --body 'LGTM'\ngh pr merge 123 --squash --delete-branch\n\n# 8. Resolve conflicts (rebase pattern)\ngit checkout feat/x\ngit fetch origin\ngit rebase origin/main\n# Conflicts apparaissent\n# Edit fichiers (cherche <<<<<<<)\ngit add resolved_file.py\ngit rebase --continue\ngit push --force-with-lease   # safer que --force",
      },
      {
        id: "d51",
        label: "pytest + fixtures",
        day: "Mer",
        description: "pytest = framework de test Python standard. Tests unitaires sont ta sécurité contre régressions. Sans tests, refactorer c'est jouer à la roulette russe. Fixtures = setup réutilisable. Couverture > 80% sur code critique.",
        url: "https://docs.pytest.org/",
        resource: "pytest - Documentation",
        resources: [
          { type: "doc", title: "pytest Documentation", url: "https://docs.pytest.org/" },
          { type: "book", title: "Python Testing with pytest (Brian Okken)", url: "https://pythontest.com/pytest-book/" },
          { type: "tool", title: "pytest-cov (coverage)", url: "https://pytest-cov.readthedocs.io/" },
          { type: "tool", title: "Hypothesis (property testing)", url: "https://hypothesis.readthedocs.io/" },
        ],
        exercise: "# Objectif: tests pro pour pipeline data\n\n# Structure recommandée:\n# tests/\n# ├── conftest.py              # fixtures partagées\n# ├── unit/\n# │   ├── test_transforms.py\n# │   └── test_validators.py\n# └── integration/\n#     └── test_pipeline.py\n\n# 1. Test unitaire basique\n# tests/unit/test_haversine.py\nimport pytest\nfrom math import isclose\nfrom src.utils import haversine\n\ndef test_haversine_paris_lyon():\n    dist = haversine(48.8566, 2.3522, 45.7640, 4.8357)\n    assert isclose(dist, 392, abs_tol=5)\n\ndef test_haversine_zero_distance():\n    assert haversine(0, 0, 0, 0) == 0\n\n# 2. Parametrize (1 test, plusieurs cas)\n@pytest.mark.parametrize('lat1,lon1,lat2,lon2,expected', [\n    (48.85, 2.35, 48.85, 2.35, 0),       # même point\n    (0, 0, 0, 180, 20015),                # antipode\n    (48.85, 2.35, 45.76, 4.84, 392),     # Paris-Lyon\n])\ndef test_haversine_cases(lat1, lon1, lat2, lon2, expected):\n    assert isclose(haversine(lat1,lon1,lat2,lon2), expected, abs_tol=10)\n\n# 3. Fixtures (setup réutilisable)\n# conftest.py\nimport pandas as pd\nimport pytest\n\n@pytest.fixture\ndef sample_gps():\n    return pd.DataFrame({\n        'lat':[48.85, 45.76, 43.30],\n        'lon':[2.35, 4.84, 5.40],\n        'speed':[50, 30, 80]\n    })\n\n@pytest.fixture(scope='session')\ndef db_engine():\n    \"\"\"Connection partagée pour toute la session test.\"\"\"\n    from sqlalchemy import create_engine\n    engine = create_engine('sqlite:///:memory:')\n    yield engine\n    engine.dispose()\n\n# Utilisation:\ndef test_filter_speed(sample_gps):\n    fast = sample_gps[sample_gps.speed > 40]\n    assert len(fast) == 2\n\n# 4. Mocking (isoler du monde extérieur)\nfrom unittest.mock import patch, MagicMock\n\n@patch('src.api.requests.get')\ndef test_fetch_weather(mock_get):\n    mock_get.return_value.json.return_value = {'temp': 20}\n    from src.api import fetch_weather\n    assert fetch_weather('Paris')['temp'] == 20\n\n# 5. pytest fixtures avancées\n@pytest.fixture(autouse=True)   # exécuté avant CHAQUE test\ndef cleanup_files():\n    yield\n    # cleanup après test\n    Path('/tmp/test_output').unlink(missing_ok=True)\n\n# 6. Coverage\n# pip install pytest-cov\n# pytest --cov=src --cov-report=html\n# Ouvre htmlcov/index.html\n\n# 7. Marqueurs (run sélectif)\n@pytest.mark.slow\ndef test_huge_dataset(): ...\n\n@pytest.mark.integration\ndef test_db_round_trip(): ...\n\n# pytest -m 'not slow'\n# pytest -m 'integration'\n\n# 8. pytest options utiles\n# pytest -v                    # verbose\n# pytest -x                    # stop au premier fail\n# pytest -k 'haversine'         # match nom test\n# pytest --pdb                 # debugger sur fail\n# pytest --lf                  # last failed only",
      },
      {
        id: "d52",
        label: "Type hints + mypy",
        day: "Jeu",
        description: "Type hints (Python 3.9+) rendent ton code auto-documenté et catch les bugs au design time. mypy vérifie la cohérence. Niveau pro = type hints partout. Avec IDE moderne (PyCharm/VSCode), autocomplétion est x10.",
        url: "https://docs.python.org/3/library/typing.html",
        resource: "Python - typing module",
        resources: [
          { type: "doc", title: "Python typing docs", url: "https://docs.python.org/3/library/typing.html" },
          { type: "doc", title: "mypy Documentation", url: "https://mypy.readthedocs.io/" },
          { type: "article", title: "Real Python - Type Checking", url: "https://realpython.com/python-type-checking/" },
          { type: "tool", title: "Pyright (faster than mypy)", url: "https://github.com/microsoft/pyright" },
        ],
        exercise: "# Objectif: code type-safe\n\nfrom typing import Optional, Union, Literal, TypedDict, Protocol\nfrom collections.abc import Iterator, Callable\n\n# 1. Types basiques\ndef haversine(lat1: float, lon1: float, lat2: float, lon2: float) -> float:\n    return ...\n\n# 2. Collections (Python 3.9+)\ndef process(coords: list[tuple[float, float]]) -> dict[str, float]:\n    return ...\n\n# Avant 3.9:\n# from typing import List, Tuple, Dict\n# def process(coords: List[Tuple[float, float]]) -> Dict[str, float]: ...\n\n# 3. Optional / Union\ndef find_city(code: str) -> Optional[str]:   # ou: str | None (3.10+)\n    return ...\n\ndef parse(value: int | str) -> str:   # Python 3.10+\n    return str(value)\n\n# 4. Literal (valeurs précises)\ndef set_mode(mode: Literal['fast','slow','medium']) -> None:\n    return ...\n\n# 5. TypedDict (dict avec schema)\nclass GPSPoint(TypedDict):\n    lat: float\n    lon: float\n    timestamp: str\n    speed: float | None\n\ndef process_point(p: GPSPoint) -> bool:\n    return p['lat'] != 0\n\n# 6. Generic types\nfrom typing import TypeVar\nT = TypeVar('T')\ndef first_or_none(items: list[T]) -> T | None:\n    return items[0] if items else None\n\n# 7. Protocol (duck typing)\nclass Pipeline(Protocol):\n    def run(self, data: list) -> list: ...\n\ndef execute(p: Pipeline) -> None:\n    p.run([])\n\n# 8. Pandas/NumPy types\nimport pandas as pd\nimport numpy as np\n\ndef stats(df: pd.DataFrame) -> pd.Series:\n    return df.describe()\n\ndef compute(arr: np.ndarray) -> np.ndarray:\n    return arr * 2\n\n# 9. Callable\ndef apply_fn(\n    data: list[float],\n    fn: Callable[[float], float]\n) -> list[float]:\n    return [fn(x) for x in data]\n\n# 10. mypy config\n# pyproject.toml ou mypy.ini:\n# [tool.mypy]\n# strict = true\n# python_version = '3.11'\n# warn_return_any = true\n# disallow_untyped_defs = true\n# disallow_incomplete_defs = true\n\n# Run:\n# mypy src/\n# Cible: 0 errors\n\n# 11. Pydantic (runtime validation)\nfrom pydantic import BaseModel, Field, field_validator\n\nclass Trip(BaseModel):\n    id: int\n    distance_km: float = Field(gt=0, le=10000)\n    city: Literal['Paris','Lyon','Marseille']\n\n    @field_validator('distance_km')\n    @classmethod\n    def validate_dist(cls, v):\n        if v > 1000: raise ValueError('Suspicious')\n        return v\n\ntrip = Trip(id=1, distance_km=50, city='Paris')\n# Lève ValidationError si données invalides",
      },
      {
        id: "d53",
        label: "CI/CD GitHub Actions",
        day: "Ven",
        description: "GitHub Actions = exécuter automatiquement tests/lint/deploy à chaque push. Sans CI, des PRs cassés finissent en main. Intégration en 5min, gratuit pour repos publics. Premier réflexe pro = ajouter CI au repo.",
        url: "https://docs.github.com/en/actions",
        resource: "GitHub Actions Docs",
        resources: [
          { type: "doc", title: "GitHub Actions Docs", url: "https://docs.github.com/en/actions" },
          { type: "doc", title: "Actions for Python", url: "https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python" },
          { type: "github", title: "Awesome Actions", url: "https://github.com/sdras/awesome-actions" },
          { type: "tool", title: "act (run actions locally)", url: "https://github.com/nektos/act" },
        ],
        exercise: "# Objectif: pipeline CI complet\n\n# Fichier: .github/workflows/ci.yml\n\nname: CI\n\non:\n  push:\n    branches: [main]\n  pull_request:\n    branches: [main]\n\njobs:\n  lint:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: '3.11'\n          cache: 'pip'\n      - run: pip install ruff black mypy\n      - run: ruff check .\n      - run: black --check .\n      - run: mypy src/\n\n  test:\n    runs-on: ubuntu-latest\n    strategy:\n      matrix:\n        python: ['3.10', '3.11', '3.12']\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: ${{ matrix.python }}\n          cache: 'pip'\n      - run: pip install -r requirements.txt -r requirements-dev.txt\n      - run: pytest --cov=src --cov-report=xml\n      - uses: codecov/codecov-action@v4\n        with:\n          file: ./coverage.xml\n\n  build-docker:\n    runs-on: ubuntu-latest\n    needs: [lint, test]\n    if: github.ref == 'refs/heads/main'\n    steps:\n      - uses: actions/checkout@v4\n      - uses: docker/setup-buildx-action@v3\n      - uses: docker/login-action@v3\n        with:\n          registry: ghcr.io\n          username: ${{ github.actor }}\n          password: ${{ secrets.GITHUB_TOKEN }}\n      - uses: docker/build-push-action@v5\n        with:\n          push: true\n          tags: ghcr.io/${{ github.repository }}:latest\n          cache-from: type=gha\n          cache-to: type=gha,mode=max\n\n# Patterns avancés:\n\n# 1. Secrets (Settings → Secrets)\n# - DATABASE_URL, AWS_ACCESS_KEY, etc.\n# - Référencé: ${{ secrets.MY_SECRET }}\n\n# 2. Conditional (if)\n# if: github.event_name == 'pull_request'\n# if: contains(github.event.head_commit.message, '[skip ci]')\n\n# 3. Matrix (test multi-versions)\n# strategy:\n#   matrix:\n#     os: [ubuntu-latest, macos-latest]\n#     python: ['3.10', '3.11']\n\n# 4. Artifacts (sauvegarder résultats)\n# - uses: actions/upload-artifact@v4\n#   with:\n#     name: coverage-report\n#     path: htmlcov/\n\n# 5. Caching dépendances\n# uses: actions/cache@v4\n# with:\n#   path: ~/.cache/pip\n#   key: pip-${{ hashFiles('requirements.txt') }}\n\n# 6. Schedule (cron)\n# on:\n#   schedule:\n#     - cron: '0 8 * * 1-5'   # 8h les jours ouvrés\n\n# 7. Pre-commit local + CI\n# .pre-commit-config.yaml:\n# repos:\n#   - repo: https://github.com/astral-sh/ruff-pre-commit\n#     rev: v0.1.6\n#     hooks: [{id: ruff}, {id: ruff-format}]\n#   - repo: https://github.com/pre-commit/mirrors-mypy\n#     rev: v1.7\n#     hooks: [{id: mypy}]\n# Run: pre-commit install\n\n# 8. Test local action\n# brew install act\n# act -j test",
      },
      {
        id: "d54",
        label: "GitHub repos pro",
        day: "Sam",
        description: "Un GitHub propre = passeport pour recruteurs. README pro avec badges, screenshots, instructions. Topics, description, license. 6 repos épinglés > 60 repos abandonnés. Quality > quantity.",
        url: "https://readme.so/",
        resource: "readme.so - README builder",
        resources: [
          { type: "tool", title: "readme.so (builder)", url: "https://readme.so/" },
          { type: "tool", title: "shields.io (badges)", url: "https://shields.io/" },
          { type: "github", title: "Awesome README examples", url: "https://github.com/matiassingers/awesome-readme" },
          { type: "tool", title: "GitHub Profile README", url: "https://github.com/abhisheknaiidu/awesome-github-profile-readme" },
        ],
        exercise: "# Objectif: profil GitHub niveau pro\n\n# 1. README structure pro\n# # Project Name\n# > Tagline en 1 ligne percutante\n#\n# [![CI](badge_url)](ci_url) [![Coverage](badge)] [![License](badge)]\n#\n# ![Demo GIF](url-vers-gif)\n#\n# ## ✨ Features\n# - Feature 1 (1 phrase)\n# - Feature 2\n# - Feature 3\n#\n# ## 🚀 Quick Start\n# ```bash\n# git clone ...\n# cd ...\n# make install\n# make run\n# ```\n#\n# ## 📐 Architecture\n# ![Diagram](diagram.png)\n#\n# ## 🛠 Stack\n# - Python 3.11, Polars, FastAPI\n# - PostgreSQL, Redis\n# - Docker, GitHub Actions\n#\n# ## 📊 Results\n# Performance: traite 10M lignes en 4s\n# (avant: 60s avec Pandas)\n#\n# ## 🤝 Contributing\n# PRs welcome ! Read CONTRIBUTING.md\n#\n# ## 📝 License\n# MIT\n\n# 2. Badges shields.io (mettre 5-7 max)\n# https://img.shields.io/github/actions/workflow/status/USER/REPO/ci.yml\n# https://img.shields.io/codecov/c/github/USER/REPO\n# https://img.shields.io/github/license/USER/REPO\n# https://img.shields.io/python/required-version-toml?...\n# https://img.shields.io/github/stars/USER/REPO\n\n# 3. Screenshots & GIF (très impactant)\n# - Screenshots: PrintScreen + Pixelsnap (Mac) / ShareX (Win)\n# - GIF: LICEcap, Kap, peek\n# - Demo vidéo: Loom (gratuit jusqu'à 5min)\n# - Diagrammes: excalidraw.com (style hand-drawn)\n\n# 4. Topics (tags GitHub)\n# Settings repo → About → Topics\n# Ajoute: python, data-engineering, polars, geospatial, etc.\n# = découvrabilité\n\n# 5. Profil GitHub (le profil README)\n# Crée repo public au nom de ton username (ex: nassim-bzr/nassim-bzr)\n# README s'affiche en haut de profil\n#\n# Sections suggérées:\n# - 👋 About me\n# - 🛠 Stack\n# - 🚀 Featured Projects (3-4 max)\n# - 📈 Stats (github-readme-stats action)\n# - 📫 Contact\n\n# 6. Pin 6 repos importants\n# Profile → 'Customize your pins'\n# Choisis tes meilleurs projets\n# Ajoute description claire à chacun\n\n# 7. Project description (champ About)\n# Settings repo → About\n# Description: 1 phrase percutante\n# URL: lien vers demo deployed\n# Topics: 5-8 tags\n\n# 8. License\n# MIT pour les projets perso (le plus permissif)\n# echo 'MIT License...' > LICENSE\n\n# 9. .gitignore propre\n# Utilise gitignore.io: https://www.toptal.com/developers/gitignore\n# Sélectionne: Python, VS Code, macOS, JetBrains\n\n# 10. Contributions visibles\n# - Ouvre 1-2 PRs sur projets open source par mois\n# - Issues détaillées\n# - Documentation contributions\n# = green squares + crédibilité",
      },
    ],
  },
  {
    id: "w10",
    title: "Architecture + Portfolio",
    tasks: [
      {
        id: "d55",
        label: "Project structure Python",
        day: "Lun",
        description: "Structure d'un projet Python pro : src/, tests/, pyproject.toml. Ce sont les conventions de l'écosystème. Un projet bien structuré = facile à installer/tester/déployer pour quiconque.",
        url: "https://docs.python-guide.org/writing/structure/",
        resource: "The Hitchhiker's Guide to Python",
        resources: [
          { type: "doc", title: "Python Project Structure", url: "https://docs.python-guide.org/writing/structure/" },
          { type: "doc", title: "PEP 518 - pyproject.toml", url: "https://peps.python.org/pep-0518/" },
          { type: "github", title: "Cookiecutter Data Science", url: "https://github.com/drivendata/cookiecutter-data-science" },
          { type: "tool", title: "uv (modern package manager)", url: "https://github.com/astral-sh/uv" },
        ],
        exercise: "# Objectif: structure pro réutilisable\n\nmy-project/\n├── src/\n│   └── my_project/        # le package\n│       ├── __init__.py\n│       ├── extract.py\n│       ├── transform.py\n│       ├── load.py\n│       ├── pipeline.py\n│       └── utils/\n│           ├── __init__.py\n│           └── geo.py\n├── tests/\n│   ├── conftest.py\n│   ├── unit/\n│   └── integration/\n├── notebooks/             # exploration (NE PAS commit en prod)\n├── data/\n│   ├── raw/               # immutable\n│   ├── interim/\n│   └── processed/\n├── docs/\n├── scripts/               # one-off scripts\n├── .github/\n│   └── workflows/\n│       └── ci.yml\n├── pyproject.toml\n├── Makefile\n├── README.md\n├── LICENSE\n├── .gitignore\n├── .pre-commit-config.yaml\n└── .env.example\n\n# pyproject.toml (config moderne)\n# [project]\n# name = 'my-project'\n# version = '0.1.0'\n# description = 'GPS Pipeline'\n# authors = [{name = 'Nassim', email = 'n@ex.com'}]\n# requires-python = '>=3.11'\n# dependencies = [\n#     'polars>=0.20',\n#     'pydantic>=2',\n#     'requests',\n# ]\n#\n# [project.optional-dependencies]\n# dev = ['pytest', 'mypy', 'ruff', 'black']\n#\n# [project.scripts]\n# my-cli = 'my_project.cli:main'\n#\n# [build-system]\n# requires = ['hatchling']\n# build-backend = 'hatchling.build'\n#\n# [tool.ruff]\n# line-length = 100\n# select = ['E', 'F', 'I', 'N', 'W']\n#\n# [tool.mypy]\n# strict = true\n#\n# [tool.pytest.ini_options]\n# testpaths = ['tests']\n# pythonpath = ['src']\n\n# Makefile (commands courantes)\n# .PHONY: install test lint run\n# install:\n# \tuv pip install -e '.[dev]'\n# test:\n# \tpytest --cov=src\n# lint:\n# \truff check src/ tests/ && mypy src/\n# format:\n# \truff format src/ tests/\n# run:\n# \tpython -m my_project.pipeline\n# clean:\n# \trm -rf build/ dist/ *.egg-info __pycache__\n\n# Install editable:\n# uv pip install -e '.[dev]'\n# Tu peux maintenant: import my_project\n\n# Cookiecutter (génère structure)\n# pip install cookiecutter\n# cookiecutter https://github.com/drivendata/cookiecutter-data-science",
      },
      {
        id: "d56",
        label: "Config YAML + .env (secrets)",
        day: "Mar",
        description: "JAMAIS de secrets dans le code (API keys, passwords). .env pour secrets locaux, config.yaml pour params publics. python-dotenv charge .env. En prod : variables d'environnement (Docker, K8s). Sécurité = base.",
        url: "https://github.com/theskumar/python-dotenv",
        resource: "python-dotenv",
        resources: [
          { type: "github", title: "python-dotenv", url: "https://github.com/theskumar/python-dotenv" },
          { type: "doc", title: "Pydantic Settings", url: "https://docs.pydantic.dev/latest/concepts/pydantic_settings/" },
          { type: "doc", title: "12 Factor App - Config", url: "https://12factor.net/config" },
          { type: "tool", title: "doppler (secrets manager)", url: "https://www.doppler.com/" },
        ],
        exercise: "# Objectif: config propre, secrets safe\n\n# 1. Structure (séparation public/secret)\n# config.yaml          ← commit (params publics)\n# .env                 ← NE PAS commit (secrets)\n# .env.example         ← commit (template sans valeurs)\n# .gitignore           ← contient '.env'\n\n# 2. .env\n# DATABASE_URL=postgresql://user:pass@host:5432/db\n# OPENAI_API_KEY=sk-xxx\n# AWS_ACCESS_KEY_ID=AKIA...\n# REDIS_URL=redis://localhost:6379\n\n# 3. .env.example (commité, sans valeurs)\n# DATABASE_URL=\n# OPENAI_API_KEY=\n# AWS_ACCESS_KEY_ID=\n# REDIS_URL=redis://localhost:6379\n\n# 4. config.yaml (params publics)\n# app:\n#   name: gps-pipeline\n#   version: 0.1.0\n# pipeline:\n#   batch_size: 10000\n#   timezone: Europe/Paris\n# regions:\n#   paris:\n#     bbox: [2.0, 48.5, 2.7, 49.0]\n#     epsg: 2154\n#   lyon:\n#     bbox: [4.7, 45.6, 5.0, 45.9]\n#     epsg: 2154\n\n# 5. python-dotenv (charge .env)\nimport os\nfrom dotenv import load_dotenv\n\nload_dotenv()   # charge .env vers os.environ\n\nDATABASE_URL = os.environ['DATABASE_URL']   # raise si absent\nAPI_KEY = os.environ.get('API_KEY', 'default')   # safe\n\n# 6. Pydantic Settings (validation + type-safe)\nfrom pydantic_settings import BaseSettings\nfrom pydantic import Field, SecretStr\n\nclass Settings(BaseSettings):\n    database_url: str\n    openai_api_key: SecretStr   # masqué dans logs/repr\n    redis_url: str = 'redis://localhost:6379'\n    batch_size: int = 1000\n\n    class Config:\n        env_file = '.env'\n        env_file_encoding = 'utf-8'\n\nsettings = Settings()   # auto-load + validate\nprint(settings.database_url)\n# print(settings.openai_api_key)   # → SecretStr('**********')\n# print(settings.openai_api_key.get_secret_value())   # value\n\n# 7. YAML config\nimport yaml\nfrom pathlib import Path\n\nconfig = yaml.safe_load(Path('config.yaml').read_text())\nbbox_paris = config['regions']['paris']['bbox']\n\n# 8. Combiné: Pydantic + YAML\nfrom pydantic import BaseModel\nimport yaml\n\nclass RegionConfig(BaseModel):\n    bbox: list[float]\n    epsg: int\n\nclass AppConfig(BaseModel):\n    name: str\n    regions: dict[str, RegionConfig]\n\nconfig = AppConfig(**yaml.safe_load(open('config.yaml')))\n\n# 9. Best practices\n# ✓ .env dans .gitignore\n# ✓ Documentation: 'cp .env.example .env && fill values'\n# ✓ Pydantic pour validation au boot (échec rapide)\n# ✓ JAMAIS print(settings) en prod (leak secrets)\n# ✓ En CI/prod: variables d'env (Docker secrets, K8s sealed secrets)\n# ✓ Rotation régulière des secrets\n\n# 10. CRYPTAGE en plus (pour secrets très sensibles)\n# - sops + age (Mozilla)\n# - HashiCorp Vault\n# - AWS Secrets Manager\n# - 1Password CLI",
      },
      {
        id: "d57",
        label: "Design patterns data",
        day: "Mer",
        description: "Patterns = solutions éprouvées à problèmes récurrents. Strategy (algos interchangeables), Factory (création objets), Repository (abstraction DB). Connaître = lire/écrire code maintenable.",
        url: "https://refactoring.guru/design-patterns/python",
        resource: "Refactoring Guru - Patterns",
        resources: [
          { type: "doc", title: "Refactoring Guru (Python)", url: "https://refactoring.guru/design-patterns/python" },
          { type: "book", title: "Architecture Patterns with Python (free)", url: "https://www.cosmicpython.com/" },
          { type: "github", title: "python-patterns", url: "https://github.com/faif/python-patterns" },
        ],
        exercise: "# Objectif: patterns appliqués au pipeline\n\n# 1. Strategy Pattern (algos interchangeables)\nfrom abc import ABC, abstractmethod\n\nclass Storage(ABC):\n    @abstractmethod\n    def save(self, data, path: str): ...\n    @abstractmethod\n    def load(self, path: str): ...\n\nclass ParquetStorage(Storage):\n    def save(self, df, path):\n        df.to_parquet(path)\n    def load(self, path):\n        return pd.read_parquet(path)\n\nclass S3Storage(Storage):\n    def save(self, df, path):\n        df.to_parquet(f's3://{path}')\n    def load(self, path):\n        return pd.read_parquet(f's3://{path}')\n\n# Choisir au runtime\nstorage: Storage = ParquetStorage() if local else S3Storage()\nstorage.save(df, 'data.parquet')\n\n# 2. Factory Pattern\nclass StorageFactory:\n    @staticmethod\n    def create(kind: str) -> Storage:\n        registry = {'parquet': ParquetStorage, 's3': S3Storage}\n        return registry[kind]()\n\nstorage = StorageFactory.create('s3')\n\n# 3. Repository Pattern (abstrait DB)\nclass TripRepository:\n    def __init__(self, db_session): self.db = db_session\n    \n    def find_by_id(self, id: int) -> Trip | None:\n        return self.db.query(Trip).filter_by(id=id).first()\n    \n    def find_by_city(self, city: str) -> list[Trip]:\n        return self.db.query(Trip).filter_by(city=city).all()\n    \n    def save(self, trip: Trip):\n        self.db.add(trip); self.db.commit()\n\n# Tests faciles avec Repository en mémoire (pas besoin DB)\nclass InMemoryTripRepo(TripRepository):\n    def __init__(self): self.trips = {}\n    def find_by_id(self, id): return self.trips.get(id)\n\n# 4. Pipeline Pattern\nclass Pipeline:\n    def __init__(self):\n        self.steps = []\n    \n    def step(self, fn):\n        self.steps.append(fn)\n        return self\n    \n    def run(self, data):\n        for s in self.steps:\n            data = s(data)\n        return data\n\np = (Pipeline()\n    .step(extract)\n    .step(clean)\n    .step(transform)\n    .step(load))\nresult = p.run(input_data)\n\n# 5. Observer Pattern (events)\nfrom collections import defaultdict\nclass EventBus:\n    def __init__(self):\n        self.subs = defaultdict(list)\n    \n    def on(self, event: str, fn):\n        self.subs[event].append(fn)\n    \n    def emit(self, event: str, data):\n        for fn in self.subs[event]:\n            fn(data)\n\nbus = EventBus()\nbus.on('trip_completed', lambda t: print(f'Trip {t.id} done'))\nbus.on('trip_completed', send_to_analytics)\nbus.emit('trip_completed', trip)\n\n# 6. Singleton (DB connection)\nclass Database:\n    _instance = None\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n            cls._instance.engine = create_engine(...)\n        return cls._instance\n\n# 7. Dependency Injection (testabilité)\nclass Pipeline:\n    def __init__(self, storage: Storage, repo: TripRepository):\n        self.storage = storage\n        self.repo = repo\n    \n    def run(self):\n        data = self.storage.load('input')\n        # ...\n\n# Tests:\np = Pipeline(MockStorage(), InMemoryTripRepo())",
      },
      {
        id: "d58",
        label: "Modularisation ETL",
        day: "Jeu",
        description: "Décomposer un script monolithique en modules réutilisables. Extract/Transform/Load séparés = chaque module testable indépendamment, swappable. Standard de tout pipeline pro.",
        url: "https://docs.python.org/3/tutorial/modules.html",
        resource: "Python - Modules",
        resources: [
          { type: "doc", title: "Python Modules Guide", url: "https://docs.python.org/3/tutorial/modules.html" },
          { type: "article", title: "Refactoring Python ETL", url: "https://towardsdatascience.com/etl-pipelines-with-python-3a5a9e7c2b54" },
          { type: "book", title: "Clean Code in Python", url: "https://www.packtpub.com/product/clean-code-in-python-second-edition/9781800560215" },
        ],
        exercise: "# Objectif: ETL modulaire et testable\n\n# Avant (monolithique - script.py 500 lignes):\n# Tout est dans 1 fichier, intestable, illisible\n\n# Après - structure modulaire\n# my_pipeline/\n#   ├── __init__.py\n#   ├── extract.py        # I: lire sources\n#   ├── transform.py      # T: nettoyer/calculer\n#   ├── load.py            # L: écrire sortie\n#   ├── orchestrate.py    # combine E+T+L\n#   ├── validators.py     # validation données\n#   └── utils/\n#       ├── geo.py\n#       └── logging.py\n\n# extract.py\nimport polars as pl\nfrom typing import Iterator\nimport logging\n\nlog = logging.getLogger(__name__)\n\ndef from_csv(path: str) -> pl.DataFrame:\n    log.info(f'Loading {path}')\n    return pl.read_csv(path, try_parse_dates=True)\n\ndef from_api(url: str, **params) -> pl.DataFrame:\n    import requests\n    r = requests.get(url, params=params)\n    return pl.from_dicts(r.json()['data'])\n\ndef from_db(query: str, conn) -> pl.DataFrame:\n    return pl.read_database(query, conn)\n\n# transform.py\nimport polars as pl\n\ndef clean_gps(df: pl.DataFrame) -> pl.DataFrame:\n    return df.filter(\n        pl.col('lat').is_between(-90, 90) &\n        pl.col('lon').is_between(-180, 180)\n    )\n\ndef compute_speed(df: pl.DataFrame) -> pl.DataFrame:\n    return df.with_columns(\n        pl.col('distance_m').truediv(pl.col('duration_s'))\n          .mul(3.6).alias('speed_kmh')\n    )\n\ndef enrich_geo(df: pl.DataFrame, zones: pl.DataFrame) -> pl.DataFrame:\n    # Spatial join\n    return df.join(zones, on='zone_id', how='left')\n\n# load.py\ndef to_parquet(df: pl.DataFrame, path: str, partition_by: list = None):\n    if partition_by:\n        df.write_parquet(path, partition_by=partition_by)\n    else:\n        df.write_parquet(path)\n\ndef to_db(df: pl.DataFrame, table: str, engine, mode='append'):\n    df.write_database(table, engine, if_exists=mode)\n\n# orchestrate.py\nfrom . import extract, transform, load\nfrom .validators import validate_schema\nimport logging\n\nlog = logging.getLogger(__name__)\n\ndef run_pipeline(input_path: str, output_path: str):\n    log.info('Pipeline START')\n    \n    # Extract\n    df = extract.from_csv(input_path)\n    \n    # Validate\n    validate_schema(df)\n    \n    # Transform (chained)\n    df = (df\n        .pipe(transform.clean_gps)\n        .pipe(transform.compute_speed)\n    )\n    \n    # Load\n    load.to_parquet(df, output_path, partition_by=['year','month'])\n    \n    log.info(f'Pipeline END: {len(df)} rows written')\n    return df\n\n# CLI: cli.py\nimport click\nfrom .orchestrate import run_pipeline\n\n@click.command()\n@click.option('--input', required=True)\n@click.option('--output', required=True)\ndef main(input, output):\n    run_pipeline(input, output)\n\nif __name__ == '__main__':\n    main()\n\n# Tests faciles maintenant:\n# tests/test_transform.py\ndef test_compute_speed():\n    df = pl.DataFrame({'distance_m':[3600], 'duration_s':[60]})\n    out = transform.compute_speed(df)\n    assert out['speed_kmh'][0] == 60.0\n\n# Avantages:\n# ✓ Chaque module testable isolément\n# ✓ Réutilisable (extract.from_csv ailleurs)\n# ✓ Lisible (1 module = 1 responsabilité)\n# ✓ Type-safe avec interfaces claires",
      },
      {
        id: "d59",
        label: "uv / Poetry / packaging",
        day: "Ven",
        description: "uv (par Astral, ultra-rapide) ou Poetry pour gérer dépendances proprement. pyproject.toml = standard moderne (vs requirements.txt). Lockfile garantit reproductibilité. Crucial pour collaboration et CI/CD.",
        url: "https://github.com/astral-sh/uv",
        resource: "uv - Modern Python Package Manager",
        resources: [
          { type: "github", title: "uv (recommandé 2025)", url: "https://github.com/astral-sh/uv" },
          { type: "doc", title: "Poetry Docs", url: "https://python-poetry.org/docs/" },
          { type: "doc", title: "PyPI - Packaging Guide", url: "https://packaging.python.org/" },
          { type: "doc", title: "PEP 621 - pyproject.toml", url: "https://peps.python.org/pep-0621/" },
        ],
        exercise: "# Objectif: packaging moderne et reproductible\n\n# uv (le plus rapide, recommandé 2025)\n# https://github.com/astral-sh/uv\n# Install: curl -LsSf https://astral.sh/uv/install.sh | sh\n\n# 1. Init nouveau projet\nuv init my-project --package\ncd my-project\n# Crée: pyproject.toml + src/my_project/__init__.py + .python-version\n\n# 2. Add dependencies\nuv add polars 'pydantic>=2' requests\nuv add --dev pytest mypy ruff   # dev only\n\n# Génère uv.lock (lockfile)\n\n# 3. Install\nuv sync   # install dev + prod\nuv sync --no-dev   # prod only\n\n# 4. Run\nuv run python -m my_project\nuv run pytest\nuv run ruff check .\n\n# 5. Update\nuv lock --upgrade            # toutes deps\nuv lock --upgrade-package polars   # 1 dep\n\n# 6. Pyproject.toml complet\n[project]\nname = 'my-project'\nversion = '0.1.0'\ndescription = 'GPS Pipeline'\nreadme = 'README.md'\nrequires-python = '>=3.11'\ndependencies = [\n    'polars>=0.20',\n    'pydantic>=2.5',\n    'requests',\n]\n\n[project.optional-dependencies]\ndev = [\n    'pytest>=7',\n    'pytest-cov',\n    'mypy',\n    'ruff',\n]\n\n[project.scripts]\nmy-cli = 'my_project.cli:main'\n\n[build-system]\nrequires = ['hatchling']\nbuild-backend = 'hatchling.build'\n\n[tool.uv]\ndev-dependencies = ['pytest', 'mypy', 'ruff']\n\n[tool.ruff]\nline-length = 100\n\n[tool.pytest.ini_options]\ntestpaths = ['tests']\npythonpath = ['src']\n\n# 7. Build & publish\nuv build                                # crée dist/\nuv publish --token $PYPI_TOKEN          # publie sur PyPI\n\n# Alternative: Poetry (mature, plus lent)\n# poetry init\n# poetry add polars pydantic\n# poetry add --group dev pytest mypy\n# poetry install\n# poetry run pytest\n# poetry build && poetry publish\n\n# 8. Lock files (REPRODUCIBILITÉ)\n# uv.lock ou poetry.lock\n# Contient hashes exacts → install identique partout\n# Toujours commit le lockfile !\n\n# 9. Versioning sémantique (semver)\n# 1.2.3\n#  │ │ │\n#  │ │ └─ patch (bug fix)\n#  │ └─── minor (feat compatible)\n#  └───── major (breaking change)\n\n# 10. Distribution\n# Local install editable:\n# uv pip install -e .\n# Tu peux modifier le code, l'import reflète\n\n# Publier sur PyPI:\n# uv build\n# uv publish\n\n# Publier sur Test PyPI d'abord:\n# uv publish --publish-url https://test.pypi.org/legacy/\n\n# 11. Bonus: gérer multiple Python\nuv python install 3.11 3.12\nuv python pin 3.11   # pour ce projet\nuv python list",
      },
      {
        id: "d60",
        label: "Article technique + blog",
        day: "Sam",
        description: "Bloguer = consolider tes apprentissages + visibilité recruteurs + réputation. Sujet : ce que tu viens d'apprendre. Fréquence : 1 article/mois suffit. Plateformes : dev.to (gratuit, simple), Medium, ton propre blog.",
        url: "https://dev.to/",
        resource: "Dev.to (gratuit)",
        resources: [
          { type: "tool", title: "Dev.to (publication facile)", url: "https://dev.to/" },
          { type: "tool", title: "Hashnode (gratuit, custom domain)", url: "https://hashnode.com/" },
          { type: "tool", title: "Astro (blog statique)", url: "https://astro.build/" },
          { type: "article", title: "How to Write Tech Articles", url: "https://www.freecodecamp.org/news/how-to-write-a-good-technical-article/" },
        ],
        exercise: "# Objectif: 1er article publié\n\n# 1. Choisir un sujet (récent + concret)\n# Exemples bons sujets:\n# - 'Comment j'ai réduit mon pipeline de 60s à 4s avec Polars'\n# - 'Spatial joins en GeoPandas: 1000x speedup avec sindex'\n# - '5 patterns Pandas à éviter (et leurs alternatives)'\n# - 'Mon premier ETL en production: ce que j'ai appris'\n\n# Critères:\n# ✓ Tu l'as VRAIMENT fait (pas que théorique)\n# ✓ Apporte de la valeur (chiffres, code, before/after)\n# ✓ Pas trop générique ('Intro to Pandas' = saturé)\n# ✓ Spécifique (ton angle perso)\n\n# 2. Structure article (1500 mots max)\n# Intro (3 phrases)\n#   - Problème rencontré\n#   - Pourquoi ça compte\n#   - Ce que tu vas montrer\n# \n# Le contexte\n#   - Le projet, les contraintes\n#   - Les datasets (taille, type)\n# \n# La solution\n#   - Code concret (snippets, pas tout)\n#   - Choix techniques + pourquoi\n#   - Pièges évités\n# \n# Résultats\n#   - Mesures avant/après\n#   - Graphiques (matplotlib screenshot)\n#   - Limitations\n# \n# Conclusion\n#   - 3 takeaways\n#   - Lien vers code GitHub\n#   - Question/CTA\n\n# 3. Markdown structure\n# # Titre percutant\n# > Sous-titre clarificateur\n# ![Cover image](url)\n# \n# ## Le problème\n# ## La solution\n# ```python\n# # code propre, commenté\n# ```\n# ## Résultats\n# | Avant | Après |\n# |-------|-------|\n# | 60s   | 4s    |\n# \n# ## Conclusion\n# ## Resources\n\n# 4. Tags (sur Dev.to)\n# Choisis 4 tags max parmi: #python #data #pandas #polars #tutorial\n# Top tags = visibilité\n\n# 5. SEO basics\n# - Titre clair: '<verbe> <quoi> avec <comment>'\n# - Première phrase = description meta\n# - Headings H2/H3 cohérents\n# - Images avec alt text\n\n# 6. Promotion (1h après publication)\n# - Partage LinkedIn (post: pourquoi tu as écrit + lien)\n# - Twitter/X si pertinent\n# - Slack/Discord communautés data (#python-fr, etc.)\n# - Reddit r/dataengineering (lis les rules avant)\n\n# 7. Setup blog perso (bonus)\n# Astro (statique, fast):\n# npm create astro@latest my-blog -- --template blog\n# cd my-blog && npm run dev\n# Deploy sur Vercel/Netlify (gratuit)\n# Custom domain: nassim-data.dev (~10€/an)\n\n# 8. Métriques de succès\n# - Pas le nombre de vues\n# - Mais: 1 commentaire positif = win\n# - 1 personne qui réutilise ton approche = win\n# - 1 recruteur qui contacte = jackpot\n\n# 9. Régularité > perfection\n# Mieux: 12 articles imparfaits/an\n# Que: 1 article 'parfait' jamais publié\n\n# 10. Ton 1er article (cette semaine)\n# Sujet: 'Mon parcours alternance Data Engineer chez Entropy'\n# OU\n# 'Pandas → Polars: gains réels sur dataset 10M lignes'",
      },
    ],
  },
];
