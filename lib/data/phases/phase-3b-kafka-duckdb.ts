import type { Week } from "../types";

export const PHASE3_KAFKA_DUCKDB: Week[] = [
  {
    id: "w18",
    title: "Kafka + PostGIS",
    tasks: [
      {
        id: "d94",
        label: "Kafka concepts (topics, producers, consumers)",
        day: "Lun",
        description: "Kafka = bus de messages distribué pour streaming. Topics (canaux), Producers (écrivent), Consumers (lisent). Standard de l'event-driven architecture (Uber, LinkedIn, Netflix). Indispensable pour streaming et microservices.",
        url: "https://kafka.apache.org/documentation/",
        resource: "Apache Kafka Documentation",
        resources: [
          { type: "doc", title: "Kafka Documentation", url: "https://kafka.apache.org/documentation/" },
          { type: "course", title: "Confluent Developer (gratuit)", url: "https://developer.confluent.io/courses/" },
          { type: "tool", title: "Redpanda (Kafka-compatible, simpler)", url: "https://redpanda.com/" },
          { type: "video", title: "Kafka Explained (ByteByteGo)", url: "https://youtu.be/PzPXRmVHMxI" },
        ],
        exercise: "# Objectif: comprendre + utiliser Kafka\n\n# 1. Concepts\n# Topic: canal nommé (ex: 'gps_events')\n# Partition: division d'un topic (parallélisme)\n# Offset: position dans une partition\n# Producer: écrit messages\n# Consumer: lit messages\n# Consumer Group: groupe de consumers (load balancing)\n# Broker: noeud Kafka\n\n# 2. Setup local (Docker)\n# docker-compose.yml:\n# services:\n#   kafka:\n#     image: bitnami/kafka:latest\n#     ports: ['9092:9092']\n#     environment:\n#       KAFKA_CFG_PROCESS_ROLES: broker,controller\n#       KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093\n#       KAFKA_CFG_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092\n#       KAFKA_CFG_NODE_ID: 1\n#       KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093\n#   kafka-ui:\n#     image: provectuslabs/kafka-ui\n#     ports: ['8080:8080']\n#     environment:\n#       KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092\n\n# 3. Producer Python\n# pip install confluent-kafka\nfrom confluent_kafka import Producer\nimport json\n\nproducer = Producer({\n    'bootstrap.servers': 'localhost:9092',\n    'client.id': 'gps-producer',\n    'acks': 'all',           # attend tous brokers (durabilité)\n    'compression.type': 'snappy',\n    'linger.ms': 10,          # batching\n})\n\ndef on_delivery(err, msg):\n    if err:\n        print(f'Failed: {err}')\n    else:\n        print(f'Delivered to {msg.topic()}[{msg.partition()}]@{msg.offset()}')\n\n# Send event\nevent = {'vehicle_id':'V-1','lat':48.85,'lon':2.35,'speed':50,'ts':'2026-05-07T10:00:00'}\nproducer.produce(\n    topic='gps_events',\n    key=event['vehicle_id'].encode(),   # même vehicle = même partition\n    value=json.dumps(event).encode(),\n    callback=on_delivery,\n)\nproducer.flush()   # attend tous deliveries\n\n# 4. Consumer Python\nfrom confluent_kafka import Consumer, KafkaError\n\nconsumer = Consumer({\n    'bootstrap.servers': 'localhost:9092',\n    'group.id': 'gps-processor',\n    'auto.offset.reset': 'earliest',   # ou 'latest'\n    'enable.auto.commit': False,        # commit manuel = at-least-once\n})\n\nconsumer.subscribe(['gps_events'])\n\ntry:\n    while True:\n        msg = consumer.poll(timeout=1.0)\n        if msg is None:\n            continue\n        if msg.error():\n            print(f'Error: {msg.error()}')\n            continue\n        \n        # Process\n        event = json.loads(msg.value())\n        print(f'Got: {event}')\n        \n        # Manual commit (after success)\n        consumer.commit(message=msg)\nexcept KeyboardInterrupt:\n    pass\nfinally:\n    consumer.close()\n\n# 5. Patterns clés\n\n# A. Partition key (ordre + co-location)\n# Même clé → même partition → ordre garanti par clé\nproducer.produce('gps_events', key=vehicle_id, value=event)\n# Tous events de V-1 dans l'ordre\n\n# B. Consumer Groups (load balancing)\n# 3 consumers same group + 6 partitions = 2 partitions chacun\n# 6 consumers same group + 6 partitions = 1 partition chacun\n# 7e consumer = idle (pas de partition à lui)\n\n# C. Multiple consumer groups (broadcast)\n# Group A (storage) + Group B (analytics) lisent tous deux\n# Indépendamment, depuis leurs propres offsets\n\n# 6. Exactly-once semantics (Kafka 2.5+)\n# Producer:\nproducer = Producer({\n    'enable.idempotence': True,   # déduplique côté broker\n    'transactional.id': 'my-tx',\n})\nproducer.init_transactions()\nproducer.begin_transaction()\nproducer.produce(...)\nproducer.commit_transaction()\n\n# 7. Schema Registry (Avro / JSON Schema)\n# Confluent Schema Registry valide structure events\nfrom confluent_kafka.schema_registry import SchemaRegistryClient\nfrom confluent_kafka.schema_registry.avro import AvroSerializer\n\nschema_str = '''\n{\n    \"type\": \"record\",\n    \"name\": \"GpsEvent\",\n    \"fields\": [\n        {\"name\": \"vehicle_id\", \"type\": \"string\"},\n        {\"name\": \"lat\", \"type\": \"double\"},\n        {\"name\": \"lon\", \"type\": \"double\"}\n    ]\n}\n'''\n# Serializer valide chaque message vs schema\n\n# 8. Outils écosystème\n# Kafka Connect: connectors prêts (S3, Postgres, Elastic)\n# Kafka Streams: transformation streaming (Java)\n# ksqlDB: SQL sur streams\n# Faust: Python Streams (Robinhood)\n\n# 9. Production: Confluent Cloud / AWS MSK\n# - Self-host: complexe (ZooKeeper/KRaft, brokers HA)\n# - Confluent Cloud: managed (~50€/mois starter)\n# - AWS MSK Serverless: 0.25$/h\n# - Redpanda: drop-in Kafka, plus simple/perf\n\n# 10. Use cases data\n# - Event sourcing (audit trail)\n# - CDC (Change Data Capture from DB)\n# - Streaming analytics (avec Flink, Spark Streaming)\n# - Microservices async communication\n# - Real-time dashboards",
      },
      {
        id: "d95",
        label: "PostGIS spatial SQL",
        day: "Mar",
        description: "PostGIS = extension PostgreSQL pour données géospatiales. ST_Contains, ST_Distance, ST_DWithin = SQL spatial standard. Index GiST = queries 1000x plus rapides. Indispensable pour apps géospatiales.",
        url: "https://postgis.net/documentation/",
        resource: "PostGIS Documentation",
        resources: [
          { type: "doc", title: "PostGIS Manual", url: "https://postgis.net/documentation/" },
          { type: "doc", title: "PostGIS Workshop (gratuit)", url: "https://postgis.net/workshops/postgis-intro/" },
          { type: "tool", title: "QGIS (visualiser PostGIS)", url: "https://qgis.org/" },
          { type: "github", title: "Awesome PostGIS", url: "https://github.com/awesome-postgis/awesome-postgis" },
        ],
        exercise: "-- Objectif: maîtriser PostGIS\n\n-- 1. Setup\nCREATE EXTENSION postgis;\nCREATE EXTENSION postgis_topology;\nSELECT PostGIS_Full_Version();\n\n-- 2. Création table avec géométrie\nCREATE TABLE laveries (\n    id SERIAL PRIMARY KEY,\n    name VARCHAR(100),\n    geom GEOMETRY(Point, 4326),    -- SRID 4326 = WGS84 (lat/lon)\n    capacity INT\n);\n\n-- Insert avec ST_MakePoint\nINSERT INTO laveries (name, geom, capacity) VALUES\n    ('Laverie Bastille', ST_SetSRID(ST_MakePoint(2.369, 48.853), 4326), 12),\n    ('Laverie Pigalle', ST_SetSRID(ST_MakePoint(2.337, 48.881), 4326), 8);\n\n-- 3. INDEX SPATIAL (CRUCIAL pour perf)\nCREATE INDEX idx_laveries_geom ON laveries USING GIST(geom);\nVACUUM ANALYZE laveries;\n\n-- 4. Distance\nSELECT name, ST_Distance(\n    geom::geography,\n    ST_SetSRID(ST_MakePoint(2.35, 48.86), 4326)::geography\n) AS dist_meters\nFROM laveries\nORDER BY dist_meters;\n-- Cast en geography = distance en mètres (sphérique)\n\n-- 5. Find within radius (SUPER courant)\nSELECT name FROM laveries\nWHERE ST_DWithin(\n    geom::geography,\n    ST_SetSRID(ST_MakePoint(2.35, 48.86), 4326)::geography,\n    1000   -- 1km\n);\n-- Plus rapide que ST_Distance < 1000 (utilise index)\n\n-- 6. Spatial join\nCREATE TABLE arrondissements (\n    id INT PRIMARY KEY,\n    name VARCHAR(50),\n    geom GEOMETRY(Polygon, 4326)\n);\nCREATE INDEX idx_arr_geom ON arrondissements USING GIST(geom);\n\n-- Quel arrondissement pour chaque laverie ?\nSELECT l.name, a.name AS arrondissement\nFROM laveries l\nJOIN arrondissements a ON ST_Contains(a.geom, l.geom);\n\n-- 7. Buffer (zone d'influence)\nSELECT name, ST_Buffer(geom::geography, 500)::geometry AS zone\nFROM laveries;\n-- Zone de 500m autour\n\n-- 8. Aggregate spatial\n-- Centroïde toutes laveries\nSELECT ST_Centroid(ST_Collect(geom)) FROM laveries;\n\n-- Convex hull\nSELECT ST_ConvexHull(ST_Collect(geom)) FROM laveries;\n\n-- 9. K-Nearest Neighbors\n-- Plus proches voisins via opérateur <-> (knn)\nSELECT name, geom <-> ST_SetSRID(ST_MakePoint(2.35, 48.86), 4326) AS dist\nFROM laveries\nORDER BY dist\nLIMIT 5;\n-- Utilise index GiST KNN (très rapide)\n\n-- 10. Geocoding (avec extension)\nCREATE EXTENSION postgis_tiger_geocoder;\nSELECT * FROM geocode('1 Place de la Concorde, Paris');\n\n-- Ou via API externe (Nominatim, Mapbox)\n\n-- 11. ST_Transform (changer projection)\nSELECT ST_AsText(\n    ST_Transform(geom, 2154)   -- vers Lambert 93 (mètres)\n) FROM laveries;\n\n-- 12. Import GeoJSON\nCREATE TABLE quartiers (id SERIAL, props JSONB, geom GEOMETRY);\n\n-- Via ogr2ogr CLI:\n-- ogr2ogr -f PostgreSQL PG:\"...\" data.geojson -nln quartiers\n\n-- Ou via psycopg + GeoPandas:\n-- import geopandas as gpd\n-- gdf = gpd.read_file('data.geojson')\n-- gdf.to_postgis('quartiers', engine, if_exists='replace')\n\n-- 13. Tile generation (cartes web)\n-- pg_tileserv génère MVT (Mapbox Vector Tiles) directement de PostGIS\n-- = très rapide pour cartes interactives Leaflet/Mapbox\n\n-- 14. Performance tips\n-- - Toujours indexer geom\n-- - Utiliser SRID consistent (4326 ou projeté local)\n-- - geography vs geometry: geography pour distances exactes\n-- - Pré-calculer centroids/buffers en colonnes\n-- - PARTITION BY geom_type ou region pour très gros\n\n-- 15. Use cases courants\n-- - Find nearest (recherche restaurant proche)\n-- - Catchment area (zone d'attraction)\n-- - Spatial aggregations (par quartier)\n-- - Routing (avec pgRouting extension)\n-- - Mobility analysis (trajets, OD matrix)",
      },
      {
        id: "d96",
        label: "Star Schema + SCD avancé",
        day: "Mer",
        description: "Star Schema en pratique: fact tables + dimensions + SCD Type 2 (slowly changing). Ralph Kimball est la bible du modeling DW depuis 1996. Mémoriser les patterns = designer warehouses qui scalent.",
        url: "https://www.kimballgroup.com/data-warehouse-business-intelligence-resources/kimball-techniques/dimensional-modeling-techniques/",
        resource: "Kimball Group Techniques",
        resources: [
          { type: "doc", title: "Kimball Group Techniques", url: "https://www.kimballgroup.com/data-warehouse-business-intelligence-resources/kimball-techniques/dimensional-modeling-techniques/" },
          { type: "book", title: "Data Warehouse Toolkit (Kimball)", url: "https://www.kimballgroup.com/data-warehouse-business-intelligence-resources/books/data-warehouse-dw-toolkit/" },
          { type: "doc", title: "dbt SCD snapshots", url: "https://docs.getdbt.com/docs/build/snapshots" },
        ],
        exercise: "-- Objectif: warehouse pro avec SCD Type 2\n\n-- 1. Dimension SCD Type 2 (historique préservé)\nCREATE TABLE dim_vehicle (\n    vehicle_sk BIGSERIAL PRIMARY KEY,        -- surrogate key\n    vehicle_id VARCHAR(50) NOT NULL,         -- business key\n    brand VARCHAR(50),\n    model VARCHAR(50),\n    fleet VARCHAR(50),\n    -- SCD Type 2 fields\n    valid_from DATE NOT NULL,\n    valid_to DATE,\n    is_current BOOLEAN DEFAULT TRUE,\n    -- Audit\n    created_at TIMESTAMP DEFAULT NOW()\n);\n\n-- Index pour queries courantes\nCREATE INDEX idx_dim_vehicle_current ON dim_vehicle(vehicle_id) WHERE is_current;\nCREATE INDEX idx_dim_vehicle_business ON dim_vehicle(vehicle_id, valid_from);\n\n-- Initial load\nINSERT INTO dim_vehicle (vehicle_id, brand, model, fleet, valid_from, valid_to, is_current)\nVALUES\n('V-001', 'Renault', 'Zoe', 'taxi-blue', '2020-01-01', NULL, TRUE),\n('V-002', 'Tesla', 'Model 3', 'taxi-red', '2021-06-15', NULL, TRUE);\n\n-- 2. SCD Type 2 update (changement fleet)\n-- 'V-001' passe de 'taxi-blue' à 'taxi-green' le 2026-05-07\n\nWITH new_data AS (\n    SELECT 'V-001' AS vehicle_id, 'Renault' AS brand, 'Zoe' AS model, 'taxi-green' AS fleet\n)\n-- Step 1: ferme l'ancien record\nUPDATE dim_vehicle\nSET valid_to = '2026-05-06', is_current = FALSE\nWHERE vehicle_id = 'V-001' AND is_current = TRUE;\n\n-- Step 2: insert nouveau record\nINSERT INTO dim_vehicle (vehicle_id, brand, model, fleet, valid_from, is_current)\nSELECT 'V-001', 'Renault', 'Zoe', 'taxi-green', '2026-05-07', TRUE;\n\n-- 3. Query 'point in time' (valeur historique exacte)\nSELECT v.fleet, COUNT(*) FROM fact_trips f\nJOIN dim_vehicle v ON v.vehicle_id = f.vehicle_id\n    AND f.trip_date BETWEEN v.valid_from AND COALESCE(v.valid_to, '9999-12-31')\nWHERE f.trip_date = '2025-12-15'\nGROUP BY v.fleet;\n-- Donne le fleet AU MOMENT du trip, pas le fleet actuel\n\n-- 4. Avec dbt snapshot (auto SCD2)\n-- snapshots/vehicles_snapshot.sql:\n-- {% snapshot dim_vehicle_snapshot %}\n-- {{ config(\n--     target_schema='snapshots',\n--     unique_key='vehicle_id',\n--     strategy='check',\n--     check_cols=['brand','model','fleet'],\n--     invalidate_hard_deletes=True\n-- ) }}\n-- SELECT * FROM {{ source('raw','vehicles') }}\n-- {% endsnapshot %}\n\n-- dbt snapshot   ← run quotidien, gère SCD2 auto\n\n-- 5. Fact table (events business)\nCREATE TABLE fact_trips (\n    trip_sk BIGSERIAL PRIMARY KEY,\n    -- Foreign keys (dimension surrogate keys)\n    vehicle_sk INT REFERENCES dim_vehicle(vehicle_sk),\n    driver_sk INT REFERENCES dim_driver(driver_sk),\n    date_sk INT REFERENCES dim_date(date_sk),\n    origin_sk INT REFERENCES dim_location(location_sk),\n    dest_sk INT REFERENCES dim_location(location_sk),\n    -- Métriques (faits)\n    distance_km NUMERIC(10,2),\n    duration_seconds INT,\n    fuel_liters NUMERIC(8,2),\n    revenue_eur NUMERIC(10,2),\n    -- Degenerate dim (IDs sources, no dim)\n    trip_id_source VARCHAR(50)\n);\n\n-- 6. Materialized View pour KPIs\nCREATE MATERIALIZED VIEW mv_monthly_kpis AS\nSELECT\n    d.year, d.month, v.fleet,\n    COUNT(*) AS n_trips,\n    SUM(f.distance_km) AS total_km,\n    SUM(f.revenue_eur) AS revenue,\n    AVG(f.duration_seconds / 60.0) AS avg_duration_min\nFROM fact_trips f\nJOIN dim_date d ON f.date_sk = d.date_sk\nJOIN dim_vehicle v ON f.vehicle_sk = v.vehicle_sk\n    AND v.is_current = TRUE   -- ou point-in-time (selon besoin)\nGROUP BY d.year, d.month, v.fleet;\n\nCREATE UNIQUE INDEX ON mv_monthly_kpis(year, month, fleet);\nREFRESH MATERIALIZED VIEW CONCURRENTLY mv_monthly_kpis;\n\n-- 7. Bridge tables (relations many-to-many)\n-- 1 trip = N drivers (binôme conduite)\nCREATE TABLE bridge_trip_drivers (\n    trip_sk BIGINT,\n    driver_sk INT,\n    role VARCHAR(20),   -- 'main', 'secondary'\n    PRIMARY KEY (trip_sk, driver_sk)\n);\n\n-- 8. Patterns d'analyse\n\n-- A. KPI mensuel par fleet (utilise SCD2 historique)\nSELECT d.year, d.month, v.fleet,\n    COUNT(DISTINCT f.trip_sk) AS n_trips\nFROM fact_trips f\nJOIN dim_date d ON f.date_sk = d.date_sk\nJOIN dim_vehicle v ON f.vehicle_sk = v.vehicle_sk\nGROUP BY 1, 2, 3;\n\n-- B. Cohort analysis (rétention drivers)\nWITH first_trip AS (\n    SELECT driver_sk, MIN(d.date_actual) AS cohort_date\n    FROM fact_trips f JOIN dim_date d ON f.date_sk = d.date_sk\n    GROUP BY driver_sk\n)\nSELECT\n    DATE_TRUNC('month', ft.cohort_date) AS cohort,\n    DATE_TRUNC('month', d.date_actual) AS active_month,\n    COUNT(DISTINCT f.driver_sk) AS active_drivers\nFROM first_trip ft\nJOIN fact_trips f ON ft.driver_sk = f.driver_sk\nJOIN dim_date d ON f.date_sk = d.date_sk\nGROUP BY 1, 2;\n\n-- 9. Conformed dimensions (réutilisables)\n-- dim_date utilisée par fact_trips, fact_revenue, fact_inventory\n-- = analyse cross-fact possible\n\n-- 10. Anti-patterns à éviter\n-- ❌ Nested fact tables (utilise bridge)\n-- ❌ NULL dans foreign keys (utilise dim 'unknown')\n-- ❌ Business logic dans fact (mettre en dim ou view)\n-- ❌ Skipping date dim (perdre cohort analysis)",
      },
      {
        id: "d97",
        label: "Data warehouse complet",
        day: "Sam",
        description: "Projet portfolio MAJEUR : data warehouse complet avec PostGIS + dbt + SCD2 + visualisation. Démontre maîtrise senior. Ce projet seul peut décrocher des entretiens.",
        url: "https://github.com/DataTalksClub/data-engineering-zoomcamp",
        resource: "DataTalks DE Zoomcamp",
        resources: [
          { type: "course", title: "DE Zoomcamp (full pipeline)", url: "https://github.com/DataTalksClub/data-engineering-zoomcamp" },
          { type: "github", title: "Awesome dbt", url: "https://github.com/Hiflylabs/awesome-dbt" },
          { type: "tool", title: "Metabase (BI gratuit)", url: "https://www.metabase.com/" },
        ],
        exercise: "# Projet: Data Warehouse Mobilité (PROJET CV)\n\n# Repo: github.com/nassim/mobility-data-warehouse\n\n# Stack:\n# - PostgreSQL + PostGIS (warehouse)\n# - dbt (transforms + tests + docs)\n# - Airflow / Prefect (orchestration)\n# - Metabase (BI)\n# - Streamlit (dashboard exec)\n# - Docker Compose (local dev)\n# - GitHub Actions (CI)\n\n# Architecture:\n# Sources: Vélib API + DVF + Météo + INSEE + OSM\n#         ↓\n# Staging (raw + minimal cleaning)\n#         ↓\n# Intermediate (joins, enrichments)\n#         ↓\n# Marts:\n#   - Star Schema (fact_trips, dim_*)\n#   - SCD Type 2 (dim_stations, dim_vehicles)\n#   - Aggregates (kpi_daily, kpi_monthly)\n\n# Structure:\n# warehouse/\n# ├── docker-compose.yml\n# ├── Makefile\n# ├── ingestion/\n# │   ├── velib_loader.py\n# │   ├── weather_loader.py\n# │   └── osm_loader.py\n# ├── dbt_project/\n# │   ├── models/\n# │   │   ├── staging/\n# │   │   │   ├── stg_velib__trips.sql\n# │   │   │   └── stg_weather__hourly.sql\n# │   │   ├── intermediate/\n# │   │   │   └── int_trips_enriched.sql\n# │   │   └── marts/\n# │   │       ├── core/\n# │   │       │   ├── fct_trips.sql\n# │   │       │   ├── dim_stations.sql\n# │   │       │   └── dim_date.sql\n# │   │       └── analytics/\n# │   │           └── kpi_monthly.sql\n# │   ├── snapshots/\n# │   │   └── stations_snapshot.sql\n# │   ├── tests/\n# │   ├── macros/\n# │   └── dbt_project.yml\n# ├── airflow/\n# │   └── dags/\n# │       └── daily_pipeline.py\n# ├── dashboard/\n# │   └── streamlit_app.py\n# ├── docs/\n# │   ├── architecture.md\n# │   └── erd.png   # entity relationship diagram\n# └── README.md\n\n# Métriques business à calculer:\n# - Top 10 stations par mois\n# - Pattern weekend vs semaine\n# - Impact météo sur trips\n# - Cohort utilisateurs (rétention)\n# - Heatmap géographique densité\n# - Optimisation rotations vélos (capacity ÷ usage)\n# - Predict next-hour demand (prep ML)\n\n# Critères qualité:\n# ✓ Star schema rigoureux\n# ✓ SCD Type 2 sur stations (stations bougent dans le temps)\n# ✓ 50+ tests dbt (not_null, unique, ranges, custom)\n# ✓ 100% colonnes documentées (dbt docs)\n# ✓ Lineage diagram généré\n# ✓ CI: dbt build sur PR\n# ✓ Idempotent (rerun safe)\n# ✓ Performant (indexes, materialized views)\n# ✓ Monitoring (Elementary / custom)\n# ✓ README clair avec setup en 30s\n\n# Stretch goals (différenciants):\n# - Lineage avec OpenLineage\n# - dbt-osmosis (auto-doc)\n# - Federated query (PostGIS + S3 Parquet via FDW)\n# - dbt-expectations pour data quality\n# - Multi-env (dev/staging/prod profiles)\n# - Slack alerts sur fail dbt\n# - GitHub Pages avec dbt docs deployed\n\n# Article LinkedIn:\n# 'J'ai construit un Data Warehouse Mobilité de A à Z'\n# - Architecture (Star Schema)\n# - Stack moderne (Postgres + dbt + Airflow)\n# - Insights business (3 actionnables)\n# - URL démo + GitHub + dbt docs\n\n# C'est LE projet qui te démarque vs CVs classiques.\n# Investissement: 40-60h sur 3 semaines\n# ROI: high-quality entretiens senior",
      },
    ],
  },
  {
    id: "w_duckdb",
    title: "DuckDB & Analytics moderne",
    tasks: [
      {
        id: "d149",
        label: "DuckDB basics",
        day: "Lun",
        description: "DuckDB = SQLite mais pour analytics. Embedded, ultra-rapide (Vectorized engine), zéro setup. Lit Parquet/CSV/JSON nativement. Le 'Polars du SQL'. Game changer pour analytics local et embedded.",
        url: "https://duckdb.org/docs/",
        resource: "DuckDB Documentation",
        resources: [
          { type: "doc", title: "DuckDB Documentation", url: "https://duckdb.org/docs/" },
          { type: "doc", title: "DuckDB Python API", url: "https://duckdb.org/docs/api/python/overview" },
          { type: "article", title: "DuckDB vs Pandas vs Polars", url: "https://duckdb.org/2021/05/14/sql-on-pandas.html" },
          { type: "tool", title: "MotherDuck (cloud DuckDB)", url: "https://motherduck.com/" },
        ],
        exercise: "# Objectif: analytics 10-100x faster que Pandas\n\n# Install: pip install duckdb\nimport duckdb\n\n# 1. SQL direct sur Parquet (zero ETL)\nresult = duckdb.sql('''\n    SELECT city, COUNT(*) AS n, AVG(speed) AS avg_speed\n    FROM 's3://bucket/trips/*.parquet'\n    WHERE date >= '2026-01-01'\n    GROUP BY city\n    ORDER BY n DESC\n''').df()   # → Pandas DataFrame\n\n# 2. Multi-format avec wildcards\nduckdb.sql('SELECT * FROM \"data/*.csv\"').df()\nduckdb.sql('SELECT * FROM \"data/**/*.parquet\"').df()   # recursive glob\n\n# 3. Hive partition support\nduckdb.sql('''\n    SELECT * FROM read_parquet(\n        \"data/year=*/month=*/data.parquet\",\n        hive_partitioning = true\n    )\n    WHERE year = 2026 AND month = 5\n''').df()\n# Lit QUE les partitions concernées (predicate pushdown)\n\n# 4. Persistent DB\ncon = duckdb.connect('analytics.db')\ncon.sql('CREATE TABLE trips AS SELECT * FROM \"trips/*.parquet\"')\ncon.sql('SELECT COUNT(*) FROM trips').show()\ncon.close()\n\n# 5. Pandas/Polars integration (zero-copy via Arrow)\nimport pandas as pd\nimport polars as pl\n\npd_df = pd.DataFrame({'a': [1,2,3]})\nresult = duckdb.sql('SELECT a * 2 FROM pd_df').df()\n\npl_df = pl.DataFrame({'a': [1,2,3]})\nresult = duckdb.sql('SELECT a * 2 FROM pl_df').pl()\n\n# 6. Window functions, CTEs, JSON, lateral - tout SQL standard\nduckdb.sql('''\n    WITH stats AS (\n        SELECT vehicle_id, AVG(speed) AS avg_speed\n        FROM trips GROUP BY vehicle_id\n    )\n    SELECT t.*, s.avg_speed,\n        t.speed - s.avg_speed AS speed_diff,\n        ROW_NUMBER() OVER (PARTITION BY t.vehicle_id ORDER BY t.ts) AS trip_num\n    FROM trips t\n    JOIN stats s USING (vehicle_id)\n''').df()\n\n# 7. Read JSON natif\nduckdb.sql('''\n    SELECT * FROM read_json('events.json',\n        format='newline_delimited',\n        records=true\n    )\n''')\n\n# 8. UDF Python\n@duckdb.create_function('haversine', haversine_fn,\n    [duckdb.typing.DOUBLE]*4, duckdb.typing.DOUBLE\n)\nduckdb.sql('SELECT haversine(lat1, lon1, lat2, lon2) FROM trips')\n\n# 9. Spatial extension\nduckdb.sql('INSTALL spatial; LOAD spatial')\nduckdb.sql('''\n    SELECT name, ST_Distance(\n        ST_Point(lon, lat),\n        ST_Point(2.35, 48.86)\n    ) AS dist\n    FROM laveries\n''')\n\n# 10. Performance: 10M rows agg\nimport time\nimport pandas as pd\n\nN = 10_000_000\ndf = pd.DataFrame({\n    'cat': ['A','B','C']*int(N/3),\n    'val': range(N)\n})\n\n# Pandas:\nt = time.time()\ndf.groupby('cat')['val'].agg(['mean','sum','count'])\nprint(f'Pandas: {time.time()-t:.3f}s')\n\n# DuckDB (sur le DataFrame Pandas):\nt = time.time()\nduckdb.sql('SELECT cat, AVG(val), SUM(val), COUNT(*) FROM df GROUP BY cat').df()\nprint(f'DuckDB: {time.time()-t:.3f}s')\n# DuckDB ~10x plus rapide\n\n# 11. Use cases\n# - Analytics local sans serveur\n# - Notebooks data science (remplace SQLAlchemy + Pandas)\n# - ETL léger (S3 → transform → S3)\n# - Embedded analytics (dans app Python/Rust)\n# - dbt-duckdb (dbt sans cloud DW)\n\n# 12. MotherDuck: DuckDB cloud collaboratif\n# duckdb.connect('md:my_db?motherduck_token=xxx')\n# Permet sharing + scaling cloud",
      },
      {
        id: "d150",
        label: "DuckDB vs Pandas/Polars/Spark",
        day: "Mar",
        description: "Quand utiliser DuckDB vs alternatives. DuckDB excelle pour SQL analytics local. Polars excelle pour DataFrame ergonomic. Spark seul scale au-delà single-node. Comprendre les forces = bon choix.",
        url: "https://duckdb.org/docs/guides/performance/overview",
        resource: "DuckDB - Performance Guide",
        resources: [
          { type: "doc", title: "DuckDB Performance", url: "https://duckdb.org/docs/guides/performance/overview" },
          { type: "article", title: "DuckDB vs Pandas Benchmarks", url: "https://h2oai.github.io/db-benchmark/" },
          { type: "article", title: "Why DuckDB?", url: "https://duckdb.org/why_duckdb.html" },
        ],
        exercise: "# Objectif: choisir le bon outil\n\nimport time\nimport pandas as pd\nimport polars as pl\nimport duckdb\nimport numpy as np\n\nN = 50_000_000\ndata = {\n    'id': np.random.randint(0, 1000, N),\n    'cat': np.random.choice(['A','B','C','D'], N),\n    'val': np.random.randn(N)\n}\n\n# Pandas\nt = time.time()\npd.DataFrame(data).groupby('cat')['val'].agg(['mean','sum','count'])\nprint(f'Pandas: {time.time()-t:.2f}s')\n\n# Polars\nt = time.time()\npl.DataFrame(data).group_by('cat').agg([\n    pl.col('val').mean(), pl.col('val').sum(), pl.len()\n])\nprint(f'Polars: {time.time()-t:.2f}s')\n\n# DuckDB\nt = time.time()\nduckdb.sql('''\n    SELECT cat, AVG(val), SUM(val), COUNT(*)\n    FROM (SELECT * FROM data) GROUP BY cat\n''').df()\nprint(f'DuckDB: {time.time()-t:.2f}s')\n\n# Décision matrix:\n# \n# Use case                   | Best tool\n# --------------------------|---------------\n# Pandas existing code       | Polars (drop-in)\n# SQL-first analytics       | DuckDB\n# Notebook EDA               | Pandas (ecosystem)\n# Big data > 100GB           | Spark / Polars streaming\n# Embedded in Python app    | DuckDB / Polars\n# dbt local dev              | DuckDB (dbt-duckdb)\n# Lakehouse on S3            | DuckDB ou Polars\n# Streaming                  | Spark Streaming / Flink\n# ML training (Pandas-like) | Polars ou Pandas\n\n# Patterns hybrides:\n# 1. ETL: Polars/DuckDB (selon préférence)\n# 2. ML: Pandas (sklearn ecosystem)\n# 3. Analytics: DuckDB (SQL puissant)\n# 4. Big data: Spark\n\n# DuckDB superpowers:\n# - Joins gros volumes en mémoire\n# - Scan Parquet S3 sans copy\n# - Window functions avancées\n# - SQL extensions (HTTPFS, spatial, JSON)",
      },
      {
        id: "d151",
        label: "DuckDB sur Parquet S3",
        day: "Mer",
        description: "DuckDB lit S3 Parquet directement avec httpfs extension = lakehouse engine local. Coût quasi-nul vs Athena. Idéal pour ad-hoc analytics et POC sans setup AWS Athena/Glue.",
        url: "https://duckdb.org/docs/extensions/httpfs",
        resource: "DuckDB - HTTPFS Extension",
        resources: [
          { type: "doc", title: "DuckDB HTTPFS (S3, GCS)", url: "https://duckdb.org/docs/extensions/httpfs" },
          { type: "doc", title: "DuckDB Parquet support", url: "https://duckdb.org/docs/data/parquet/overview" },
          { type: "article", title: "DuckDB on S3 Tutorial", url: "https://duckdb.org/2022/08/03/duck-data-frames-extended.html" },
        ],
        exercise: "# Objectif: lakehouse engine sans Athena/Glue\n\nimport duckdb\n\n# 1. Setup S3\nduckdb.sql('INSTALL httpfs')\nduckdb.sql('LOAD httpfs')\n\nduckdb.sql('''\n    SET s3_region = 'eu-west-3';\n    SET s3_access_key_id = '${AWS_ACCESS_KEY}';\n    SET s3_secret_access_key = '${AWS_SECRET_KEY}';\n''')\n\n# 2. Query directement S3\nresult = duckdb.sql('''\n    SELECT city, COUNT(*) AS n, AVG(speed_kmh) AS speed\n    FROM 's3://my-bucket/trips/year=2026/**/*.parquet'\n    WHERE city IS NOT NULL\n    GROUP BY city\n    ORDER BY n DESC\n    LIMIT 20\n''').df()\n\n# Predicate pushdown auto:\n# - DuckDB lit que les Parquet partitions concernées\n# - Filter dans WHERE applied avant network\n# = bcp moins de bytes téléchargés\n\n# 3. Hive partitioning\nduckdb.sql('''\n    SELECT * FROM read_parquet(\n        's3://bucket/trips/year=*/month=*/data.parquet',\n        hive_partitioning = true\n    )\n    WHERE year = 2026 AND month BETWEEN 1 AND 5\n''')\n\n# 4. CREATE EXTERNAL VIEW (DuckDB 1.0+)\nduckdb.sql('''\n    CREATE OR REPLACE VIEW trips AS\n    SELECT * FROM read_parquet('s3://bucket/trips/**/*.parquet',\n        hive_partitioning = true)\n''')\n\n# Maintenant 'trips' est queryable comme une table\nduckdb.sql('SELECT COUNT(*) FROM trips').show()\n\n# 5. Joins multi-source\nduckdb.sql('''\n    SELECT t.city, w.temp_c, COUNT(*) AS n_trips\n    FROM 's3://bucket/trips/2026-05/*.parquet' t\n    JOIN 's3://bucket/weather/2026-05/*.parquet' w\n        ON DATE(t.started_at) = w.date AND t.city = w.city\n    GROUP BY t.city, w.temp_c\n''')\n\n# 6. Write to S3 directly\nduckdb.sql('''\n    COPY (\n        SELECT date, city, COUNT(*) AS n_trips\n        FROM 's3://bucket/raw/*.parquet'\n        GROUP BY date, city\n    ) TO 's3://bucket/aggregated/daily.parquet'\n    (FORMAT 'parquet', COMPRESSION 'snappy')\n''')\n\n# 7. Scan multiple regions\nfiles = [f's3://bucket/region={r}/data.parquet' for r in ['eu','us','ap']]\nduckdb.sql(f'''\n    SELECT region, COUNT(*) FROM read_parquet({files})\n    GROUP BY region\n''')\n\n# 8. Coûts analyse\n# Athena: 5$/TB scanned\n# DuckDB: GRATUIT (juste S3 GET requests = 0.0004$/1000)\n# Pour 1TB scan = ~5$ Athena vs ~0.04$ DuckDB (~125x moins)\n# (Mais Athena a SLA, scaling, etc.)\n\n# 9. Pattern: dbt-duckdb on S3\n# dbt + DuckDB pour transforms locales\n# Output Parquet sur S3\n# = data warehouse à 0$/mois\n\n# Profiles.yml:\n# my_project:\n#   target: dev\n#   outputs:\n#     dev:\n#       type: duckdb\n#       path: 's3://bucket/warehouse/data.duckdb'\n#       extensions: [httpfs, parquet]\n\n# 10. Use cases parfaits\n# - Ad-hoc analytics (vs spinning Athena workgroup)\n# - Notebook data science\n# - Dashboard léger (dashboard query DuckDB)\n# - dbt CI (build models en RAM, validate)\n# - POC avant Snowflake/BigQuery\n\n# Limites:\n# - Single-node (max 100GB-1TB selon RAM)\n# - Pas de concurrence multi-user (sauf MotherDuck)\n# - Pas de transactions multi-table durable",
      },
      {
        id: "d152",
        label: "DuckDB + dbt",
        day: "Jeu",
        description: "dbt-duckdb = combo magique : dbt transforms + DuckDB engine. Pas besoin de cloud DW. Idéal pour POC, formation, projets perso. Ton portfolio dbt sans facture Snowflake.",
        url: "https://github.com/duckdb/dbt-duckdb",
        resource: "dbt-duckdb GitHub",
        resources: [
          { type: "github", title: "dbt-duckdb", url: "https://github.com/duckdb/dbt-duckdb" },
          { type: "article", title: "dbt + DuckDB Tutorial", url: "https://duckdb.org/2022/05/04/dbt-on-duckdb.html" },
        ],
        exercise: "# Objectif: dbt local sans cloud DW\n\n# Install: pip install dbt-duckdb\n\n# 1. Setup dbt projet\ndbt init mobility   # choisir 'duckdb' adapter\ncd mobility\n\n# 2. profiles.yml (~/.dbt/profiles.yml)\n# mobility:\n#   target: dev\n#   outputs:\n#     dev:\n#       type: duckdb\n#       path: ./mobility.duckdb     # fichier local\n#       schema: dev\n#       threads: 4\n#       extensions:\n#         - httpfs\n#         - parquet\n#         - spatial\n#       settings:\n#         s3_region: 'eu-west-3'\n#       external_root: ./external/    # pour CSV/Parquet sources\n\n# 3. Sources directly from files\n# models/staging/_sources.yml\n# version: 2\n# sources:\n#   - name: raw\n#     meta:\n#       external_location: '../data/raw/{name}.parquet'\n#     tables:\n#       - name: trips\n#       - name: vehicles\n# \n# Maintenant {{ source('raw','trips') }} = read le Parquet\n\n# 4. Models normaux\n# models/staging/stg_trips.sql:\n# SELECT trip_id, vehicle_id,\n#     CAST(started_at AS TIMESTAMP) AS started_at,\n#     CAST(distance_m / 1000 AS DECIMAL(10,2)) AS distance_km\n# FROM {{ source('raw','trips') }}\n# WHERE distance_m > 0\n\n# 5. dbt build\ndbt build\n# Tout en local, ultra rapide\n\n# 6. Marts vers Parquet (au lieu de table DuckDB)\n# models/marts/fct_trips.sql:\n# {{ config(\n#     materialized='external',\n#     location='./marts/fct_trips.parquet',\n#     format='parquet'\n# ) }}\n# SELECT ...\n\n# = output direct en Parquet, queryable par autres outils\n\n# 7. CI complète sans cloud\n# .github/workflows/dbt.yml:\n# - run: pip install dbt-duckdb\n# - run: dbt build\n# - run: dbt docs generate\n# - run: dbt-osmosis docs propagate\n\n# 8. dbt-duckdb sur S3 (production)\n# profiles:\n#   prod:\n#     type: duckdb\n#     path: 's3://my-bucket/warehouse.duckdb'\n#     extensions: [httpfs]\n#     settings:\n#       s3_region: ...\n#       s3_access_key_id: \"{{ env_var('AWS_ACCESS_KEY_ID') }}\"\n\n# 9. Production setups\n# - GitHub Actions cron + DuckDB → S3\n# - Vercel cron + DuckDB → R2\n# - MotherDuck (DuckDB cloud)\n\n# 10. Combo killer: dbt-duckdb + Streamlit\n# dashboard.py:\nimport duckdb\nimport streamlit as st\n\ncon = duckdb.connect('warehouse.duckdb', read_only=True)\nstats = con.execute('SELECT * FROM marts.kpi_monthly').df()\nst.dataframe(stats)\n\n# = full data app sans serveur DB\n# Cost: ~5€/mois (Streamlit + GitHub Actions)",
      },
      {
        id: "d153",
        label: "Projet: Mini-warehouse DuckDB",
        day: "Sam",
        description: "Projet portfolio léger : warehouse complet DuckDB + dbt + dashboard, gratuit. Démontre maîtrise modern data stack sans dépendance cloud. Idéal pour montrer en entretien.",
        url: "https://github.com/duckdb/dbt-duckdb",
        resource: "dbt-duckdb",
        resources: [
          { type: "github", title: "dbt-duckdb examples", url: "https://github.com/duckdb/dbt-duckdb#examples" },
          { type: "tool", title: "Evidence (BI gratuit DuckDB)", url: "https://evidence.dev/" },
        ],
        exercise: "# Projet: Mini Data Warehouse DuckDB (gratuit)\n\n# Repo: github.com/nassim/duckdb-warehouse\n\n# Stack 100% gratuite:\n# - DuckDB (engine)\n# - dbt-duckdb (transforms)\n# - GitHub Actions (cron)\n# - Streamlit Cloud (dashboard)\n# - GitHub Pages (dbt docs)\n\n# Use case: Analyse Vélib (data publique)\n\n# Architecture:\n# Vélib API → ingest.py → S3/local Parquet (raw)\n#                              ↓\n#                          dbt-duckdb\n#                              ↓\n#                  Marts (Parquet/DuckDB file)\n#                              ↓\n#                      Streamlit Dashboard\n\n# Structure:\n# warehouse/\n# ├── ingest/\n# │   ├── velib.py            # appel API + write Parquet\n# │   └── weather.py\n# ├── dbt/\n# │   ├── models/\n# │   ├── snapshots/\n# │   ├── tests/\n# │   ├── dbt_project.yml\n# │   └── profiles.yml\n# ├── dashboard/\n# │   ├── streamlit_app.py\n# │   └── pages/\n# ├── .github/workflows/\n# │   └── pipeline.yml        # cron quotidien\n# ├── data/                    # NE PAS commit (gitignore)\n# └── README.md\n\n# .github/workflows/pipeline.yml\n# name: Daily ETL\n# on:\n#   schedule:\n#     - cron: '0 6 * * *'      # 6 AM UTC quotidien\n#   workflow_dispatch:\n# jobs:\n#   etl:\n#     runs-on: ubuntu-latest\n#     steps:\n#       - uses: actions/checkout@v4\n#       - uses: actions/setup-python@v5\n#         with: { python-version: '3.11' }\n#       - run: pip install -r requirements.txt\n#       - run: python ingest/velib.py\n#       - run: cd dbt && dbt build\n#       - run: cd dbt && dbt docs generate\n#       - name: Upload to S3 (free Cloudflare R2 alternatif)\n#         run: aws s3 sync data/marts s3://my-bucket/marts/\n#         env:\n#           AWS_ACCESS_KEY_ID: ${{ secrets.AWS_KEY }}\n#           AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET }}\n#       - name: Deploy docs\n#         uses: peaceiris/actions-gh-pages@v3\n#         with:\n#           publish_dir: ./dbt/target/\n\n# Total cost: 0€\n# - GitHub Actions: 2000 min/mois free\n# - GitHub Pages: free\n# - Streamlit Cloud: free\n# - Cloudflare R2: 10GB free\n\n# Critères qualité:\n# ✓ Pipeline quotidien automatique\n# ✓ dbt models bien organisés\n# ✓ Tests passent\n# ✓ Docs dbt déployés (GitHub Pages)\n# ✓ Dashboard Streamlit live\n# ✓ README avec setup en 5 min\n# ✓ Architecture diagram\n# ✓ 1 article LinkedIn\n\n# Stretch goals:\n# - Slack notification si fail\n# - Anomaly detection (volume drop)\n# - Predict next-hour demand (sklearn)\n# - Monitoring (uptime + freshness)\n\n# Article LinkedIn:\n# 'J'ai construit un Data Warehouse pour 0€ avec DuckDB'\n# - Stack: DuckDB + dbt + GitHub + Streamlit\n# - Use case réel (Vélib)\n# - Code open source\n# - Démontre que tu connais modern data stack\n# - Sans budget cloud!\n\n# Ce projet seul peut te démarquer en entretien Data Engineer junior.",
      },
    ],
  },
];
