import type { Week } from "../types";

export const PHASE1_GEO: Week[] = [
  {
    id: "w5",
    title: "GeoPandas (1/2) — Bases",
    tasks: [
      {
        id: "d25",
        label: "CRS et projections",
        day: "Lun",
        description: "Le CRS (Coordinate Reference System) définit comment représenter la Terre sur un plan. EPSG:4326 = lat/lon en degrés (pour stockage), EPSG:3857 (Web Mercator pour cartes), EPSG:32630 = UTM en mètres (pour distances). Confondre CRS = bug classique en geo (distances en degrés !!).",
        url: "https://geopandas.org/en/stable/docs/user_guide/projections.html",
        resource: "GeoPandas - Projections Guide",
        resources: [
          { type: "doc", title: "GeoPandas - Projections", url: "https://geopandas.org/en/stable/docs/user_guide/projections.html" },
          { type: "tool", title: "epsg.io (recherche CRS)", url: "https://epsg.io/" },
          { type: "article", title: "Map Projections Visual Guide", url: "https://www.axismaps.com/guide/map-projections" },
          { type: "doc", title: "PROJ Documentation", url: "https://proj.org/en/9.3/" },
        ],
        exercise: "# Objectif: maîtriser CRS pour ne plus jamais bugger\n\nimport geopandas as gpd\nfrom shapely.geometry import Point\n\n# 1. CRS courants\n# EPSG:4326 - WGS84 (lat/lon en degrés) - GPS, OSM, GeoJSON\n# EPSG:3857 - Web Mercator - cartes web (Google, OSM tiles)\n# EPSG:2154 - Lambert 93 - France métropolitaine (mètres)\n# EPSG:32630 - UTM Zone 30N - Europe Ouest (mètres)\n\n# 2. Créer GeoDataFrame avec CRS\npoints = [Point(2.3522, 48.8566),   # Paris (lon, lat) !!\n          Point(4.8357, 45.7640)]    # Lyon\ngdf = gpd.GeoDataFrame({'city':['Paris','Lyon']},\n                       geometry=points,\n                       crs='EPSG:4326')\n\n# 3. Reprojection (lat/lon → mètres)\ngdf_m = gdf.to_crs('EPSG:2154')   # Lambert 93\n# Maintenant gdf_m.geometry.x est en mètres !\n\n# 4. PIÈGE: distance en degrés vs mètres\nparis_4326 = Point(2.35, 48.85)\nlyon_4326 = Point(4.83, 45.76)\nprint(paris_4326.distance(lyon_4326))   # 3.95 (DEGRÉS, useless)\n\nparis_m = gpd.GeoSeries([paris_4326], crs='EPSG:4326').to_crs('EPSG:2154')[0]\nlyon_m = gpd.GeoSeries([lyon_4326], crs='EPSG:4326').to_crs('EPSG:2154')[0]\nprint(paris_m.distance(lyon_m))   # ~390000 mètres (OK)\n\n# 5. Auto-détection CRS optimal (UTM)\ngdf_utm = gdf.to_crs(gdf.estimate_utm_crs())\n\n# 6. Vérifier CRS d'un fichier\nimport fiona\nwith fiona.open('data.geojson') as src:\n    print(src.crs)\n\n# Règle d'or:\n# - Stockage / I/O: EPSG:4326\n# - Calculs distance/aire: projeté (UTM, Lambert)\n# - Affichage carte: EPSG:3857",
      },
      {
        id: "d26",
        label: "Point, LineString, Polygon (Shapely)",
        day: "Mar",
        description: "Shapely = librairie pour manipuler géométries en mémoire. Point (position), LineString (trajet), Polygon (zone). Les opérations within/contains/intersects sont la base de tout calcul géospatial. GeoPandas utilise Shapely sous le capot.",
        url: "https://shapely.readthedocs.io/en/stable/manual.html",
        resource: "Shapely Manual",
        resources: [
          { type: "doc", title: "Shapely Documentation", url: "https://shapely.readthedocs.io/en/stable/manual.html" },
          { type: "doc", title: "Shapely Geometry Types", url: "https://shapely.readthedocs.io/en/stable/geometry.html" },
          { type: "article", title: "OGC Simple Features Spec", url: "https://www.ogc.org/standards/sfa" },
        ],
        exercise: "# Objectif: maîtriser géométries Shapely\n\nfrom shapely.geometry import Point, LineString, Polygon, MultiPoint\nfrom shapely.ops import unary_union, nearest_points\n\n# 1. Créer géométries\np = Point(2.35, 48.85)                     # x=lon, y=lat\nline = LineString([(2.3, 48.8), (2.4, 48.9), (2.5, 48.7)])\npoly = Polygon([(0,0), (1,0), (1,1), (0,1), (0,0)])  # carré\ntriangle = Polygon([(0,0), (2,0), (1,2)])\n\n# 2. Properties utiles\nprint(p.x, p.y)\nprint(line.length)         # longueur\nprint(poly.area)            # aire\nprint(poly.bounds)          # (minx, miny, maxx, maxy)\nprint(poly.centroid)\nprint(poly.exterior.coords[:])\n\n# 3. Tests spatiaux (booléens)\np_in = Point(0.5, 0.5)\np_out = Point(2, 2)\np_in.within(poly)         # True - point dans poly\npoly.contains(p_in)        # True - inverse\npoly.intersects(triangle)  # True\npoly.touches(triangle)     # False (pas seulement bord)\np.distance(line)            # distance min\n\n# 4. Operations spatiales\npoly1 = Polygon([(0,0),(2,0),(2,2),(0,2)])\npoly2 = Polygon([(1,1),(3,1),(3,3),(1,3)])\n\nintersection = poly1.intersection(poly2)   # zone commune\nunion = poly1.union(poly2)                  # combiné\ndifference = poly1.difference(poly2)        # poly1 sans poly2\nsymdiff = poly1.symmetric_difference(poly2) # XOR\n\n# 5. Buffer (élargir géométrie)\np = Point(0, 0)\ncircle = p.buffer(1.0)        # cercle rayon 1\n# Sur GPS en EPSG:2154: buffer(500) = 500 mètres\n\n# 6. Cas pratique: trouver point le plus proche\npts = MultiPoint([(0,0), (1,1), (2,3), (5,5)])\ntarget = Point(1.5, 1.5)\nnearest = nearest_points(target, pts)[1]\nprint(f'Plus proche: {nearest}')\n\n# 7. Validate geometries\npoly = Polygon([(0,0),(1,0),(1,1),(0,1),(0,0)])\nprint(poly.is_valid)\n# Repair invalid: poly.buffer(0)",
      },
      {
        id: "d27",
        label: "Spatial joins (sjoin)",
        day: "Mer",
        description: "Spatial join = join SQL mais avec des géométries (point dans polygone, lignes qui se croisent). Au lieu de faire 1M boucles for, sjoin utilise un index spatial = 1000x plus rapide. Indispensable pour enrichir données GPS avec zones, communes, quartiers.",
        url: "https://geopandas.org/en/stable/docs/user_guide/mergingdata.html",
        resource: "GeoPandas - Spatial Joins",
        resources: [
          { type: "doc", title: "GeoPandas Merging/Joins", url: "https://geopandas.org/en/stable/docs/user_guide/mergingdata.html" },
          { type: "doc", title: "GeoPandas - sindex (R-tree)", url: "https://geopandas.org/en/stable/docs/reference/sindex.html" },
          { type: "article", title: "Spatial Indexing Explained", url: "https://www.youtube.com/watch?v=hsCJDfGr_iI" },
        ],
        exercise: "# Objectif: enrichir GPS avec zones administratives\n\nimport geopandas as gpd\n\n# 1. Charger points GPS et polygones (communes France)\npoints = gpd.read_file('gps_points.geojson').to_crs('EPSG:2154')\ncommunes = gpd.read_file('communes.geojson').to_crs('EPSG:2154')\n\n# 2. Spatial join (point WITHIN polygon)\nenriched = gpd.sjoin(\n    points,\n    communes[['code_insee','nom','geometry']],\n    how='left',           # garde tous les points\n    predicate='within'    # point dans polygone\n)\n# Maintenant chaque GPS a code_insee + nom_commune !\n\n# Predicates disponibles:\n# 'intersects' (overlap any)\n# 'within' (entièrement dans)\n# 'contains' (inverse)\n# 'crosses', 'touches', 'overlaps'\n\n# 3. sjoin_nearest (point le plus proche)\nstations = gpd.read_file('stations_metro.geojson')\nwith_metro = gpd.sjoin_nearest(\n    points,\n    stations,\n    how='left',\n    distance_col='dist_metro',\n    max_distance=500   # ignore si > 500m\n)\n\n# 4. Performance: comparer avec approche naïve\nimport time\n\n# LENT (boucle for):\nt = time.time()\nfor idx, p in points.iterrows():\n    for _, c in communes.iterrows():\n        if p.geometry.within(c.geometry):\n            break\nprint(f'Naive: {time.time()-t:.1f}s')\n\n# RAPIDE (sjoin avec index):\nt = time.time()\nresult = gpd.sjoin(points, communes, predicate='within')\nprint(f'sjoin: {time.time()-t:.1f}s')\n# Speedup: 100-1000x !\n\n# 5. Spatial index manuel (avancé)\nidx = communes.sindex   # R-tree\n# Recherche candidats par bbox (très rapide)\nfor pt in points.geometry:\n    candidates = list(idx.intersection(pt.bounds))\n    # Puis test précis sur candidates seulement\n\n# 6. Group après sjoin\ntrips_per_commune = enriched.groupby('code_insee').size().sort_values(ascending=False)",
      },
      {
        id: "d28",
        label: "Buffers, intersections, dissolve",
        day: "Jeu",
        description: "Buffer = zone d'influence autour d'un point (laverie, station). Intersection = chevauchement entre zones. Dissolve = aggréger plusieurs polygones (départements → région). Operations métier classiques en analyse géomarketing.",
        url: "https://geopandas.org/en/stable/docs/user_guide/set_operations.html",
        resource: "GeoPandas - Set Operations",
        resources: [
          { type: "doc", title: "GeoPandas Set Operations", url: "https://geopandas.org/en/stable/docs/user_guide/set_operations.html" },
          { type: "doc", title: "GeoPandas - Aggregation", url: "https://geopandas.org/en/stable/docs/user_guide/aggregation_with_dissolve.html" },
          { type: "article", title: "Geomarketing avec Python", url: "https://towardsdatascience.com/geospatial-data-analysis-using-python-3994b1cea6cf" },
        ],
        exercise: "# Objectif: opérations spatiales métier\n\nimport geopandas as gpd\n\n# 1. Buffer (zone d'influence)\nlaveries = gpd.read_file('laveries.geojson').to_crs('EPSG:2154')\n\n# Zone d'influence 500m autour de chaque laverie\nzones = laveries.copy()\nzones.geometry = laveries.geometry.buffer(500)   # mètres\n\n# 2. Quels habitants dans zone d'influence ?\nbati = gpd.read_file('batiments.geojson').to_crs('EPSG:2154')\nin_zone = gpd.sjoin(bati, zones, predicate='intersects')\nprint(f'{len(in_zone)} bâtiments dans zone laverie')\n\n# 3. Intersection (chevauchement entre zones)\n# Combien de surface se chevauche entre 2 laveries ?\nzone_a = zones.iloc[0].geometry\nzone_b = zones.iloc[1].geometry\noverlap = zone_a.intersection(zone_b)\nprint(f'Overlap: {overlap.area} m²')\n\n# 4. Dissolve (aggréger polygones)\ncommunes = gpd.read_file('communes.geojson')\n# Aggrégér communes par département\ndepts = communes.dissolve(by='code_dept', aggfunc={\n    'population':'sum',\n    'nom':'first'\n})\n\n# 5. Cas pratique: cannibalization analysis\n# Zone exclusive = zone - intersection avec autres\nzones['zone_exclusive'] = zones.geometry\nfor i in zones.index:\n    others = zones.drop(i).unary_union\n    zones.loc[i, 'zone_exclusive'] = zones.loc[i].geometry.difference(others)\n\nzones['exclusive_area'] = zones.zone_exclusive.area\nzones['cannibalized'] = (zones.geometry.area - zones.exclusive_area) / zones.geometry.area\n\n# 6. Concave hull (envelope ajustée)\nfrom shapely.geometry import MultiPoint\npts_paris = points[points.commune == 'Paris']\nhull = MultiPoint(pts_paris.geometry.tolist()).convex_hull\n\n# 7. Simplify (réduire complexité polygone)\nsimple = communes.geometry.simplify(tolerance=100, preserve_topology=True)\n# Idéal pour viz web (réduit taille GeoJSON 10x)",
      },
      {
        id: "d29",
        label: "GeoJSON, Shapefile, GeoPackage",
        day: "Ven",
        description: "GeoJSON = standard web (lisible JSON), Shapefile = legacy (5+ fichiers .shp/.dbf/...), GeoPackage = SQLite spatial moderne (1 fichier propre). Savoir lire/écrire tous = travailler avec n'importe quelle source open data.",
        url: "https://geopandas.org/en/stable/docs/user_guide/io.html",
        resource: "GeoPandas - I/O",
        resources: [
          { type: "doc", title: "GeoPandas I/O Guide", url: "https://geopandas.org/en/stable/docs/user_guide/io.html" },
          { type: "tool", title: "geojson.io (éditeur en ligne)", url: "https://geojson.io/" },
          { type: "doc", title: "GeoJSON Spec (RFC 7946)", url: "https://datatracker.ietf.org/doc/html/rfc7946" },
          { type: "tool", title: "QGIS (open source GIS)", url: "https://qgis.org/" },
        ],
        exercise: "# Objectif: I/O géospatial multi-formats\n\nimport geopandas as gpd\n\n# 1. Lecture (auto-detect format)\ngdf = gpd.read_file('data.geojson')\ngdf = gpd.read_file('data.shp')\ngdf = gpd.read_file('data.gpkg', layer='communes')   # GeoPackage = multi-layer\ngdf = gpd.read_file('data.kml')\n\n# 2. Lecture depuis URL/zip (super pratique)\nurl = 'https://france-geojson.gregoiredavid.fr/repo/regions.geojson'\nregions = gpd.read_file(url)\n\nzip_url = 'https://example.com/data.zip'\ngdf = gpd.read_file(f'zip+{zip_url}!data.shp')\n\n# 3. Lecture filtrée (économise RAM)\nimport pyogrio\nbbox = (2.0, 48.0, 3.0, 49.0)   # autour de Paris\nparis_only = gpd.read_file('france.gpkg', bbox=bbox)\n\n# Filter by attribute (SQL-like, sans tout charger)\ngdf = gpd.read_file('communes.gpkg',\n    where=\"code_dept = '75'\")\n\n# 4. Écriture\ngdf.to_file('out.geojson', driver='GeoJSON')\ngdf.to_file('out.gpkg', driver='GPKG', layer='points')\ngdf.to_file('out.shp')   # legacy, à éviter\ngdf.to_parquet('out.parquet')   # plus rapide à relire\n\n# 5. GeoJSON manuel (pour APIs)\nimport json\ngeojson_str = gdf.to_json()\ndata = json.loads(geojson_str)\n\n# 6. Sources open data utiles:\n# - data.gouv.fr/communes\n# - opendata.paris.fr\n# - Overture Maps (global, gratuit)\n# - OpenStreetMap via Overpass\n\n# 7. Créer GeoJSON sur geojson.io et l'utiliser\n# 1. Va sur geojson.io\n# 2. Dessine polygones zones d'analyse\n# 3. Save → download .geojson\n# 4. Charge dans Python\nzones = gpd.read_file('mes_zones.geojson')",
      },
      {
        id: "d30",
        label: "Projet: Carte open data",
        day: "Sam",
        description: "Projet portfolio combinant open data + spatial join + viz interactive. Les recruteurs en data adorent les projets géo car c'est immédiatement visuel et impressionnant. Choisis un sujet local pour rester proche du concret.",
        url: "https://opendata.paris.fr/",
        resource: "Open Data Paris",
        resources: [
          { type: "tool", title: "Open Data Paris", url: "https://opendata.paris.fr/" },
          { type: "tool", title: "data.gouv.fr", url: "https://www.data.gouv.fr/" },
          { type: "tool", title: "France GeoJSON", url: "https://github.com/gregoiredavid/france-geojson" },
          { type: "github", title: "Awesome Open Data FR", url: "https://github.com/dbohdan/awesome-open-data" },
        ],
        exercise: "# Projet: Carte interactive open data\n\n# Idées (choisis-en 1):\n# - Densité parcs Paris par arrondissement\n# - Bornes recharge VE par département\n# - Accidents vélo France 2023\n# - Foncier vacant Lyon\n# - Loyers moyens par commune IDF\n\n# Stack:\nimport geopandas as gpd\nimport folium\nfrom folium import Choropleth\nimport pandas as pd\n\n# 1. Récupère 2 sources open data:\n#    - Polygones (communes/départements/quartiers)\n#    - Données métier (CSV avec lat/lon ou code commune)\n\ncommunes = gpd.read_file('communes-idf.geojson')\nbornes = pd.read_csv('bornes-recharge.csv')\n\n# 2. Crée GeoDataFrame avec coordonnées\nbornes_gdf = gpd.GeoDataFrame(\n    bornes,\n    geometry=gpd.points_from_xy(bornes.lon, bornes.lat),\n    crs='EPSG:4326'\n)\n\n# 3. Spatial join: bornes par commune\njoined = gpd.sjoin(bornes_gdf, communes, predicate='within')\nstats = joined.groupby('code_insee').size().reset_index(name='n_bornes')\n\n# 4. Merge stats avec géométries communes\ncommunes_stats = communes.merge(stats, on='code_insee', how='left').fillna(0)\ncommunes_stats['densite'] = communes_stats.n_bornes / communes_stats.population * 1000\n\n# 5. Carte choroplèthe Folium\nm = folium.Map(location=[48.85, 2.35], zoom_start=10, tiles='cartodbpositron')\n\nChoropleth(\n    geo_data=communes_stats.to_json(),\n    data=communes_stats,\n    columns=['code_insee', 'densite'],\n    key_on='feature.properties.code_insee',\n    fill_color='YlOrRd',\n    legend_name='Bornes / 1000 hab',\n).add_to(m)\n\n# 6. Marqueurs détail\nfor _, b in bornes_gdf.iterrows():\n    folium.CircleMarker(\n        [b.lat, b.lon],\n        radius=3,\n        popup=f\"{b.nom} - {b.puissance} kW\",\n        color='blue',\n    ).add_to(m)\n\nm.save('carte.html')\n\n# Livrables GitHub:\n# - notebook.ipynb (code commenté)\n# - carte.html (interactive)\n# - README avec screenshots + insights métier\n# - data/sources.md (provenance données)",
      },
    ],
  },
  {
    id: "w6",
    title: "GeoPandas (2/2) — Avancé",
    tasks: [
      {
        id: "d31",
        label: "Haversine vs projeté (distances)",
        day: "Lun",
        description: "Pour calculer une distance entre 2 points GPS : Haversine (formule sphère, en degrés directement) ou projection en mètres puis distance euclidienne. À petite échelle, projeté est plus précis. À grande échelle (continents), Haversine.",
        url: "https://en.wikipedia.org/wiki/Haversine_formula",
        resource: "Wikipedia - Haversine",
        resources: [
          { type: "doc", title: "Haversine Formula Wikipedia", url: "https://en.wikipedia.org/wiki/Haversine_formula" },
          { type: "tool", title: "geopy (distances)", url: "https://github.com/geopy/geopy" },
          { type: "article", title: "Distances on Earth Compared", url: "https://towardsdatascience.com/calculating-distance-between-two-geolocations-in-python-26ad3afe287b" },
        ],
        exercise: "# Objectif: calculer distances correctement\n\nimport numpy as np\nimport math\n\n# 1. Haversine from scratch (vectorisé)\ndef haversine(lat1, lon1, lat2, lon2):\n    \"\"\"Distance en km. Inputs en degrés.\n    Vectorisé: lat1/lon1 peuvent être arrays.\"\"\"\n    R = 6371   # rayon Terre km\n    lat1, lon1, lat2, lon2 = map(np.radians, [lat1, lon1, lat2, lon2])\n    dlat = lat2 - lat1\n    dlon = lon2 - lon1\n    a = np.sin(dlat/2)**2 + np.cos(lat1) * np.cos(lat2) * np.sin(dlon/2)**2\n    return 2 * R * np.arcsin(np.sqrt(a))\n\n# Test\nparis = (48.8566, 2.3522)\nlyon = (45.7640, 4.8357)\nprint(f'Paris-Lyon: {haversine(*paris, *lyon):.1f} km')   # 392 km\n\n# 2. Avec geopy (plus simple, plus précis - Vincenty)\nfrom geopy.distance import geodesic\nprint(geodesic(paris, lyon).kilometers)   # 391.5\n\n# 3. Distance vectorisée 1M points (vs centre Paris)\nimport pandas as pd\ndf = pd.DataFrame({\n    'lat': np.random.uniform(48.7, 48.9, 1_000_000),\n    'lon': np.random.uniform(2.2, 2.4, 1_000_000)\n})\ndf['dist_paris_km'] = haversine(df.lat, df.lon, *paris)\n\n# 4. Comparaison: Haversine vs UTM projeté\nimport geopandas as gpd\nfrom shapely.geometry import Point\n\npts = gpd.GeoDataFrame(\n    geometry=[Point(p[1],p[0]) for p in [paris, lyon]],\n    crs='EPSG:4326'\n).to_crs('EPSG:32631')   # UTM Zone 31N\n\ndist_utm = pts.geometry[0].distance(pts.geometry[1]) / 1000\nprint(f'UTM: {dist_utm:.1f} km')   # ~390 km (légèrement différent)\n\n# 5. Benchmark: 1M distances\nimport time\nlat1 = np.random.uniform(40, 50, 1_000_000)\nlon1 = np.random.uniform(0, 10, 1_000_000)\nt = time.time(); haversine(lat1, lon1, 48.8, 2.3); print(f'Haversine vec: {time.time()-t:.3f}s')\n# 0.05s (vectorisé NumPy)\n\n# 6. Quand utiliser quoi:\n# - Petite zone (<100km): UTM/Lambert (plus précis, opérations OK)\n# - Continental: Haversine (sphère, suffisant)\n# - Précision navale: Vincenty (ellipsoïde, geopy)\n# - Approximation rapide: degrés² × 111 (1° lat ≈ 111km)",
      },
      {
        id: "d32",
        label: "DBSCAN géospatial (clustering)",
        day: "Mar",
        description: "DBSCAN trouve des clusters de densité (vs KMeans qui force k clusters). Idéal pour identifier hubs (origines GPS, points de chute Uber). Outliers automatiquement détectés. Très utilisé en analyse mobilité.",
        url: "https://scikit-learn.org/stable/modules/clustering.html#dbscan",
        resource: "scikit-learn - DBSCAN",
        resources: [
          { type: "doc", title: "sklearn DBSCAN", url: "https://scikit-learn.org/stable/modules/clustering.html#dbscan" },
          { type: "video", title: "StatQuest - DBSCAN (intuition)", url: "https://youtu.be/RDZUdRSDOok" },
          { type: "article", title: "DBSCAN avec coordonnées GPS", url: "https://geoffboeing.com/2014/08/clustering-to-reduce-spatial-data-set-size/" },
        ],
        exercise: "# Objectif: identifier hubs GPS automatiquement\n\nimport numpy as np\nfrom sklearn.cluster import DBSCAN\nimport geopandas as gpd\nimport folium\n\n# 1. Prep données\ndf = pd.read_csv('gps_origins.csv')[['lat','lon']]\n\n# 2. DBSCAN avec haversine (degrees)\n# eps = rayon en RADIANS (1.5km / 6371km Terre)\nkms_per_radian = 6371.0\nepsilon = 0.1 / kms_per_radian   # 100m\n\ncoords = df[['lat','lon']].values\ncoords_rad = np.radians(coords)\n\ndb = DBSCAN(\n    eps=epsilon,\n    min_samples=10,\n    metric='haversine',\n    algorithm='ball_tree'\n).fit(coords_rad)\n\ndf['cluster'] = db.labels_\n# -1 = noise (point isolé)\nprint(f'{df.cluster.nunique()-1} clusters trouvés')\nprint(f'{(df.cluster == -1).sum()} points outliers')\n\n# 3. Centroides des clusters\ncentroids = df[df.cluster != -1].groupby('cluster').agg({\n    'lat':'mean', 'lon':'mean', 'cluster':'size'\n}).rename(columns={'cluster':'size'})\n\n# 4. Visualisation Folium\nm = folium.Map(location=[df.lat.mean(), df.lon.mean()], zoom_start=12)\n\n# Points colorés par cluster\nimport matplotlib.cm as cm\ncolors = cm.tab10(np.linspace(0, 1, df.cluster.nunique()))\nfor _, row in df.sample(5000).iterrows():   # sample pour perf\n    c = colors[row.cluster] if row.cluster != -1 else (0,0,0,1)\n    folium.CircleMarker([row.lat, row.lon], radius=2,\n        color=f'rgb({int(c[0]*255)},{int(c[1]*255)},{int(c[2]*255)})',\n        fill=True).add_to(m)\n\n# Centroides cluster (cercle gros)\nfor cid, row in centroids.iterrows():\n    folium.Circle([row.lat, row.lon],\n        radius=100,\n        color='red',\n        popup=f'Cluster {cid}: {row[\"size\"]} pts'\n    ).add_to(m)\n\nm.save('clusters.html')\n\n# 5. HDBSCAN (auto-tune density)\nimport hdbscan\nclusterer = hdbscan.HDBSCAN(min_cluster_size=30, metric='haversine')\nlabels = clusterer.fit_predict(coords_rad)\n\n# 6. Tuning eps avec K-distance plot\nfrom sklearn.neighbors import NearestNeighbors\nnbrs = NearestNeighbors(n_neighbors=10).fit(coords_rad)\ndists, _ = nbrs.kneighbors(coords_rad)\ndists = np.sort(dists[:, -1])\n# Plot: trouve le 'coude' = bon eps",
      },
      {
        id: "d33",
        label: "Heatmaps & visualisation densité",
        day: "Mer",
        description: "Heatmap = visualiser concentration (où il y a beaucoup de points). Folium HeatMap pour cartes interactives, hexbin matplotlib pour static print. KDE = densité continue. Indispensable pour rapports métier visuels.",
        url: "https://python-visualization.github.io/folium/latest/user_guide/plugins/heat_map.html",
        resource: "Folium - HeatMap Plugin",
        resources: [
          { type: "doc", title: "Folium HeatMap", url: "https://python-visualization.github.io/folium/latest/user_guide/plugins/heat_map.html" },
          { type: "doc", title: "Matplotlib hexbin", url: "https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.hexbin.html" },
          { type: "tool", title: "kepler.gl (web heatmaps)", url: "https://kepler.gl/" },
          { type: "doc", title: "scipy.stats.gaussian_kde", url: "https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.gaussian_kde.html" },
        ],
        exercise: "# Objectif: heatmaps géospatiales\n\nimport folium\nfrom folium.plugins import HeatMap, HeatMapWithTime\nimport numpy as np\nimport pandas as pd\n\ndf = pd.read_csv('gps.csv')\n\n# 1. Heatmap simple\nm = folium.Map(location=[df.lat.mean(), df.lon.mean()], zoom_start=12)\nheat_data = df[['lat','lon']].values.tolist()\nHeatMap(heat_data, radius=15, blur=20, max_zoom=15).add_to(m)\nm.save('heatmap.html')\n\n# 2. Heatmap pondérée (par valeur)\n# Plus la vitesse est élevée, plus le point pèse\nweighted = df[['lat','lon','speed']].values.tolist()\nHeatMap(weighted, radius=15).add_to(m)\n\n# 3. Heatmap animée (time-slider)\ndf['hour'] = pd.to_datetime(df.ts).dt.hour\ndata_per_hour = []\nfor h in range(24):\n    sub = df[df.hour == h][['lat','lon']].values.tolist()\n    data_per_hour.append(sub)\n\nm2 = folium.Map(location=[df.lat.mean(), df.lon.mean()], zoom_start=12)\nHeatMapWithTime(data_per_hour,\n    index=[f'{h}h' for h in range(24)],\n    auto_play=True,\n    max_opacity=0.8\n).add_to(m2)\nm2.save('heatmap_time.html')\n\n# 4. Hexbin (static, pour rapport PDF)\nimport matplotlib.pyplot as plt\nfig, ax = plt.subplots(figsize=(10,8))\nhb = ax.hexbin(df.lon, df.lat, gridsize=50, cmap='YlOrRd', mincnt=1)\nplt.colorbar(hb, label='Nombre de points')\nax.set_xlabel('Longitude')\nax.set_ylabel('Latitude')\nplt.savefig('hexbin.png', dpi=150, bbox_inches='tight')\n\n# 5. KDE 2D (smooth density)\nfrom scipy.stats import gaussian_kde\nx = df.lon.values\ny = df.lat.values\nkde = gaussian_kde([x, y])\n\nxx, yy = np.mgrid[x.min():x.max():100j, y.min():y.max():100j]\nzz = kde(np.vstack([xx.flatten(), yy.flatten()])).reshape(xx.shape)\n\nplt.contourf(xx, yy, zz, levels=20, cmap='YlOrRd')\nplt.scatter(x[::100], y[::100], s=1, alpha=0.3)\n\n# 6. Comparaison Heatmap vs Hexbin\n# Heatmap (Folium): interactive, zoom-aware, joli pour web\n# Hexbin (matplotlib): static, idéal PDF/article\n# KDE: lisse, scientifique, intervalles de confiance",
      },
      {
        id: "d34",
        label: "Isochrones & routing",
        day: "Jeu",
        description: "Isochrone = zone accessible en X minutes depuis un point (5min en voiture, 15min à pied). Calculé via routing API (OpenRouteService gratuit, OSRM, Google). Indispensable pour analyse d'accessibilité (catchment area).",
        url: "https://openrouteservice.org/",
        resource: "OpenRouteService - Free routing",
        resources: [
          { type: "tool", title: "OpenRouteService API (free)", url: "https://openrouteservice.org/" },
          { type: "doc", title: "OSRM (self-host)", url: "http://project-osrm.org/" },
          { type: "tool", title: "Mapbox Isochrone API", url: "https://docs.mapbox.com/api/navigation/isochrone/" },
          { type: "article", title: "Catchment Areas Analysis", url: "https://towardsdatascience.com/computing-isochrone-with-python-30b9bcb6cc40" },
        ],
        exercise: "# Objectif: zones accessibles depuis un point\n\nimport requests\nimport folium\nimport os\n\n# 1. Setup OpenRouteService\n# Inscription gratuite: openrouteservice.org\nORS_KEY = os.getenv('ORS_API_KEY')\nheaders = {'Authorization': ORS_KEY}\n\n# 2. Isochrone simple (5/10/15 min en voiture)\nlaverie_paris = [2.3522, 48.8566]   # [lon, lat] !\n\nbody = {\n    'locations': [laverie_paris],\n    'range': [300, 600, 900],   # secondes (5/10/15 min)\n    'attributes': ['area', 'reachfactor']\n}\n\nr = requests.post(\n    'https://api.openrouteservice.org/v2/isochrones/driving-car',\n    json=body,\n    headers=headers\n)\nisochrones = r.json()   # GeoJSON FeatureCollection\n\n# 3. Visualiser sur carte\nm = folium.Map(location=[48.8566, 2.3522], zoom_start=13)\n\ncolors = ['#ff0000', '#ff8800', '#ffcc00']\nfor i, feature in enumerate(isochrones['features']):\n    folium.GeoJson(\n        feature,\n        style_function=lambda x, c=colors[i]: {\n            'fillColor': c, 'color': c, 'fillOpacity': 0.3\n        },\n        tooltip=f\"{feature['properties']['value']/60:.0f} min\"\n    ).add_to(m)\n\nfolium.Marker(\n    [48.8566, 2.3522],\n    popup='Laverie',\n    icon=folium.Icon(color='blue', icon='star')\n).add_to(m)\n\nm.save('isochrone.html')\n\n# 4. Multiple modes (voiture vs piéton vs vélo)\nfor mode in ['driving-car', 'foot-walking', 'cycling-regular']:\n    body = {'locations': [laverie_paris], 'range': [600]}\n    r = requests.post(\n        f'https://api.openrouteservice.org/v2/isochrones/{mode}',\n        json=body, headers=headers\n    )\n    print(f'{mode}: {r.json()[\"features\"][0][\"properties\"][\"area\"]:.0f} m²')\n\n# 5. Cas pratique: combien d'habitants à 10min de la laverie ?\nimport geopandas as gpd\niso_10min = gpd.GeoDataFrame.from_features(\n    isochrones['features'][1:2],   # 10min\n    crs='EPSG:4326'\n)\n\nbati = gpd.read_file('batiments_paris.geojson')\nin_zone = gpd.sjoin(bati, iso_10min, predicate='within')\nprint(f'{in_zone.population.sum()} habitants à 10min')\n\n# 6. Routing (matrix multiple origines x destinations)\nbody = {\n    'locations': [[2.35, 48.85], [2.40, 48.85], [2.35, 48.90]],\n    'sources': [0],\n    'destinations': [1, 2],\n    'metrics': ['distance', 'duration']\n}\nr = requests.post(\n    'https://api.openrouteservice.org/v2/matrix/driving-car',\n    json=body, headers=headers\n)",
      },
      {
        id: "d35",
        label: "Spatial index R-tree",
        day: "Ven",
        description: "R-tree = structure d'arbre pour recherche spatiale rapide. Sans index : 1M points × 1M points = 10^12 comparaisons (impossible). Avec R-tree : ~10ms. GeoPandas l'utilise via .sindex automatiquement, mais comprendre c'est pouvoir optimiser.",
        url: "https://geopandas.org/en/stable/docs/reference/sindex.html",
        resource: "GeoPandas - sindex",
        resources: [
          { type: "doc", title: "GeoPandas sindex", url: "https://geopandas.org/en/stable/docs/reference/sindex.html" },
          { type: "doc", title: "Rtree library Python", url: "https://rtree.readthedocs.io/" },
          { type: "article", title: "R-tree explained", url: "https://en.wikipedia.org/wiki/R-tree" },
        ],
        exercise: "# Objectif: speedup spatial queries 1000x\n\nimport geopandas as gpd\nimport time\n\n# Setup: 100K points et 1K polygones\npoints = gpd.read_file('gps.geojson')        # 100K\nzones = gpd.read_file('zones.geojson')         # 1K\n\n# 1. Sans index (catastrophe)\ndef naive_within(points, zones):\n    results = []\n    for i, p in points.iterrows():\n        for j, z in zones.iterrows():\n            if p.geometry.within(z.geometry):\n                results.append((i, j))\n                break\n    return results\n# 100K * 1K = 100M comparaisons → minutes\n\n# 2. Avec sjoin (sindex automatique)\nt = time.time()\nresult = gpd.sjoin(points, zones, predicate='within')\nprint(f'sjoin: {time.time()-t:.2f}s')   # ~1-3s\n\n# 3. sindex manuel (cas custom)\nidx = zones.sindex   # construit R-tree\n\nresults = []\nfor _, p in points.iterrows():\n    # Étape 1: pre-filter par bounding box (très rapide)\n    candidates = list(idx.intersection(p.geometry.bounds))\n    # Étape 2: test précis sur candidats SEULEMENT\n    for c in candidates:\n        if p.geometry.within(zones.geometry.iloc[c]):\n            results.append((p.name, c))\n            break\n\n# 4. Benchmark progressif\nimport pandas as pd\nresults = []\nfor n in [1000, 10000, 100000, 1000000]:\n    pts_n = points.sample(n, replace=True)\n    t = time.time()\n    gpd.sjoin(pts_n, zones, predicate='within')\n    results.append((n, time.time() - t))\n\npd.DataFrame(results, columns=['n_points','time_s']).plot(\n    x='n_points', y='time_s', logx=True, logy=True\n)\n# Avec index: O(N log M)\n# Sans index: O(N * M)\n\n# 5. Index pour recherches custom\nfrom rtree import index\nidx = index.Index()\nfor i, geom in enumerate(zones.geometry):\n    idx.insert(i, geom.bounds)\n\n# Trouver toutes zones qui intersectent une bbox\nbbox = (2.3, 48.8, 2.4, 48.9)\nhits = list(idx.intersection(bbox))\n\n# 6. Persistance index sur disque (gros datasets)\np = index.Property()\np.dimension = 2\nidx_disk = index.Index('zones_idx', properties=p)   # crée fichiers\n# zones_idx.dat + zones_idx.idx",
      },
      {
        id: "d36",
        label: "Projet: Densité commerces OSM",
        day: "Sam",
        description: "Projet final geo : récupérer données OSM via Overpass API + analyse densité + visualisation. OpenStreetMap est une mine d'or de données géographiques gratuites. Très pratique pour études de marché ou analyses urbaines.",
        url: "https://overpass-turbo.eu/",
        resource: "Overpass Turbo (UI)",
        resources: [
          { type: "tool", title: "Overpass Turbo (test queries)", url: "https://overpass-turbo.eu/" },
          { type: "doc", title: "Overpass QL Language", url: "https://wiki.openstreetmap.org/wiki/Overpass_API/Overpass_QL" },
          { type: "tool", title: "osmnx (Python wrapper)", url: "https://osmnx.readthedocs.io/" },
          { type: "tool", title: "Pyrosm (fast OSM Python)", url: "https://pyrosm.readthedocs.io/" },
        ],
        exercise: "# Projet: Analyse densité commerces avec OSM\n\nimport osmnx as ox\nimport geopandas as gpd\nimport folium\nfrom shapely.geometry import Point\n\n# 1. Récupérer commerces autour de Paris (avec osmnx)\ntags = {'shop': True, 'amenity': ['restaurant','cafe','bar']}\nshops = ox.features_from_place('Paris, France', tags)\n\n# Filtrer pour ne garder que Points\nshops = shops[shops.geom_type == 'Point'].reset_index()\nprint(f'{len(shops)} commerces récupérés')\n\n# 2. Charger arrondissements\narr = gpd.read_file('arrondissements_paris.geojson')\n\n# 3. Spatial join\nshops_arr = gpd.sjoin(shops, arr, predicate='within')\n\n# 4. Densité par arrondissement\ndensity = shops_arr.groupby('c_ar').agg(\n    n_shops=('geometry','count')\n).reset_index()\ndensity = arr.merge(density, on='c_ar')\ndensity['density'] = density.n_shops / density.geometry.area * 1e6   # par km²\n\n# 5. Buffer 200/500/1000m autour de ta laverie\nlaverie = Point(2.3522, 48.8566)\nlaverie_gdf = gpd.GeoDataFrame(geometry=[laverie], crs='EPSG:4326').to_crs('EPSG:2154')\n\nresults = []\nfor radius_m in [200, 500, 1000]:\n    buffer = laverie_gdf.geometry.buffer(radius_m).iloc[0]\n    buffer_4326 = gpd.GeoSeries([buffer], crs='EPSG:2154').to_crs('EPSG:4326').iloc[0]\n    in_buffer = shops[shops.geometry.within(buffer_4326)]\n    results.append({\n        'radius_m': radius_m,\n        'n_shops': len(in_buffer),\n        'top_types': in_buffer.shop.value_counts().head(5).to_dict()\n    })\n\nfor r in results: print(r)\n\n# 6. Carte interactive\nm = folium.Map(location=[48.8566, 2.3522], zoom_start=14)\n\n# Buffers\nfor r, color in [(200,'red'),(500,'orange'),(1000,'yellow')]:\n    folium.Circle([48.8566, 2.3522], radius=r,\n        color=color, fill=True, fillOpacity=0.1,\n        popup=f'{r}m'\n    ).add_to(m)\n\n# Commerces\nfrom folium.plugins import MarkerCluster\nmc = MarkerCluster().add_to(m)\nfor _, s in shops_arr.iterrows():\n    folium.Marker(\n        [s.geometry.y, s.geometry.x],\n        popup=s.get('name','?')\n    ).add_to(mc)\n\nm.save('commerces.html')\n\n# Livrable GitHub:\n# - notebook.ipynb avec narratif\n# - carte.html\n# - Rapport: 'Quel meilleur emplacement laverie ?'\n# - Article LinkedIn (= portfolio + branding)",
      },
    ],
  },
];
