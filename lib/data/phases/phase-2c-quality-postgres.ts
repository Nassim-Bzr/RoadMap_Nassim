import type { Week } from "../types";

export const PHASE2_QUALITY_POSTGRES: Week[] = [
  {
    id: "w15",
    title: "Data Quality + Portfolio",
    tasks: [
      {
        id: "d81",
        label: "Great Expectations / dbt-expectations",
        day: "Lun",
        description: "Great Expectations = framework de tests data scientifiques. Définit 'expectations' (assertions) sur tes données et génère docs auto. dbt-expectations port pour dbt. Standard pour data quality en 2025.",
        url: "https://greatexpectations.io/",
        resource: "Great Expectations",
        resources: [
          { type: "doc", title: "Great Expectations Docs", url: "https://docs.greatexpectations.io/" },
          { type: "github", title: "dbt-expectations", url: "https://github.com/calogica/dbt-expectations" },
          { type: "tool", title: "Soda Core (alternative)", url: "https://github.com/sodadata/soda-core" },
          { type: "article", title: "Data Quality Frameworks Compared", url: "https://www.montecarlodata.com/blog-data-quality-tools/" },
        ],
        exercise: "# Objectif: data quality automatisé\n\n# Option 1: Great Expectations (Python)\n# pip install great_expectations\n\nimport great_expectations as gx\n\n# 1. Init\ncontext = gx.get_context()\n\n# 2. Connect data source\ndatasource = context.sources.add_pandas('my_pandas')\n\n# 3. Get validator (dataset)\nimport pandas as pd\ndf = pd.read_parquet('trips.parquet')\nvalidator = datasource.read_dataframe(df, asset_name='trips')\n\n# 4. Define expectations\nvalidator.expect_column_values_to_not_be_null('trip_id')\nvalidator.expect_column_values_to_be_unique('trip_id')\nvalidator.expect_column_values_to_be_between('speed_kmh', 0, 200)\nvalidator.expect_column_values_to_match_regex('email', r'^[\\w.-]+@[\\w.-]+\\.\\w+$')\nvalidator.expect_column_value_lengths_to_be_between('vehicle_id', 5, 20)\nvalidator.expect_table_row_count_to_be_between(min_value=1000)\nvalidator.expect_column_values_to_be_in_set('status', ['ok','cancelled','pending'])\n\n# 5. Save expectations suite\nvalidator.save_expectation_suite('trips_suite')\n\n# 6. Run validation\nresult = validator.validate()\nif not result.success:\n    print('FAILED expectations:')\n    for r in result.results:\n        if not r.success:\n            print(f'- {r.expectation_config.expectation_type}: {r.result}')\n\n# 7. Generate Data Docs (HTML report)\ncontext.build_data_docs()\ncontext.open_data_docs()   # ouvre dans browser\n\n# 8. Checkpoints (run en prod)\ncheckpoint = context.checkpoint(\n    name='trips_checkpoint',\n    validations=[{\n        'batch_request': {...},\n        'expectation_suite_name': 'trips_suite',\n    }],\n)\nresult = checkpoint.run()\n\n# Option 2: dbt-expectations (intégré dbt)\n# packages.yml\n# packages:\n#   - package: calogica/dbt_expectations\n#     version: 0.10.1\n# dbt deps\n\n# models/_models.yml\n# version: 2\n# models:\n#   - name: fct_trips\n#     tests:\n#       - dbt_expectations.expect_table_row_count_to_be_between:\n#           min_value: 1000\n#       - dbt_expectations.expect_table_columns_to_match_set:\n#           column_set: [trip_id, vehicle_id, started_at]\n#     columns:\n#       - name: speed_kmh\n#         tests:\n#           - dbt_expectations.expect_column_values_to_be_between:\n#               min_value: 0\n#               max_value: 300\n#           - dbt_expectations.expect_column_quantile_values_to_be_between:\n#               quantile: 0.95\n#               min_value: 50\n#               max_value: 120\n\n# Option 3: Soda Core (YAML-first, alternatif)\n# soda_check.yml\n# checks for trips:\n#   - row_count > 1000\n#   - missing_count(trip_id) = 0\n#   - duplicate_count(trip_id) = 0\n#   - failed rows: speed_kmh out of range\n#       fail condition: speed_kmh < 0 OR speed_kmh > 300\n# \n# soda scan -d datasource soda_check.yml\n\n# 9. CI Integration (GitHub Actions)\n# - name: Run data quality\n#   run: |\n#     dbt build --select fct_trips\n#     # ou:\n#     soda scan -d prod soda_checks.yml\n# - name: Block deploy if QA failed\n#   if: failure()\n#   run: exit 1\n\n# 10. Pattern: 4 niveaux de checks\n# Level 1 (CI): schema (column exists, type)\n# Level 2 (Pre-deploy): not_null, unique, ranges\n# Level 3 (Post-load): row count, freshness\n# Level 4 (Daily): anomaly detection (drift, outliers)",
      },
      {
        id: "d82",
        label: "Data contracts (Pydantic + YAML)",
        day: "Mar",
        description: "Data contract = accord formel entre producteur/consommateur sur le schéma. Évite les casses silencieuses quand un producer change le schema. Pydantic en code, YAML pour docs/registry. Concept émergent crucial 2025.",
        url: "https://www.gable.ai/blog/data-contracts",
        resource: "Data Contracts Explained",
        resources: [
          { type: "article", title: "Data Contracts (Gable)", url: "https://www.gable.ai/blog/data-contracts" },
          { type: "doc", title: "Pydantic v2", url: "https://docs.pydantic.dev/latest/" },
          { type: "github", title: "datacontract.com (open spec)", url: "https://github.com/datacontract/datacontract-specification" },
          { type: "tool", title: "Soda Data Contracts", url: "https://docs.soda.io/soda/data-contracts.html" },
        ],
        exercise: "# Objectif: data contracts robustes\n\n# 1. Pydantic v2 contract\nfrom pydantic import BaseModel, Field, EmailStr, field_validator\nfrom typing import Literal\nfrom datetime import datetime\nfrom decimal import Decimal\n\nclass TripContract(BaseModel):\n    \"\"\"Trip event contract v1.0.\n    Producer: gps-collector-service\n    Consumers: data-platform, analytics\n    \"\"\"\n    \n    # Schema strict\n    trip_id: str = Field(min_length=1, max_length=50, pattern=r'^[A-Z0-9-]+$')\n    vehicle_id: str = Field(min_length=1, max_length=50)\n    started_at: datetime\n    ended_at: datetime\n    distance_km: Decimal = Field(ge=0, le=10000, decimal_places=2)\n    duration_seconds: int = Field(ge=0)\n    status: Literal['completed','cancelled','ongoing']\n    driver_email: EmailStr | None = None\n    \n    # Validations métier\n    @field_validator('ended_at')\n    @classmethod\n    def validate_dates(cls, v, info):\n        if 'started_at' in info.data and v < info.data['started_at']:\n            raise ValueError('ended_at < started_at')\n        return v\n    \n    @field_validator('duration_seconds')\n    @classmethod\n    def reasonable_duration(cls, v):\n        if v > 86400 * 7:   # > 1 semaine\n            raise ValueError('Trip too long, suspicious')\n        return v\n    \n    model_config = {\n        'extra': 'forbid',   # rejette champs inconnus\n        'frozen': True,       # immuable\n    }\n\n# Validation au flow\ntry:\n    trip = TripContract(**raw_data)\nexcept ValidationError as e:\n    log.error('Contract violation', extra={\n        'errors': e.errors(),\n        'raw': raw_data\n    })\n    send_to_dlq(raw_data, str(e))\n\n# 2. YAML Data Contract spec (open spec)\n# datacontract.yaml\n# dataContractSpecification: 0.9.3\n# id: gps-trips\n# info:\n#   title: GPS Trips Data\n#   version: 1.0.0\n#   owner: data-team\n#   description: Trip events from fleet\n# \n# servers:\n#   production:\n#     type: s3\n#     location: s3://prod/trips/\n#     format: parquet\n# \n# models:\n#   trips:\n#     type: table\n#     fields:\n#       trip_id:\n#         type: string\n#         required: true\n#         unique: true\n#         pattern: '^[A-Z0-9-]+$'\n#       vehicle_id:\n#         type: string\n#         required: true\n#       started_at:\n#         type: timestamp\n#         required: true\n#       distance_km:\n#         type: decimal\n#         minimum: 0\n#         maximum: 10000\n# \n# quality:\n#   - type: SodaCL\n#     specification:\n#       checks for trips:\n#         - row_count > 1000\n#         - duplicate_count(trip_id) = 0\n# \n# terms:\n#   usage: |\n#     Updated daily at 4 AM UTC.\n#     SLA: 99% availability\n#   limitations: PII data anonymized after 90d\n\n# 3. Génération auto code depuis YAML\n# datacontract validate --datacontract datacontract.yaml\n# datacontract generate code --target python > models.py\n\n# 4. Tests d'intégration de contrat\n# tests/test_contract.py\nimport pytest\nfrom my_app.contracts import TripContract\n\ndef test_valid_trip():\n    trip = TripContract(\n        trip_id='ABC-123',\n        vehicle_id='V-001',\n        started_at='2026-05-07T10:00:00',\n        ended_at='2026-05-07T10:30:00',\n        distance_km='5.5',\n        duration_seconds=1800,\n        status='completed'\n    )\n    assert trip.distance_km == Decimal('5.50')\n\ndef test_invalid_negative_distance():\n    with pytest.raises(ValidationError):\n        TripContract(distance_km=-1, ...)\n\n# 5. Schema evolution rules\n# Backward compatible (consumers continuent à marcher):\n# ✓ Ajouter champ optionnel (avec default)\n# ✓ Élargir contraintes (max_length 50 → 100)\n# ✗ Supprimer champ\n# ✗ Renommer champ (utiliser alias)\n# ✗ Resserrer contraintes\n# ✗ Changer type\n\n# 6. Versioning\n# trips_v1: schéma initial\n# trips_v2: ajoute 'fuel_type' (compatible)\n# trips_v3: BREAKING change: renomme 'distance_km' → 'distance'\n#   → produire les 2 versions en parallèle 6 mois\n#   → migrer consumers\n#   → deprecate v2\n\n# 7. Schema Registry (centralisé)\n# Pour Kafka: Confluent Schema Registry (Avro/JSON Schema/Protobuf)\n# Self-hosted: Apicurio\n\n# 8. Ingest with contract enforcement\nimport polars as pl\n\ndef ingest_trips(raw_path: str):\n    df = pl.read_parquet(raw_path)\n    \n    valid_records = []\n    invalid_records = []\n    \n    for row in df.iter_rows(named=True):\n        try:\n            trip = TripContract(**row)\n            valid_records.append(trip.model_dump())\n        except ValidationError as e:\n            invalid_records.append({'row': row, 'errors': e.errors()})\n    \n    # Write valid\n    pl.DataFrame(valid_records).write_parquet('valid.parquet')\n    \n    # DLQ pour invalid\n    pl.DataFrame(invalid_records).write_parquet('dlq.parquet')\n    \n    log.info(f'Valid: {len(valid_records)}, Invalid: {len(invalid_records)}')\n    \n    if len(invalid_records) / len(df) > 0.05:   # > 5% invalid\n        raise QualityException('Too many bad records')",
      },
      {
        id: "d83",
        label: "Monitoring & observability",
        day: "Mer",
        description: "Observabilité = freshness (data récente?), volume (trop/peu?), distribution (changement subtil?), schema (cassé?). Sans monitoring, les bugs data passent silencieux jusqu'au CEO. Outils : OpenLineage, Datadog, Monte Carlo.",
        url: "https://www.databand.ai/blog/the-five-pillars-of-data-observability/",
        resource: "5 Pillars of Data Observability",
        resources: [
          { type: "article", title: "5 Pillars of Data Observability", url: "https://www.databand.ai/blog/the-five-pillars-of-data-observability/" },
          { type: "tool", title: "OpenLineage (open standard)", url: "https://openlineage.io/" },
          { type: "tool", title: "Elementary (dbt-native)", url: "https://www.elementary-data.com/" },
          { type: "video", title: "Data Observability Explained", url: "https://youtu.be/Xs8LFj1CknI" },
        ],
        exercise: "# Objectif: pipeline observable\n\n# 5 piliers à monitorer:\n# 1. FRESHNESS: data à jour ?\n# 2. VOLUME: nombre de records cohérent ?\n# 3. DISTRIBUTION: stats colonnes habituelles ?\n# 4. SCHEMA: structure inchangée ?\n# 5. LINEAGE: dépendances mappées ?\n\n# 1. Freshness check\n# SQL Sensor pour Airflow\nfrom airflow.providers.postgres.sensors.postgres import SqlSensor\n\nfreshness_check = SqlSensor(\n    task_id='check_freshness',\n    conn_id='postgres',\n    sql='''\n        SELECT 1 FROM trips\n        WHERE created_at > NOW() - INTERVAL '2 hours'\n        LIMIT 1\n    ''',\n    poke_interval=300,\n    timeout=3600,\n)\n\n# Ou en Python avec alerte:\ndef check_freshness():\n    last_trip = db.execute('SELECT MAX(created_at) FROM trips').scalar()\n    age = datetime.now(tz=last_trip.tzinfo) - last_trip\n    if age > timedelta(hours=2):\n        send_alert(f'Trips stale by {age}')\n\n# 2. Volume anomaly detection\ndef check_volume():\n    today = db.execute(\n        \"SELECT COUNT(*) FROM trips WHERE date = CURRENT_DATE\"\n    ).scalar()\n    \n    historical = db.execute('''\n        SELECT AVG(daily_count) AS mu, STDDEV(daily_count) AS sigma\n        FROM (\n            SELECT date, COUNT(*) AS daily_count\n            FROM trips\n            WHERE date >= CURRENT_DATE - INTERVAL '30 days'\n            GROUP BY date\n        ) sub\n    ''').first()\n    \n    z_score = (today - historical.mu) / historical.sigma\n    if abs(z_score) > 3:\n        send_alert(f'Volume anomaly: {today} (Z={z_score:.2f})')\n\n# 3. Distribution drift (column stats)\nimport polars as pl\nfrom scipy.stats import ks_2samp\n\ndef check_drift():\n    today = pl.read_parquet('s3://trips/today.parquet')\n    last_week = pl.read_parquet('s3://trips/last_week.parquet')\n    \n    # Kolmogorov-Smirnov test\n    stat, pvalue = ks_2samp(today['speed'], last_week['speed'])\n    \n    if pvalue < 0.001:   # distribution significativement différente\n        send_alert(f'Speed distribution drifted: KS={stat:.3f}')\n\n# 4. Schema validation\nimport pyarrow as pa\nimport pyarrow.parquet as pq\n\nEXPECTED_SCHEMA = pa.schema([\n    ('trip_id', pa.string()),\n    ('vehicle_id', pa.string()),\n    ('started_at', pa.timestamp('us')),\n    ('distance_km', pa.float64()),\n])\n\ndef check_schema(path: str):\n    actual = pq.read_schema(path)\n    \n    # Champs manquants ?\n    expected_fields = {f.name for f in EXPECTED_SCHEMA}\n    actual_fields = {f.name for f in actual}\n    if expected_fields - actual_fields:\n        raise SchemaError(f'Missing: {expected_fields - actual_fields}')\n    \n    # Nouveaux champs (warning)\n    new = actual_fields - expected_fields\n    if new:\n        log.warning(f'New columns: {new}')\n\n# 5. Lineage (OpenLineage)\n# pip install openlineage-airflow\n# Auto-instrumente Airflow\n# airflow.cfg:\n# [openlineage]\n# transport = '{\"type\":\"http\",\"url\":\"http://marquez:5000\"}'\n\n# Marquez (UI lineage gratuit)\n# docker compose up marquez\n\n# 6. Elementary (dbt-native, recommandé si tu fais dbt)\n# pip install elementary-data\n# packages.yml:\n# - package: elementary-data/elementary\n#   version: 0.13.0\n# dbt deps\n# edr report   ← génère HTML report avec freshness/anomalies\n\n# 7. Custom monitoring DAG\n@dag(schedule='*/15 * * * *', tags=['monitoring'])\ndef data_health_dag():\n    \n    @task\n    def check_freshness(): ...\n    @task\n    def check_volume(): ...\n    @task\n    def check_drift(): ...\n    @task\n    def check_schema(): ...\n    \n    @task(trigger_rule='one_failed')\n    def alert_team():\n        # Slack alert avec détails\n        ...\n    \n    [check_freshness(), check_volume(),\n     check_drift(), check_schema()] >> alert_team()\n\n# 8. Métriques Prometheus\nfrom prometheus_client import Counter, Gauge, push_to_gateway\n\nrecords_processed = Counter('records_processed', 'Total records', ['pipeline'])\npipeline_duration = Gauge('pipeline_duration_seconds', 'Duration', ['pipeline'])\n\n# Dans pipeline:\nrecords_processed.labels(pipeline='gps').inc(len(df))\npipeline_duration.labels(pipeline='gps').set(elapsed)\n\npush_to_gateway('pushgateway:9091', job='gps_pipeline')\n\n# Grafana dashboard: graphique latence + débit + erreurs\n\n# 9. Outils SaaS production-ready\n# - Monte Carlo (leader)\n# - Datadog Data Observability\n# - Bigeye\n# - Acceldata\n# - Soda Cloud\n\n# Pour démarrer petit:\n# - Elementary (dbt)\n# - Soda Core (Python)\n# - Custom Airflow DAGs",
      },
      {
        id: "d84",
        label: "Blog & portfolio",
        day: "Sam",
        description: "Tu as appris dbt + Airflow + Quality. Bloguer un projet réel = consolider + visibilité. Les recruteurs Google 'votre nom' avant l'entretien : 1 article tech bien placé = différencier vs 1000 CVs identiques.",
        url: "https://medium.com/",
        resource: "Medium / Dev.to",
        resources: [
          { type: "tool", title: "Dev.to (community-friendly)", url: "https://dev.to/" },
          { type: "tool", title: "Medium (large reach)", url: "https://medium.com/" },
          { type: "tool", title: "Substack (newsletter)", url: "https://substack.com/" },
          { type: "github", title: "Awesome Data Engineering Articles", url: "https://github.com/igorbarinov/awesome-data-engineering" },
        ],
        exercise: "# Objectif: 2e article + portfolio complet\n\n# Idées articles concrets (basés sur ce que tu viens d'apprendre):\n\n# 1. 'Comment j'ai industrialisé mon ETL avec Airflow + dbt'\n#    Cible: alternants/juniors\n#    Angle: parcours perso (de scripts cron à pipeline pro)\n#    Contenu:\n#    - Avant: scripts Python lancés via cron\n#    - Problèmes rencontrés (no monitoring, débug enfer)\n#    - La solution: Airflow + dbt\n#    - Code snippets clé (DAG simplifié, dbt project)\n#    - Résultats: alertes Slack, lineage visible, tests automatisés\n#    - Ce que j'aurais aimé savoir avant\n\n# 2. 'Data Contracts: pourquoi votre prochain incident vient d'un schema change'\n#    Cible: data engineers / managers\n#    Angle: post-mortem\n#    Contenu:\n#    - Histoire (vraie ou fictive) d'un schema change qui casse tout\n#    - Concept de data contract\n#    - Implémentation Pydantic + YAML\n#    - Adoption progressive\n\n# 3. '5 patterns Airflow que j'aurais aimé connaître plus tôt'\n#    Cible: data engineers juniors\n#    Format: liste actionable\n#    - TaskFlow API > Operators legacy\n#    - Sensors mode='reschedule' (libère slots)\n#    - DataAware Scheduling\n#    - Custom XCom backend pour gros DataFrames\n#    - Cosmos pour intégrer dbt\n\n# Structure article gagnante (1500 mots):\n# Hook (3 phrases): le problème en 30s\n# Contexte (200 mots)\n# Solution (1000 mots avec 5+ snippets)\n# Résultats (200 mots avec graphique avant/après)\n# Conclusion + CTA (100 mots)\n\n# Tips pour visibilité:\n# 1. Titre = SEO (commence par chiffre/question)\n#    'Comment j'ai...' / '5 pattern pour...' / 'Airflow vs...'\n# 2. Image cover (canva, screenshot, ou architecture diagram)\n# 3. Cross-post: dev.to + Medium + LinkedIn (avec tag canonical URL)\n# 4. Partage: Slack data fr, Discord, Reddit r/dataengineering\n# 5. Réponds aux commentaires < 24h\n\n# Portfolio actuel (à ce stade):\n# 1. ETL Python OOP + tests (Phase 1)\n# 2. EDA GPS Kaggle (Pandas)\n# 3. Pipeline Polars 10x faster (benchmark)\n# 4. SQL portfolio (10 queries avancées)\n# 5. Carte open data Folium\n# 6. Dashboard Streamlit déployé\n# 7. Pipeline ETL dockerisé (Compose)\n# 8. dbt project + lineage docs\n# 9. Airflow DAG production GPS\n# = 9 projets concrets\n\n# Site portfolio (en plus du GitHub)\n# Option A: Astro + GitHub Pages (gratuit)\n# npm create astro@latest my-portfolio\n# Sections:\n# - About (1 paragraphe + photo)\n# - Projects (3-4 highlights avec liens GitHub + demos)\n# - Articles (avec previews)\n# - Contact + LinkedIn + GitHub\n\n# Option B: linktr.ee / about.me (5 min, simple)\n\n# Option C: domaine perso (10€/an)\n# nassim-data.dev\n# = pro look avec branding\n\n# CV technique mise à jour:\n# Section 'Projets':\n# - Dashboard analytics mobilité (Streamlit, Folium, déployé)\n#   github.com/x/y | demo: https://...\n#   Stack: Python, Polars, dbt, Airflow\n# \n# Section 'Compétences':\n# - Python (avancé): Polars, Pandas, FastAPI, Streamlit\n# - SQL (avancé): PostgreSQL, BigQuery, Window Functions, CTEs\n# - Data Engineering: dbt, Airflow, Docker, Spark\n# - Cloud: AWS S3/Athena, Vercel\n# - Géospatial: GeoPandas, PostGIS, Folium\n\n# LinkedIn optimisation:\n# - Photo pro\n# - Headline: 'Data Engineer | Polars · dbt · Airflow | Open to work'\n# - About: 3 paragraphes (qui tu es, ce que tu fais, ce que tu cherches)\n# - Featured: 3 articles + 3 projets GitHub\n# - Experience détaillée avec stack technique\n# - Open to work activé (visible recruteurs)\n\n# Stratégie networking:\n# - Connecte-toi avec 5 data engineers/sem\n# - Engage sur 3 posts/jour (commentaires substantiels)\n# - Poste 1x/sem (ton article ou apprentissage)\n# - Rejoins meetups data Paris (Data Engineer Paris, France's Data Squad)\n\n# Time investment:\n# - 1 article/mois (4-6h)\n# - LinkedIn 15 min/jour\n# - 1 meetup/mois\n# = ~10h/mois pour ROI massif (recruteurs te trouvent)",
      },
    ],
  },
  {
    id: "w_postgres",
    title: "PostgreSQL Avancé",
    tasks: [
      {
        id: "d140",
        label: "Index strategies (B-tree, GIN, GiST, BRIN)",
        day: "Lun",
        description: "Index = secret des bases performantes. B-tree (général), GIN (full-text, JSON), GiST (spatial, ranges), BRIN (très grosses tables append-only). Mauvais index = queries lentes, trop d'index = writes lents.",
        url: "https://www.postgresql.org/docs/current/indexes-types.html",
        resource: "PostgreSQL - Index Types",
        resources: [
          { type: "doc", title: "PostgreSQL Index Types", url: "https://www.postgresql.org/docs/current/indexes-types.html" },
          { type: "article", title: "Use The Index, Luke!", url: "https://use-the-index-luke.com/" },
          { type: "article", title: "PostgreSQL Indexes Visualized", url: "https://www.postgresql.org/docs/current/indexes-multicolumn.html" },
          { type: "tool", title: "pgmustard (analyse plans)", url: "https://www.pgmustard.com/" },
        ],
        exercise: "-- Objectif: indexer intelligemment\n\n-- Setup\nCREATE TABLE trips (\n    id BIGSERIAL PRIMARY KEY,\n    vehicle_id INT NOT NULL,\n    city VARCHAR(50),\n    started_at TIMESTAMP,\n    distance_km NUMERIC,\n    metadata JSONB,\n    location GEOMETRY(Point, 4326)\n);\n-- Insert 10M rows (simuler)\n\n-- 1. B-TREE (default - 90% des cas)\nCREATE INDEX idx_trips_vehicle ON trips(vehicle_id);\nCREATE INDEX idx_trips_started ON trips(started_at);\n\n-- Multi-column (ordre important !)\nCREATE INDEX idx_trips_vehicle_started ON trips(vehicle_id, started_at);\n-- Couvre: WHERE vehicle_id=X\n--         WHERE vehicle_id=X AND started_at > '2026-01-01'\n-- Ne couvre PAS: WHERE started_at > '2026-01-01' SEUL\n\n-- Partial index (filtrer le plus utilisé)\nCREATE INDEX idx_trips_active\n  ON trips(vehicle_id, started_at)\n  WHERE status = 'active';\n-- Plus petit, plus rapide pour ce cas\n\n-- 2. GIN (full-text, arrays, JSONB)\nCREATE INDEX idx_metadata_gin ON trips USING GIN(metadata);\n-- Permet: WHERE metadata @> '{\"engine\": \"diesel\"}'\n\n-- Full-text search\nCREATE INDEX idx_trips_search ON trips USING GIN(to_tsvector('french', notes));\nSELECT * FROM trips\n  WHERE to_tsvector('french', notes) @@ plainto_tsquery('panne moteur');\n\n-- 3. GiST (spatial, ranges)\nCREATE INDEX idx_trips_location_gist ON trips USING GIST(location);\n-- Rapide pour: ST_DWithin, ST_Contains, etc.\n\nCREATE INDEX idx_trips_period ON trips USING GIST(\n    tstzrange(started_at, ended_at)\n);\n-- Recherche dans une fenêtre temporelle\n\n-- 4. BRIN (Block Range INdex - très grosses tables)\nCREATE INDEX idx_trips_started_brin ON trips USING BRIN(started_at);\n-- 1000x plus petit que B-tree\n-- Idéal pour append-only avec ordre naturel (logs, timeseries)\n\n-- 5. Hash (rare, pour égalité stricte)\nCREATE INDEX idx_trips_uuid_hash ON trips USING HASH(uuid);\n\n-- 6. Covering index (Postgres 11+)\nCREATE INDEX idx_trips_vehicle_covering\n  ON trips(vehicle_id) INCLUDE (started_at, distance_km);\n-- Queries qui SELECT que ces colonnes lisent QUE l'index (pas la table)\n-- = ULTRA rapide\n\n-- 7. Voir si tes indexes sont utilisés\nSELECT\n    schemaname, relname AS table, indexrelname AS index,\n    idx_scan AS scans,\n    pg_size_pretty(pg_relation_size(indexrelid)) AS size\nFROM pg_stat_user_indexes\nORDER BY idx_scan ASC;\n-- Index avec 0 scans = candidat à supprimer\n\n-- 8. EXPLAIN ANALYZE (le pouvoir)\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT * FROM trips\nWHERE vehicle_id = 42 AND started_at > '2026-05-01';\n\n-- Cherche dans le plan:\n-- - Seq Scan = bug (pas d'index utilisé)\n-- - Index Scan = OK\n-- - Index Only Scan = parfait (covering index)\n-- - Bitmap Heap Scan = OK pour gros résultats\n-- - Buffers: shared hit (bon) vs read (lent)\n\n-- 9. REINDEX (maintenance)\nREINDEX INDEX idx_trips_vehicle;\nREINDEX TABLE trips;\nVACUUM ANALYZE trips;   -- update stats du planner\n\n-- 10. Trade-offs:\n-- ✓ Indexes accélèrent SELECT (10-1000x)\n-- ✗ Indexes ralentissent INSERT/UPDATE (writes en plus)\n-- ✗ Indexes prennent espace disque\n-- \n-- Règle: index toutes les FK + colonnes filtrées/triées souvent\n-- Pas plus de 5-7 indexes par table en général\n\n-- 11. Bonus: indexes sur expressions\nCREATE INDEX idx_trips_lower_email ON trips(LOWER(driver_email));\n-- Couvre WHERE LOWER(driver_email) = 'abc@def.com'\n\nCREATE INDEX idx_trips_year_month\n  ON trips(EXTRACT(YEAR FROM started_at), EXTRACT(MONTH FROM started_at));",
      },
      {
        id: "d141",
        label: "EXPLAIN ANALYZE & query optimization",
        day: "Mar",
        description: "EXPLAIN ANALYZE = lire dans la tête du planner Postgres. Comprendre les stages (Seq Scan, Hash Join, Sort) permet d'optimiser les queries lentes. Skill différenciant DE junior vs senior.",
        url: "https://www.postgresql.org/docs/current/using-explain.html",
        resource: "PostgreSQL - EXPLAIN",
        resources: [
          { type: "doc", title: "PostgreSQL EXPLAIN", url: "https://www.postgresql.org/docs/current/using-explain.html" },
          { type: "tool", title: "pgmustard (visualizer)", url: "https://www.pgmustard.com/" },
          { type: "tool", title: "explain.depesz.com (free analyzer)", url: "https://explain.depesz.com/" },
          { type: "article", title: "Use The Index, Luke - SQL Performance", url: "https://use-the-index-luke.com/" },
        ],
        exercise: "-- Objectif: identifier et fixer queries lentes\n\n-- 1. EXPLAIN basique\nEXPLAIN SELECT * FROM trips WHERE vehicle_id = 42;\n-- Plan estimé sans exécuter\n\n-- 2. EXPLAIN ANALYZE (exécute + mesure)\nEXPLAIN ANALYZE SELECT * FROM trips WHERE vehicle_id = 42;\n-- Voir actual time + rows\n\n-- 3. EXPLAIN avec tout\nEXPLAIN (\n    ANALYZE,         -- exec et mesure\n    BUFFERS,         -- I/O\n    VERBOSE,         -- détails\n    FORMAT TEXT      -- ou JSON, YAML\n)\nSELECT * FROM trips t\nJOIN vehicles v ON v.id = t.vehicle_id\nWHERE v.fleet = 'taxi' AND t.started_at > '2026-01-01';\n\n-- 4. Lire un plan\n-- Plan affiché de bas en haut, chaque étape = node:\n-- \n-- HashAggregate  (cost=143.5..143.7 rows=42)\n--   ->  Hash Join  (cost=10.5..142.4)\n--         Hash Cond: (t.vehicle_id = v.id)\n--         ->  Seq Scan on trips t  (cost=0..98.3)\n--         ->  Hash  (cost=8.5..8.5 rows=80)\n--               ->  Index Scan on vehicles v\n\n-- Indicateurs ROUGES (à fixer):\n-- - 'Seq Scan' sur grosse table avec filtre\n-- - 'rows=1000000' sur opération coûteuse\n-- - 'Sort' avec disk (memory full)\n-- - actual time bcp > planned (stats périmées)\n\n-- 5. Indicateurs VERTS:\n-- - 'Index Scan' / 'Index Only Scan'\n-- - 'Bitmap Heap Scan' pour grosses sélections\n-- - 'Nested Loop' avec petites tables\n-- - 'Hash Join' avec medium tables\n\n-- 6. Anti-patterns à éviter\n\n-- ❌ Function autour de colonne indexée (kill index)\nSELECT * FROM trips WHERE LOWER(driver_email) = 'a@b.com';\n-- ✓ Solution 1: index sur LOWER\nCREATE INDEX idx_lower_email ON trips(LOWER(driver_email));\n-- ✓ Solution 2: store lowercase\n\n-- ❌ Cast implicite\nSELECT * FROM trips WHERE id = '42';   -- string vs int\n-- ✓\nSELECT * FROM trips WHERE id = 42;\n\n-- ❌ LIKE avec wildcard prefix\nSELECT * FROM trips WHERE city LIKE '%aris';\n-- Index inutile\n-- ✓ Postgres: index trigram pour LIKE prefix-trigram\n-- CREATE EXTENSION pg_trgm;\n-- CREATE INDEX idx_city_trgm ON trips USING GIN (city gin_trgm_ops);\n\n-- ❌ NOT IN sur grosse liste\nSELECT * FROM trips WHERE city NOT IN (SELECT city FROM excluded);\n-- ✓ Plus efficace:\nSELECT * FROM trips t\nWHERE NOT EXISTS (SELECT 1 FROM excluded e WHERE e.city = t.city);\n\n-- ❌ Sous-requête correlée\nSELECT t.*, (SELECT COUNT(*) FROM trips t2 WHERE t2.vehicle_id = t.vehicle_id) AS n\nFROM trips t;\n-- = 1 query par row !!\n-- ✓ Window function\nSELECT t.*, COUNT(*) OVER (PARTITION BY vehicle_id) AS n FROM trips t;\n\n-- 7. Optimisations courantes\n\n-- A. Index manquant\nEXPLAIN ANALYZE SELECT * FROM trips WHERE vehicle_id = 42;\n-- Seq Scan sur 10M rows = 5s\n-- + Index → 5ms (1000x faster)\nCREATE INDEX idx_vehicle ON trips(vehicle_id);\n\n-- B. JOIN order\nEXPLAIN ANALYZE\nSELECT * FROM big_table b JOIN small_table s ON b.id = s.id;\n-- Postgres réordonne souvent bien\n-- Mais avec subqueries: parfois besoin de WITH MATERIALIZED\n\n-- C. Limit + ORDER BY sans index\nSELECT * FROM trips ORDER BY started_at DESC LIMIT 100;\n-- Sans index: SORT 10M rows then LIMIT (lent)\n-- ✓\nCREATE INDEX idx_started_desc ON trips(started_at DESC);\n-- LIMIT 100 lit que 100 entrées\n\n-- D. Stats périmées (planner se trompe)\nVACUUM ANALYZE trips;\n-- Force update statistics\n\n-- E. Partition table (>100M rows)\nCREATE TABLE trips_partitioned (...) PARTITION BY RANGE (started_at);\nCREATE TABLE trips_2026_05 PARTITION OF trips_partitioned\n    FOR VALUES FROM ('2026-05-01') TO ('2026-06-01');\n-- Query sur 1 mois = scan QUE 1 partition\n\n-- 8. pg_stat_statements (queries les plus lentes)\nCREATE EXTENSION pg_stat_statements;\n\n-- Postgresql.conf:\n-- shared_preload_libraries = 'pg_stat_statements'\n\nSELECT\n    query, calls, total_time, mean_time,\n    rows, 100.0 * shared_blks_hit /\n        nullif(shared_blks_hit + shared_blks_read, 0) AS hit_pct\nFROM pg_stat_statements\nORDER BY mean_time DESC\nLIMIT 20;\n-- Top 20 queries les plus chères = candidats optimisation\n\n-- 9. auto_explain (log slow queries)\n-- postgresql.conf:\n-- session_preload_libraries = 'auto_explain'\n-- auto_explain.log_min_duration = '1s'\n-- auto_explain.log_analyze = on\n-- → toute query > 1s loggée avec son plan\n\n-- 10. Pattern: index review trimestriel\n-- 1. Identifier slow queries (pg_stat_statements)\n-- 2. EXPLAIN ANALYZE chaque\n-- 3. Ajouter / réviser indexes\n-- 4. Mesurer gain\n-- 5. Drop indexes inutiles (pg_stat_user_indexes)",
      },
      {
        id: "d142",
        label: "Stored procedures, triggers",
        day: "Mer",
        description: "Stored procedures = logique en SQL/PLpgSQL exécutée côté DB. Triggers = code automatique sur INSERT/UPDATE/DELETE. À utiliser avec parcimonie : peut cacher logique métier, mais utile pour audit/dénormalisation.",
        url: "https://www.postgresql.org/docs/current/plpgsql.html",
        resource: "PostgreSQL - PL/pgSQL",
        resources: [
          { type: "doc", title: "PL/pgSQL Documentation", url: "https://www.postgresql.org/docs/current/plpgsql.html" },
          { type: "doc", title: "Triggers PostgreSQL", url: "https://www.postgresql.org/docs/current/triggers.html" },
          { type: "article", title: "When to use Triggers", url: "https://www.postgresql.org/docs/current/sql-createtrigger.html" },
        ],
        exercise: "-- Objectif: maîtriser logique côté DB\n\n-- 1. Function basique\nCREATE OR REPLACE FUNCTION calc_distance(\n    lat1 FLOAT, lon1 FLOAT, lat2 FLOAT, lon2 FLOAT\n) RETURNS FLOAT AS $$\nDECLARE\n    R FLOAT := 6371;   -- Earth radius km\n    a FLOAT;\nBEGIN\n    a := SIN(RADIANS((lat2-lat1)/2))^2 +\n         COS(RADIANS(lat1)) * COS(RADIANS(lat2)) *\n         SIN(RADIANS((lon2-lon1)/2))^2;\n    RETURN 2 * R * ASIN(SQRT(a));\nEND;\n$$ LANGUAGE plpgsql IMMUTABLE;\n\n-- Utiliser comme une fonction native\nSELECT calc_distance(48.85, 2.35, 45.76, 4.84);\n\n-- 2. Stored procedure (Postgres 11+)\nCREATE PROCEDURE refresh_daily_kpis(target_date DATE)\nLANGUAGE plpgsql AS $$\nBEGIN\n    DELETE FROM daily_kpis WHERE date = target_date;\n    INSERT INTO daily_kpis (date, n_trips, total_km, avg_speed)\n    SELECT\n        target_date,\n        COUNT(*),\n        SUM(distance_km),\n        AVG(speed_kmh)\n    FROM trips\n    WHERE DATE(started_at) = target_date;\n    \n    COMMIT;\nEND;\n$$;\n\nCALL refresh_daily_kpis('2026-05-07');\n\n-- 3. Trigger: audit log auto\nCREATE TABLE audit_log (\n    id BIGSERIAL PRIMARY KEY,\n    table_name VARCHAR(50),\n    operation VARCHAR(10),   -- INSERT/UPDATE/DELETE\n    user_id INT,\n    timestamp TIMESTAMP DEFAULT NOW(),\n    old_data JSONB,\n    new_data JSONB\n);\n\nCREATE OR REPLACE FUNCTION audit_trigger_fn()\nRETURNS TRIGGER AS $$\nBEGIN\n    INSERT INTO audit_log (table_name, operation, user_id, old_data, new_data)\n    VALUES (\n        TG_TABLE_NAME,\n        TG_OP,\n        current_setting('app.current_user_id', true)::INT,\n        CASE WHEN TG_OP = 'DELETE' THEN row_to_json(OLD) ELSE NULL END,\n        CASE WHEN TG_OP IN ('INSERT','UPDATE') THEN row_to_json(NEW) ELSE NULL END\n    );\n    RETURN COALESCE(NEW, OLD);\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER audit_trips\nAFTER INSERT OR UPDATE OR DELETE ON trips\nFOR EACH ROW EXECUTE FUNCTION audit_trigger_fn();\n\n-- 4. Trigger: maintenir colonne dénormalisée\nCREATE OR REPLACE FUNCTION update_vehicle_stats()\nRETURNS TRIGGER AS $$\nBEGIN\n    UPDATE vehicles\n    SET\n        total_trips = total_trips + 1,\n        total_distance_km = total_distance_km + NEW.distance_km,\n        last_trip_at = NEW.started_at\n    WHERE id = NEW.vehicle_id;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_update_vehicle_stats\nAFTER INSERT ON trips\nFOR EACH ROW EXECUTE FUNCTION update_vehicle_stats();\n\n-- 5. Trigger: validation métier\nCREATE OR REPLACE FUNCTION validate_trip()\nRETURNS TRIGGER AS $$\nBEGIN\n    IF NEW.distance_km < 0 THEN\n        RAISE EXCEPTION 'Distance cannot be negative';\n    END IF;\n    IF NEW.ended_at < NEW.started_at THEN\n        RAISE EXCEPTION 'ended_at < started_at';\n    END IF;\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_validate_trip\nBEFORE INSERT OR UPDATE ON trips\nFOR EACH ROW EXECUTE FUNCTION validate_trip();\n\n-- 6. Trigger: updated_at automatique\nCREATE OR REPLACE FUNCTION set_updated_at()\nRETURNS TRIGGER AS $$\nBEGIN\n    NEW.updated_at = NOW();\n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_set_updated_at\nBEFORE UPDATE ON trips\nFOR EACH ROW EXECUTE FUNCTION set_updated_at();\n\n-- 7. Aggregate function custom\nCREATE OR REPLACE FUNCTION median_state(numeric[], numeric)\nRETURNS numeric[] AS $$\n    SELECT $1 || $2;\n$$ LANGUAGE SQL IMMUTABLE;\n\nCREATE OR REPLACE FUNCTION median_final(numeric[])\nRETURNS numeric AS $$\n    SELECT percentile_cont(0.5) WITHIN GROUP (ORDER BY x)\n    FROM unnest($1) AS x;\n$$ LANGUAGE SQL IMMUTABLE;\n\nCREATE AGGREGATE median(numeric) (\n    SFUNC = median_state,\n    STYPE = numeric[],\n    FINALFUNC = median_final,\n    INITCOND = '{}'\n);\n\n-- Maintenant utilisable comme AVG, SUM:\nSELECT vehicle_id, median(distance_km) FROM trips GROUP BY vehicle_id;\n\n-- 8. Quand utiliser triggers ?\n-- ✓ Audit log (toujours)\n-- ✓ updated_at automatique\n-- ✓ Validation contrainte cross-row\n-- ✓ Cache columns dénormalisés (counters)\n-- ✗ Logique métier complexe (bug enfer à débugger)\n-- ✗ Calls à services externes (sync/async issues)\n\n-- 9. Désactiver/débugger triggers\nALTER TABLE trips DISABLE TRIGGER trg_audit;\nALTER TABLE trips ENABLE TRIGGER trg_audit;\n\n-- Logs:\nSET client_min_messages = NOTICE;\n-- Dans trigger:\n-- RAISE NOTICE 'Trigger fired with %', NEW;\n\n-- 10. Performance impact\n-- Trigger AFTER = OK\n-- Trigger BEFORE avec UPDATE complexe = peut multiplier write time\n-- Toujours benchmark avec/sans trigger\n\n-- 11. Alternative moderne: CDC (Change Data Capture)\n-- Au lieu de triggers DB, capture changes via WAL\n-- Outils: Debezium, AWS DMS, Fivetran\n-- Avantage: découplé de la DB, scalable",
      },
      {
        id: "d143",
        label: "Partitioning tables (>100M rows)",
        day: "Jeu",
        description: "Partitioning = découper une grosse table en plusieurs tables filles selon une clé (date, région). Queries sur 1 partition = scan partiel. Indispensable >100M rows pour garder perf et faciliter maintenance.",
        url: "https://www.postgresql.org/docs/current/ddl-partitioning.html",
        resource: "PostgreSQL - Table Partitioning",
        resources: [
          { type: "doc", title: "PostgreSQL Partitioning", url: "https://www.postgresql.org/docs/current/ddl-partitioning.html" },
          { type: "tool", title: "pg_partman (auto-partition)", url: "https://github.com/pgpartman/pg_partman" },
          { type: "article", title: "Postgres Partitioning Best Practices", url: "https://www.timescale.com/blog/postgresql-partitioning-best-practices/" },
        ],
        exercise: "-- Objectif: gérer 1B rows efficacement\n\n-- 1. Partitioning par RANGE (date)\nCREATE TABLE trips (\n    id BIGSERIAL,\n    vehicle_id INT,\n    started_at TIMESTAMP NOT NULL,\n    distance_km NUMERIC,\n    PRIMARY KEY (id, started_at)\n) PARTITION BY RANGE (started_at);\n\n-- Créer partitions par mois\nCREATE TABLE trips_2026_01 PARTITION OF trips\n    FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');\nCREATE TABLE trips_2026_02 PARTITION OF trips\n    FOR VALUES FROM ('2026-02-01') TO ('2026-03-01');\n-- ...\n\n-- 2. Indexes par partition\nCREATE INDEX idx_trips_2026_01_vehicle ON trips_2026_01(vehicle_id);\n-- Postgres applique auto à toutes nouvelles partitions ?\n-- Non: tu dois indexer chaque ou utiliser partition par défaut\n\n-- Index sur table parent (Postgres 11+)\nCREATE INDEX idx_trips_vehicle ON trips(vehicle_id);\n-- → propagé auto à toutes partitions\n\n-- 3. Query: planner ne scanne que partition pertinente\nEXPLAIN SELECT * FROM trips WHERE started_at = '2026-05-15';\n-- Plan:\n-- Seq Scan on trips_2026_05 (lit QUE cette partition)\n-- Pas les autres = 100x plus rapide\n\n-- Partition pruning (suppression auto si pas concernée)\nSET enable_partition_pruning = on;   -- défaut\n\n-- 4. Partitioning par LIST (catégories)\nCREATE TABLE trips_by_country (...)\n    PARTITION BY LIST (country);\n\nCREATE TABLE trips_fr PARTITION OF trips_by_country\n    FOR VALUES IN ('FR');\nCREATE TABLE trips_de PARTITION OF trips_by_country\n    FOR VALUES IN ('DE');\nCREATE TABLE trips_default PARTITION OF trips_by_country DEFAULT;\n\n-- 5. Partitioning par HASH (distribution équilibrée)\nCREATE TABLE trips_hashed (...)\n    PARTITION BY HASH (vehicle_id);\n\nCREATE TABLE trips_h0 PARTITION OF trips_hashed FOR VALUES WITH (modulus 4, remainder 0);\nCREATE TABLE trips_h1 PARTITION OF trips_hashed FOR VALUES WITH (modulus 4, remainder 1);\n-- ... 4 partitions, même size\n\n-- 6. Sub-partitioning (RANGE puis LIST)\nCREATE TABLE trips_2d (...)\n    PARTITION BY RANGE (started_at);\n\nCREATE TABLE trips_2026 PARTITION OF trips_2d\n    FOR VALUES FROM ('2026-01-01') TO ('2027-01-01')\n    PARTITION BY LIST (country);\n\nCREATE TABLE trips_2026_fr PARTITION OF trips_2026 FOR VALUES IN ('FR');\n\n-- 7. ATTACH/DETACH partition (online maintenance)\n-- Détacher partition ancienne (archivage)\nALTER TABLE trips DETACH PARTITION trips_2024_01;\n-- Backup vers S3, puis DROP\n\n-- Attacher partition externe\nCREATE TABLE trips_2026_06 (LIKE trips INCLUDING ALL);\n-- Fill with data\nALTER TABLE trips ATTACH PARTITION trips_2026_06\n    FOR VALUES FROM ('2026-06-01') TO ('2026-07-01');\n\n-- 8. pg_partman (auto-création partitions)\nCREATE EXTENSION pg_partman;\n\nSELECT partman.create_parent(\n    p_parent_table => 'public.trips',\n    p_control => 'started_at',\n    p_type => 'range',\n    p_interval => '1 month',\n    p_premake => 6   -- 6 mois d'avance\n);\n\n-- Cron: création + cleanup auto\nSELECT partman.run_maintenance();\n\n-- 9. Cas pratique: drop ancien data\n-- Avant partitioning:\nDELETE FROM trips WHERE started_at < '2024-01-01';\n-- = scan 100M rows, slow + bloat\n\n-- Avec partitioning:\nDROP TABLE trips_2023_01;\nDROP TABLE trips_2023_02;\n-- = instantané, pas de bloat\n\n-- 10. Limites partitioning\n-- ✓ Postgres 11+ pour partitioning natif moderne\n-- ✓ Max 1000 partitions par table (perf)\n-- ✗ FK vers table partitionnée: limitations\n-- ✗ UNIQUE constraint inclut clé partition\n\n-- 11. Patterns par cas d'usage:\n-- Time-series GPS: RANGE par mois (rotation 12 mois)\n-- Multi-tenant: LIST par tenant_id\n-- Big data analytics: HASH par hash_id\n-- Multi-régions: RANGE par région (data residency)\n\n-- 12. Alternative: TimescaleDB\n-- Extension pour timeseries, partitionne auto, optimisations\n-- pip install timescaledb\n-- CREATE EXTENSION timescaledb;\n-- SELECT create_hypertable('trips', 'started_at');\n-- → Auto-partitioning + compression + retention",
      },
      {
        id: "d144",
        label: "Replication & High Availability",
        day: "Ven",
        description: "Replication = copier data de master vers replicas (read scaling, backup, failover). Streaming WAL = standard Postgres. Indispensable en prod : single-point-of-failure DB = company down.",
        url: "https://www.postgresql.org/docs/current/high-availability.html",
        resource: "PostgreSQL - High Availability",
        resources: [
          { type: "doc", title: "PostgreSQL HA Guide", url: "https://www.postgresql.org/docs/current/high-availability.html" },
          { type: "tool", title: "Patroni (HA cluster)", url: "https://github.com/zalando/patroni" },
          { type: "tool", title: "PgBouncer (connection pool)", url: "https://www.pgbouncer.org/" },
          { type: "article", title: "Postgres Replication Patterns", url: "https://www.crunchydata.com/blog/replication-models-in-postgresql" },
        ],
        exercise: "-- Objectif: comprendre architectures HA\n\n-- 1. Concepts\n-- WAL: Write-Ahead Log (toutes les modifications)\n-- Master/Primary: lit + écrit\n-- Replica/Standby: read-only, copie WAL du master\n-- Failover: replica → master si master down\n\n-- 2. Streaming Replication (built-in)\n-- master postgresql.conf:\n-- wal_level = replica\n-- max_wal_senders = 10\n-- archive_mode = on\n\n-- master pg_hba.conf:\n-- host replication replicator 10.0.0.0/24 md5\n\n-- replica recovery.conf (legacy) ou postgresql.auto.conf:\n-- primary_conninfo = 'host=master.db port=5432 user=replicator'\n-- restore_command = 'cp /archive/%f %p'\n\n-- 3. Modes\n-- Asynchronous (default): commit master ne attend PAS replicas\n--   Avantage: latence faible\n--   Risque: perdre transactions si crash\n\n-- Synchronous: master attend ack 1+ replicas\n--   Avantage: durabilité\n--   Coût: latence x 2-3\n\n-- postgresql.conf:\n-- synchronous_commit = on   -- défaut\n-- synchronous_standby_names = '1 (replica1, replica2)'\n\n-- 4. Read replicas (scaling)\n-- Stack:\n-- - 1 master (writes)\n-- - 2-N replicas (reads)\n-- - Load balancer (HAProxy / PgBouncer)\n\n-- App layer:\n-- pool_master = create_engine('postgresql://master.db')\n-- pool_replica = create_engine('postgresql://replica.db', isolation_level='AUTOCOMMIT')\n\n-- def write(x):\n--     with pool_master.begin() as conn:\n--         conn.execute(...)\n-- def read(q):\n--     with pool_replica.connect() as conn:\n--         return conn.execute(q).fetchall()\n\n-- Beware: replication lag (qq ms à 10s)\n-- Si user vient d'écrire et lit immédiatement → read master\n\n-- 5. Logical Replication (par table)\n-- Permet de sélectionner ce qui est répliqué\n-- Cross-version possible (12 → 16)\n\n-- master:\nCREATE PUBLICATION trips_pub FOR TABLE trips;\n\n-- replica:\nCREATE SUBSCRIPTION trips_sub\n    CONNECTION 'host=master dbname=app user=repl'\n    PUBLICATION trips_pub;\n\n-- Use case: warehouses (que les tables business)\n\n-- 6. Patroni (orchestrateur HA)\n-- Cluster Postgres avec failover auto\n-- Configurations:\n-- - 3+ nodes (Raft consensus)\n-- - etcd ou Consul pour state\n-- - Patroni gère failover (60s typique)\n\n-- docker-compose:\n# patroni:\n#   image: zalando/patroni\n#   environment:\n#     PATRONI_NAME: db1\n#     PATRONI_ETCD3_HOSTS: etcd:2379\n#     PATRONI_POSTGRESQL_DATA_DIR: /pgdata\n# etcd:\n#   image: bitnami/etcd\n\n-- 7. PgBouncer (connection pooling)\n-- Postgres = 1 connexion = 1 process Linux\n-- 1000 connexions = 1000 processes (lourd)\n-- PgBouncer multiplexe (10000 client → 100 server)\n\n-- pgbouncer.ini:\n-- [databases]\n-- prod_db = host=master port=5432\n-- \n-- [pgbouncer]\n-- pool_mode = transaction\n-- max_client_conn = 10000\n-- default_pool_size = 100\n-- min_pool_size = 10\n\n-- 8. Backup strategies\n\n-- A. pg_dump (logique, par table)\npg_dump -h host -U user -F c -f backup.dump dbname\n-- Restore:\npg_restore -d new_db backup.dump\n-- Petit/moyen, dev\n\n-- B. pg_basebackup (binaire, full cluster)\npg_basebackup -h master -D /backup/base -X stream\n-- Production, fast restore\n\n-- C. Continuous archiving (WAL)\n-- archive_command = 'rsync %p backup-server:/wal/%f'\n-- Permet point-in-time recovery (PITR)\n\n-- D. WAL-E / WAL-G (S3 backup)\n-- envoie base + WAL vers S3 continuously\n\n-- 9. Cloud managed (production réelle)\n-- AWS RDS Postgres\n-- - Multi-AZ replication (sync, ~auto failover 60s)\n-- - Read replicas multi-region\n-- - Automated backups (35 jours)\n-- - Coût: 0.10€/h pour micro\n\n-- Aiven, Supabase, Neon: alternatives serverless\n-- Neon: branching (git-like sur DB)\n\n-- 10. Monitoring HA\n-- Métriques critiques:\n-- - replication_lag_bytes (master vs replica)\n-- - active_connections / max_connections\n-- - long_running_queries\n-- - dead_tuples (besoin VACUUM)\n-- - cache_hit_ratio (>99% = OK)\n\nSELECT\n    pg_wal_lsn_diff(pg_current_wal_lsn(), replay_lsn) AS lag_bytes\nFROM pg_stat_replication;\n\nSELECT\n    100 * blks_hit / NULLIF(blks_hit + blks_read, 0) AS cache_hit_ratio\nFROM pg_stat_database\nWHERE datname = current_database();",
      },
    ],
  },
];
